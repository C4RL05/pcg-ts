{
  "formatVersion": 1,
  "seed": 4211,
  "meta": {
    "title": "scatter a lap with as many markers as its own length asks for",
    "description": "THE COUNT IS NOT IN THIS FILE. Thirty-five markers land on this loop, and nowhere does the graph say thirty-five: `pointScatterOnPath`'s `count` is the expression `0.35 * length`, resolved against the path's OWN primitive-domain length column, which `pathResample` measured at 100.4906. Stretch the loop and the marker count follows it; there is no number to keep in step by hand.\n\nTHAT COLUMN IS `resampledLengthAttr` AND NOT `lengthAttr`, WHICH MATTERS MORE THAN THIS LOOP SHOWS. `lengthAttr` reports the INPUT curve; `resampledLengthAttr` reports the polyline through the 128 samples, which is the thing this node actually scatters onto. Here the two agree to ONE f32 ulp — 0.0759 parts per million — so the count is 35 under either and nothing on screen turns on the choice. Write the right one anyway.\n\nTHAT AGREEMENT IS AN ACCIDENT OF THE SAMPLE COUNT AND NOT A PROPERTY OF THE SHAPE, which is the actual reason. This loop is a 64-segment circle and 128 is twice 64, so every other sample lands exactly ON an input vertex, no chord spans a corner, and there is nothing to cut — the ulp above is two sums rounding to f32 rather than a shortfall anyone measured, the real gap being 0.0057 ppm. Change nothing but `count` to 127 and the SAME curve parts by 405 ppm. So the gap cannot be eyeballed off the picture, and it is not simply that coarser is worse: a 16-gon at this count reads exactly 0.00. It is what the chords MISS, and whether they miss anything is a question about where the samples fall relative to the bends. The sibling `basics-runs-along-a-path` reads 381 ppm not because its ring has 40 corners but because that ring is an ELLIPSE whose 40 segments are unequal, so no count lands them all on vertices; the same 40 corners on a circle read 0.00.\n\nA COUNT IS FORGIVING OF THAT AND `station` IS NOT. The arc position this node writes beside each marker is measured along the emitted polyline, so off `lengthAttr` the graph would size its population with one ruler and report every marker's place along it with another — which is the whole of what the two reports exist to prevent, and not a thing for the graph that TEACHES this node to get backwards.\n\nWHY THAT NEEDED A NODE. Every other arc-length placer in the library is deterministic-even — `pathResample` and `splineSample` divide a length into equal steps, `arcTile` walks a fixed spacing, `pathSegments` emits one point per segment — so a RANDOM population along a curve had to be composed: a source node for the count, a `setAttribute` for a random station, then `transferAlongPath` sampling `P` to pull the cloud onto the curve. That recipe still works and `basics-stations-on-a-path` still shows it. What it cannot do is decide HOW MANY.\n\nA source node emits points from nothing, so it has no element against which to read a field, and `fieldCapability.test.ts` refuses a field-capable param on one for exactly that reason. The question is never what a param decides — `pathResample.spacing` decides an output count and is field-capable — it is whether an element exists to read the param PER. A scatter that takes the path as an input has one: the polyline itself. So the count becomes a field, and the population becomes a property of the curve rather than a constant the author maintains beside it.\n\nONE COUNT PER POLYLINE, NOT ONE PER CLOUD. The field resolves on the primitive domain, so a geometry carrying four paths gets four independent counts and each path is scattered to its own. The emitted total is always the sum of them, so no path is ever silently skipped.\n\nWHAT THE MARKERS DO NOT CARRY. This node writes three things — the position on the curve, the arc position it was drawn at (`station` here), and the per-point seed. It does not write a tangent, and that is deliberate: `writeTangents`, `writeCurveFrame` and `transferAlongPath` already answer that question against the same shared arc table, and a fourth node measuring the same curve is how two nodes come to disagree about where the halfway point is. Orient these markers by gathering the tangent, the way the sibling graph does.",
    "tags": ["basics", "path", "curve", "closed", "scatter", "field", "instancing"]
  },
  "nodes": [
    {
      "id": "loop",
      "type": "subgraph",
      "params": { "count": 64, "size": [16, 16, 16] },
      "ref": { "name": "shape/path-loop" }
    },
    {
      "id": "measure",
      "type": "pathResample",
      "params": { "mode": "count", "count": 128, "resampledLengthAttr": "length" }
    },
    {
      "id": "scatter",
      "type": "pointScatterOnPath",
      "params": {
        "count": {
          "fn": "mul",
          "args": [0.35, { "fn": "attribute", "name": "length" }]
        },
        "arcAttr": "station",
        "seed": 7
      }
    },
    {
      "id": "size",
      "type": "setAttribute",
      "params": {
        "name": "scale",
        "tupleSize": 3,
        "value": { "fn": "vec", "args": [0.6, 1.8, 0.6] }
      }
    },
    {
      "id": "spawn",
      "type": "spawnInstances",
      "params": { "assetId": "marker" }
    }
  ],
  "connections": [
    { "from": ["loop", "out"], "to": ["measure", "in"] },
    { "from": ["measure", "out"], "to": ["scatter", "path"] },
    { "from": ["scatter", "out"], "to": ["size", "in"] },
    { "from": ["size", "out"], "to": ["spawn", "in"] }
  ],
  "outputs": [
    { "id": "measure", "pin": "out", "name": "lap" },
    { "id": "spawn", "pin": "instances", "name": "markers" }
  ]
}
