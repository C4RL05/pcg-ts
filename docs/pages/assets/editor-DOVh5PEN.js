import{$a as e,$i as t,Ai as n,Ao as r,As as i,Ba as a,Bi as o,C as s,Ca as c,Ci as l,D as u,Da as d,Di as f,E as p,Ea as m,Ei as h,Eo as g,Fi as _,Fr as v,Ga as y,Gi as b,Ha as x,Hi as S,Ia as C,Ii as w,Ji as T,Jr as E,K as D,Ki as O,Li as k,Lt as A,M as j,Ma as M,Mi as N,Ni as P,Ns as ee,Oa as te,Oi as F,Oo as ne,Pr as re,Pt as ie,Qa as ae,Qi as oe,R as se,Ri as I,S as ce,Sa as le,Si as ue,T as de,Ta as fe,Ti as pe,Tr as me,Tt as he,Ui as L,V as ge,Vi as R,Wa as z,Wi as _e,Xa as ve,Xi as B,Yi as V,Yt as ye,Zi as H,_ as be,_a as xe,_i as U,_t as Se,a as Ce,aa as we,ao as Te,b as Ee,ba as De,bi as Oe,bt as ke,c as Ae,ca as je,co as Me,d as Ne,do as Pe,ea as W,eo as Fe,f as Ie,ga as Le,gi as Re,gn as ze,gt as Be,h as Ve,hi as He,hs as Ue,ia as We,in as Ge,io as Ke,j as qe,ji as G,js as Je,jt as Ye,ki as Xe,l as Ze,lo as Qe,m as $e,mi as et,n as tt,na as K,no as nt,o as rt,oa as it,oo as at,pi as ot,q as st,qa as ct,qi as lt,qr as ut,ra as dt,s as ft,sa as q,t as pt,ta as mt,tn as ht,to as gt,u as _t,uo as vt,v as yt,va as bt,vi as J,w as xt,wa as St,wi as Y,x as Ct,xa as wt,xi as Tt,y as Et,ya as Dt,yi as Ot,ys as kt,zi as At}from"./wordmark-BbXPYQQk.js";import{t as jt}from"./scene-DEo0xTQE.js";import{a as Mt,i as Nt,t as Pt}from"./debug-B63qDzFF.js";import{n as Ft,t as It}from"./convert-iXUsy2m5.js";import{t as X}from"./preload-helper-HclGiUj8.js";var Lt=[te,z,d,M,a,m,x,C,ct,ve,y];function Rt(){for(let t of Lt)if(!e(t.type))throw Error(`standard node type "${t.type}" is missing from the registry even though its defining module was imported — the build reordered module evaluation (a code-splitting bug); rebuild with a config that preserves symbol-level import order, and check dist entry chunks with the dist smoke test (scripts/smoke-dist.mjs)`)}var zt=[`shape`,`fill`,`transform`,`compose`,`filter`,`place`,`write`],Bt=/^[a-z][a-z0-9]*(?:-[a-z0-9]+)*$/;function Vt(e,t){throw Error(`definePrimitive "${e}": ${t}`)}function Ht(e){let t=e.indexOf(`/`);t<0&&Vt(e,`a primitive name is "<family>/<kebab-case>" and this one has no "/"; families: ${zt.join(`, `)}`);let n=e.slice(0,t),r=e.slice(t+1);return zt.includes(n)||Vt(e,`"${n}" is not one of the seven families: ${zt.join(`, `)} — the family prefix is the only place a family can live, since a registry record carries no category`),Bt.test(r)||Vt(e,`"${r}" is not kebab-case; the part after the "/" is lowercase letters and digits with single dashes between them (fill/scatter-even), never camelCase — node types are camelCase and the two namespaces must not look alike`),n}function Z(e,t){let n=Ht(e);t.title.trim()===``&&Vt(e,`needs a non-empty title — it is the catalog's heading`),t.description.trim()===``&&Vt(e,`needs a non-empty description — the catalog is the only place an agent learns what this primitive is for`),t.outputs.length===0&&Vt(e,`declares no output pin, so nothing downstream could ever read it`);let r={formatVersion:1,seed:0,meta:{title:t.title,description:t.description,tags:[n,...t.tags??[]]},nodes:t.nodes,connections:t.connections,outputs:[]},i=t.params??[];if(i.length>0){let t=xe(r);for(let n of i){n.targets.length===0&&n.acceptsField!==!0&&Vt(e,`exposed param "${n.name}" has no targets and does not declare acceptsField: true; a targetless knob is read by the body's field expressions, where a Field splices in exactly as a number substitutes, so it IS field-capable — say so in the recipe, since that is what an agent reads before setting it`);try{c(t,{name:n.name,targets:n.targets.map(e=>({node:{id:e.node},param:e.param})),description:n.description,...n.default===void 0?{}:{default:n.default},...n.min===void 0?{}:{min:n.min},...n.max===void 0?{}:{max:n.max},...n.acceptsField===!0?{acceptsField:!0}:{}})}catch(t){Vt(e,t instanceof Error?t.message:String(t))}}}return De(e,{graph:r,...t.inputs===void 0?{}:{inputs:t.inputs},outputs:t.outputs,params:i.map(e=>({name:e.name,targets:e.targets,description:e.description,...e.default===void 0?{}:{default:e.default},...e.min===void 0?{}:{min:e.min},...e.max===void 0?{}:{max:e.max}}))})}function Ut(){Z(`compose/merge-tagged`,{title:`Merge two clouds and remember which is which`,description:"Concatenates two point clouds and stamps a string attribute on each side first, so the result remembers where every point came from — then `partitionByAttribute` can route them apart again, or the spawner can read the same attribute and give each source a different asset. ONE knob names the attribute on both sides, which is the point: a hand-written version has the same string typed twice and drifts the first time one is edited. Note that merging unions the attribute sets and FAILS when the two inputs disagree on a name's type, so a scratch column left on one side can break a merge that used to work. Fully deterministic. Writes the kind attribute; carries every other attribute through.",tags:[`merge`,`routing`],nodes:[{id:`tagA`,type:`setAttribute`,params:{name:`kind`,domain:`point`,type:`string`,tupleSize:1,value:0,values:[],stringValue:`a`}},{id:`tagB`,type:`setAttribute`,params:{name:`kind`,domain:`point`,type:`string`,tupleSize:1,value:0,values:[],stringValue:`b`}},{id:`merge`,type:`mergePoints`,params:{}}],connections:[{from:[`tagA`,`out`],to:[`merge`,`in`]},{from:[`tagB`,`out`],to:[`merge`,`in`]}],inputs:[{name:`a`,node:`tagA`,pin:`in`},{name:`b`,node:`tagB`,pin:`in`}],outputs:[{name:`out`,node:`merge`,pin:`out`}],params:[{name:`kindAttr`,targets:[{node:`tagA`,param:`name`},{node:`tagB`,param:`name`}],description:`Name of the string point attribute stamped on both sides. One knob writes both, so the two labels always land on the same attribute.`},{name:`nameA`,targets:[{node:`tagA`,param:`stringValue`}],description:"Label written on every point arriving at `a`."},{name:`nameB`,targets:[{node:`tagB`,param:`stringValue`}],description:"Label written on every point arriving at `b`."}]}),Z(`compose/scatter-copies`,{title:`Copy a whole cloud onto every target point`,description:"Copies the entire `source` cloud onto every point of `target`, composing the transforms per copy, then jitters the result so the repeated copies do not read as stamped. COUNT: the output is source x target points — 20 onto 500 is 10,000, and it multiplies fast. The transforms compose rather than overwrite: each copy is placed by the target's position, turned by the target's rotation and sized by the target's scale, so a target cloud carrying `rot` and `scale` lays its copies out already varied. VARIATION: yes — the jitter is context-seeded, so two instances differ, and `seed` re-rolls one explicitly. Reads and writes `P`; carries every source attribute through.",tags:[`copy`,`clusters`],nodes:[{id:`copy`,type:`copyToPoints`,params:{}},{id:`jit`,type:`jitterPoints`,params:{amount:[.5,0,.5],seed:0}}],connections:[{from:[`copy`,`out`],to:[`jit`,`in`]}],inputs:[{name:`source`,node:`copy`,pin:`source`},{name:`target`,node:`copy`,pin:`target`}],outputs:[{name:`out`,node:`jit`,pin:`out`}],params:[{name:`jitter`,targets:[{node:`jit`,param:`amount`}],description:`Random offset per axis, in world units, uniform and SYMMETRIC: each copy moves somewhere in -jitter..+jitter on each axis independently, so the spread is 2 * jitter wide, not jitter, and the measured extremes sit on the bound exactly. What makes copied clusters look placed rather than stamped. The default [0.5,0,0.5] jitters in the ground plane only — the zero on Y is what keeps copies from sinking through a surface. Set it to [0,0,0] for exact copies.`,acceptsField:!0},{name:`seed`,targets:[{node:`jit`,param:`seed`}],description:`Re-rolls the jitter. Two instances already differ without it.`}]})}var Wt=6.283185307179586;function Gt(){return{fn:`position`}}function Kt(e,t){return t===void 0?{fn:`attribute`,name:e}:{fn:`attribute`,name:e,tupleSize:t}}function qt(e){return{fn:`constant`,value:e}}function Q(e){return{fn:`param`,name:e}}function Jt(e){return{fn:`randomField`,key:e}}function $(e,...t){return{fn:e,args:[...t]}}function Yt(...e){return{fn:`vec`,args:[...e]}}function Xt(e,t){return{fn:`component`,args:[e],index:t}}function Zt(e,t){return{fn:`ramp`,args:[e],stops:t.map(e=>[e[0],e[1]])}}function Qt(e,t){return $(`add`,$(`mul`,Gt(),Q(e)),Yt(Q(t),Q(t),Q(t)))}function $t(e,t,n=4){return{fn:`fbm`,base:`perlinNoise`,opts:{normalized:!0,octaves:n,position:Qt(e,t)}}}function en(){Z(`fill/scatter-even`,{title:`Scatter points with a guaranteed minimum spacing`,description:"Scatters candidates through a box and then removes any that fall closer than a minimum distance, giving evenly spaced points with no visible clumping — for anything with physical extent: trees, rocks, buildings. COUNT: over-scatter deliberately. The output count is EMERGENT and approaches a ceiling of about 0.7 x area / minDistance squared from BELOW — the default 4000 candidates reach only about 85% of it — so raising `count` keeps adding a few points for a long time, while the way to get materially more is a smaller `minDistance`. The scan is a deterministic greedy pass in index order, not a Poisson-disc sample, so the count is not controllable and looping to hit a target count will not converge. VARIATION: yes — two instances in one graph scatter differently, and `seed` re-rolls one explicitly.",tags:[`scatter`,`spacing`],nodes:[{id:`scatter`,type:`pointScatterInBounds`,params:{count:4e3,boundsMin:[0,0,0],boundsMax:[50,0,50],seed:0}},{id:`prune`,type:`selfPrune`,params:{minDistance:2}}],connections:[{from:[`scatter`,`out`],to:[`prune`,`in`]}],outputs:[{name:`out`,node:`prune`,pin:`out`}],params:[{name:`count`,targets:[{node:`scatter`,param:`count`}],description:"Candidates scattered before pruning. Over-scatter, and expect to over-scatter HARD: the survivor count climbs slowly toward saturation instead of reaching it, so this knob still moves the result long after it looks like it should not. Saturation is about 0.7 * area / minDistance^2 — roughly 440 points in the default 50x50 box at minDistance 2 — and it takes on the order of 150 candidates per surviving point to get within a few percent of it. Measured in that box at minDistance 2: 4000 candidates give 374 (84% of saturation), 16000 give 416, 64000 give 436, 200000 give 444. Each 4x costs the next ~5%. Nothing here ever produces MORE than saturation, so a target count above it is unreachable at any `count`.",min:0},{name:`minDistance`,targets:[{node:`prune`,param:`minDistance`}],description:"Closest two kept points may be, in world units — and it is exact, not approximate: the measured nearest pair sits at minDistance to within a rounding error. This is the real knob, because the count follows it as an inverse square: the achievable maximum is about 0.7 * area / minDistance^2, so halving it makes room for four times as many points (and needs four times the `count` to find them).",min:0},{name:`boundsMin`,targets:[{node:`scatter`,param:`boundsMin`}],description:`Minimum corner of the box to fill, in world units.`},{name:`boundsMax`,targets:[{node:`scatter`,param:`boundsMax`}],description:`Maximum corner of the box to fill, in world units.`},{name:`seed`,targets:[{node:`scatter`,param:`seed`}],description:`Re-rolls the scatter. Two instances already differ without it.`}]}),Z(`fill/scatter-by-density`,{title:`Scatter points into noise-driven clumps`,description:"Scatters candidates through a box and keeps each one with a probability read from a noise field, so the points arrive in soft clumps instead of spread evenly — the single most common authoring situation in the library. COUNT: about `count` x 0.5 survive, since the normalized pattern averages 0.5. VARIATION: this is the MIXED case and the one an agent gets wrong. Which candidates survive varies per instance, but the PATTERN does not, so two instances put different points in the SAME clumps unless their `variant` differs. Writes `density`; reads nothing. Built on `filter/thin-by-density`, which is the same thinning applied to a cloud you already have.",tags:[`scatter`,`noise`,`density`],nodes:[{id:`scatter`,type:`pointScatterInBounds`,params:{count:4e3,boundsMin:[0,0,0],boundsMax:[100,0,100],seed:0}},{id:`thin`,type:`subgraph`,params:{},ref:{name:`filter/thin-by-density`}}],connections:[{from:[`scatter`,`out`],to:[`thin`,`in`]}],outputs:[{name:`out`,node:`thin`,pin:`out`}],params:[{name:`count`,targets:[{node:`scatter`,param:`count`}],description:`Candidates scattered before thinning. Roughly half survive.`,min:0},{name:`boundsMin`,targets:[{node:`scatter`,param:`boundsMin`}],description:`Minimum corner of the box to fill, in world units.`},{name:`boundsMax`,targets:[{node:`scatter`,param:`boundsMax`}],description:`Maximum corner of the box to fill, in world units.`},{name:`frequency`,targets:[{node:`thin`,param:`frequency`}],description:`Feature size of the clumps: smaller means broader clumps.`},{name:`variant`,targets:[{node:`thin`,param:`variant`}],description:`Offset added to the noise sample position — the only way to give two instances different CLUMPS rather than different points in the same clumps.`},{name:`seed`,targets:[{node:`scatter`,param:`seed`},{node:`thin`,param:`seed`}],description:"Re-rolls both the candidate positions and which of them survive. It does NOT move the clumps; `variant` does that."}]}),Z(`fill/scatter-clustered`,{title:`Scatter points in groups rather than spread out`,description:"Scatters a few cluster centres through a box, then copies a small local cloud onto each one, so points arrive in groups — villages, groves, boulder fields. COUNT: the output is clusters x perCluster exactly, which multiplies fast. SHAPE: a group is a BOX running -spread to +spread around its centre and all three components of `spread` are live. It ships flat — the default is [4, 0, 4], a ground-plane patch, because that is what a village or a grove is — so raise `spread.y` to make the group volumetric (a swarm, an asteroid field, a cave's boulders) rather than reaching for a box scatter. The groups OVERHANG the bounds by up to `spread`, deliberately and unclamped: `boundsMin`/`boundsMax` place the CENTRES, and a group straddling the edge is a whole group rather than a clipped one — inset the bounds by `spread` if the points themselves have to stay inside a region. VARIATION: yes — both scatters are context-seeded, so two instances differ, and `seed` re-rolls both explicitly. The local cloud's own `scale` is reset before copying, so `spread` sizes the cluster and not the assets in it.",tags:[`scatter`,`clusters`],nodes:[{id:`centres`,type:`pointScatterInBounds`,params:{count:24,boundsMin:[0,0,0],boundsMax:[100,0,100],seed:0}},{id:`local`,type:`pointScatterInBounds`,params:{count:12,boundsMin:[-1,-1,-1],boundsMax:[1,1,1],seed:0}},{id:`spread`,type:`transformPoints`,params:{scale:[4,0,4],rotateEuler:[0,0,0],translate:[0,0,0]}},{id:`reset`,type:`setAttribute`,params:{name:`scale`,domain:`point`,type:`f32`,tupleSize:3,value:qt([1,1,1])}},{id:`copy`,type:`copyToPoints`,params:{}}],connections:[{from:[`local`,`out`],to:[`spread`,`in`]},{from:[`spread`,`out`],to:[`reset`,`in`]},{from:[`reset`,`out`],to:[`copy`,`source`]},{from:[`centres`,`out`],to:[`copy`,`target`]}],outputs:[{name:`out`,node:`copy`,pin:`out`}],params:[{name:`clusters`,targets:[{node:`centres`,param:`count`}],description:`How many groups to place.`,min:0},{name:`perCluster`,targets:[{node:`local`,param:`count`}],description:`How many points each group holds. The output count is clusters x this. One local cloud is scattered and then copied onto every centre, so every group is the SAME arrangement translated — this is also how finely a group's shape is sampled, and two groups differ by position, not by shape.`,min:0},{name:`spread`,targets:[{node:`spread`,param:`scale`}],description:'Half-extent of one group, per axis, in world units: its points land uniformly in a BOX running -spread to +spread around the centre, so a group measures 2 * spread across and the reach is exactly linear in the value. The peak sits just under `spread` because it is `perCluster` uniform draws deep, not because the bound is different: measured 0.88 to 0.96 of it at the default 12 points per group, 0.995 at 300. All three axes are live, Y included. It DEFAULTS to [4, 0, 4] — a flat ground patch, where every point of a group shares its centre\'s Y exactly — because the common group stands on the ground; [4, 4, 4] gives a cube-shaped group instead, and [4, 12, 4] a column. The box bounds the POINTS while `boundsMin`/`boundsMax` bound only the CENTRES, so the result overhangs the box by up to `spread` on each side, on Y as well once the group has height. Nothing clamps that, on purpose — inset the bounds by `spread` when the points must stay inside a region. A bare number is not accepted: pass [4, 0, 4], or {"fn":"constant","value":4} for equal reach on all three axes.',acceptsField:!0},{name:`boundsMin`,targets:[{node:`centres`,param:`boundsMin`}],description:`Minimum corner of the box the group centres can land in, in world units.`},{name:`boundsMax`,targets:[{node:`centres`,param:`boundsMax`}],description:`Maximum corner of the box the group centres can land in, in world units.`},{name:`seed`,targets:[{node:`centres`,param:`seed`},{node:`local`,param:`seed`}],description:`Re-rolls both the group positions and the shape of the group. Two instances already differ without it.`}]}),Z(`fill/volume-by-noise`,{title:`Carve connected volumes out of a solid box`,description:"Fills a box with a jittered grid of points and keeps only the cells where a 3D noise field rises above a threshold, carving connected volumes out of solid — caves, clouds, asteroid interiors, floating islands. COUNT: the grid is extent cubed over cellSize cubed, so this is the one primitive here that can blow up; halving `cellSize` costs eight times as many points in a three-dimensional region (four over a flat one) before the threshold takes any away. Connect the `in` pin and the bounds come from that geometry's own extents instead of the params, which is the library's only 'adapt to whatever arrives' mechanism. VARIATION: the jitter varies per instance but the CARVE PATTERN does not, so two instances hollow out the same shape unless their `variant` differs.",tags:[`noise`,`volume`],nodes:[{id:`vol`,type:`volumeSample`,params:{boundsMin:[0,0,0],boundsMax:[32,32,32],cellSize:2,jitter:.5,seed:0}},{id:`carve`,type:`filterByExpression`,params:{predicate:$(`ge`,$t(`frequency`,`variant`),Q(`threshold`)),seed:0}}],connections:[{from:[`vol`,`out`],to:[`carve`,`in`]}],inputs:[{name:`in`,node:`vol`,pin:`in`}],outputs:[{name:`out`,node:`carve`,pin:`out`}],params:[{name:`cellSize`,targets:[{node:`vol`,param:`cellSize`}],description:"Grid resolution in world units. The candidate count is floor(extent / cellSize) per axis — at least 1, whole cells only, no partial cell at the far edge — multiplied together, so halving it costs EIGHT times as many points only where the region is fully three-dimensional: over a flat region — a plane, or any scatter with a constant Y — it is four times, and along a line twice. Measured on the default 32-unit box: cellSize 8 gives 64 candidates, 4 gives 512, 2 gives 4096, 1 gives 32768, exactly 8x per halving because 32 divides by all of them. Measured on the same box arriving through the `in` pin, where a scatter's extent lands just under 32 and every axis loses its last whole cell: 27, 343, 3375, 29791 (3, 7, 15 and 31 per axis), with the ratios running 12.7x, 9.8x, 8.8x down toward 8. The threshold then takes some away; this is the count BEFORE it."},{name:`jitter`,targets:[{node:`vol`,param:`jitter`}],description:"How far each point may wander from its own lattice node: 0 is a hard lattice, 1 is fully irregular. The offset is uniform on each axis, exactly linear in the value, and bounded by HALF A CELL structurally rather than statistically — a point never leaves the cell it was generated in, at any jitter, so neighbours keep their grid order, never cross, and stay at least (1 - jitter) * cell apart. Build non-overlap on that. What that bound is NOT is half of the `cellSize` you typed, and the difference surprises: the grid divides the extent into floor(extent / `cellSize`) whole cells, so the real cell is extent / that — equal to `cellSize` only when the extent is a whole multiple of it, wider otherwise. Measured on the default 32-unit box at cellSize 2 (16 whole cells of exactly 2): +/-1.0 at jitter 1. Measured with the bounds taken from the `in` pin, where the extent lands just under 32 and leaves 15 cells of 2.13: +/-1.066 — 0.53 of the number typed, and still exactly half of its own cell.",min:0,max:1,acceptsField:!0},{name:`threshold`,targets:[],default:.5,description:"Where the cut falls on the 0..1 noise. Higher leaves less material, but the noise only reaches the MIDDLE of that range, so the whole knob lives between about 0.35 (solid) and 0.68 (empty) — four octaves of normalized fBm are bell-shaped around 0.5 and never near the ends. Measured on the default 32-unit box at cellSize 2: 0.3 keeps 100% of the grid, 0.45 keeps 85%, 0.5 keeps 65%, 0.55 keeps 41%, 0.6 keeps 18%, 0.65 keeps 3.6%, 0.7 keeps nothing. The centre of that band is NOT fixed at 0.5 the way the flat filters' is: the box times `frequency` decides how much of the field is sampled, and a small window sits wherever its own patch of noise happens to sit (the same box read as a 2D spread has 0.5 keeping half rather than two thirds). Sweep in steps of 0.02, not 0.1.",min:0,max:1,acceptsField:!0},{name:`frequency`,targets:[],default:.05,description:`Feature size: smaller means larger, smoother caverns.`,acceptsField:!0},{name:`variant`,targets:[],default:0,description:`Offset added to the noise sample position — the per-instance re-roll of the carve pattern.`,acceptsField:!0},{name:`boundsMin`,targets:[{node:`vol`,param:`boundsMin`}],description:"Minimum corner of the box to fill. IGNORED when the `in` pin is connected."},{name:`boundsMax`,targets:[{node:`vol`,param:`boundsMax`}],description:"Maximum corner of the box to fill. IGNORED when the `in` pin is connected."},{name:`seed`,targets:[{node:`vol`,param:`seed`}],description:"Re-rolls the per-cell jitter. It does NOT move the carve pattern; `variant` does that."}]})}function tn(){Z(`filter/thin-by-density`,{title:`Thin a point cloud by a noise density`,description:'Writes a normalized noise field into the standard `density` attribute and keeps each point with a probability equal to its density, so dense regions stay full and sparse ones fade out. The result is SOFT-EDGED: individual points thin out gradually, with no boundary. For hard-edged regions with a visible coastline, use `filter/mask-by-noise` instead. VARIATION: which points survive varies per instance (the draw is context-seeded), but the PATTERN does not — this field writes no `opts.seed` at all, and that slot is closed to expressions anyway: it takes an integer, or the tagged `{"from": "node", "variant": N}` form whose `variant` may be an inline `param`, and nothing else, because a field column is f32 and a seed rounded through one avalanches to an unrelated noise — two instances thin the same blobs unless their `variant` differs. Writes `density`; reads `P`.',tags:[`noise`,`density`],nodes:[{id:`densityAttr`,type:`setAttribute`,params:{name:`density`,domain:`point`,type:`f32`,tupleSize:1,value:$t(`frequency`,`variant`)}},{id:`thin`,type:`filterByDensity`,params:{mode:`probabilistic`,threshold:.5,seed:0}}],connections:[{from:[`densityAttr`,`out`],to:[`thin`,`in`]}],inputs:[{name:`in`,node:`densityAttr`,pin:`in`}],outputs:[{name:`out`,node:`thin`,pin:`out`}],params:[{name:`frequency`,targets:[],default:.02,description:`Feature size: the noise sample position is multiplied by this, so smaller means broader clumps. 0.02 gives clumps tens of world units across.`,acceptsField:!0},{name:`variant`,targets:[],default:0,description:`Offset added to the noise sample position — the only way to give two instances different PATTERNS. Any two different values are unrelated; the same value always reproduces.`,acceptsField:!0},{name:`seed`,targets:[{node:`thin`,param:`seed`}],description:"Re-rolls which points survive within the same pattern. It does NOT move the pattern; `variant` does that."}]}),Z(`filter/mask-by-noise`,{title:`Keep the points where a noise field is above a threshold`,description:"Keeps only the points where a normalized noise field rises above a threshold — a HARD mask, giving connected regions with visible edges, the way a coastline separates land from sea. For a soft, gradual fade instead, use `filter/thin-by-density`. On normalized noise a threshold of 0.5 keeps roughly half the area, and higher keeps less — but the usable band is only about 0.32 to 0.68, not the full 0..1 the param's range suggests; the `threshold` description gives the measured table. VARIATION: none by default — noise carries its own seed inside its field spec, so two instances mask IDENTICALLY unless their `variant` differs. Reads `P`; writes nothing at all — the whole test is one field expression, so no scratch column is created to be cleaned up.",tags:[`noise`,`mask`],nodes:[{id:`mask`,type:`filterByExpression`,params:{predicate:$(`ge`,$t(`frequency`,`variant`),Q(`threshold`)),seed:0}}],connections:[],inputs:[{name:`in`,node:`mask`,pin:`in`}],outputs:[{name:`out`,node:`mask`,pin:`out`}],params:[{name:`threshold`,targets:[],default:.5,description:"Where the cut falls on the 0..1 noise — but the noise only reaches the MIDDLE of that range, so the whole knob lives between about 0.32 (keeps everything) and 0.68 (keeps nothing). Four octaves of normalized fBm come out bell-shaped around 0.5 with a standard deviation near 0.065, never at the ends: the theoretical 0..1 is the nominal range of the term, not the values it takes. Measured on a wide 2D spread: 0.42 keeps ~89%, 0.46 ~75%, 0.50 ~50%, 0.55 ~23%, 0.59 ~9%. So a threshold of 0.8 does not keep a fifth, it keeps NOTHING, and 0.2 does not keep four fifths, it keeps everything. `frequency`, `variant` and how widely the points sample the field move the two ends by a few hundredths.",min:0,max:1,acceptsField:!0},{name:`frequency`,targets:[],default:.02,description:`Feature size: the noise sample position is multiplied by this, so smaller means larger regions.`,acceptsField:!0},{name:`variant`,targets:[],default:0,description:`Offset added to the noise sample position — the per-instance re-roll. Two instances with the same value mask identically.`,acceptsField:!0}]}),Z(`filter/inside-radius`,{title:`Keep the points within a radius of a centre`,description:"Keeps the points whose distance to a centre satisfies a comparison — 'le' for a circular district, 'ge' for an exclusion zone around a landmark. The distance is the true 3D distance, not a squared one and not a planar one, which are the two ways a hand-written version goes wrong. Fully deterministic. Reads `P`; writes nothing (the scratch distance column is removed again).",tags:[`spatial`,`mask`],nodes:[{id:`distAttr`,type:`setAttribute`,params:{name:`__radial`,domain:`point`,type:`f32`,tupleSize:1,value:$(`length`,$(`sub`,Gt(),Q(`center`)))}},{id:`keep`,type:`filterByAttribute`,params:{attribute:`__radial`,comparison:`le`,value:10,stringValue:``}},{id:`cleanup`,type:`removeAttribute`,params:{names:[`__radial`],domain:`point`,strict:!0}}],connections:[{from:[`distAttr`,`out`],to:[`keep`,`in`]},{from:[`keep`,`out`],to:[`cleanup`,`in`]}],inputs:[{name:`in`,node:`distAttr`,pin:`in`}],outputs:[{name:`out`,node:`cleanup`,pin:`out`}],params:[{name:`center`,targets:[],default:[0,0,0],description:`World position the distance is measured from, as three numbers [x, y, z]. It is read straight into the distance expression, so the whole triple is set at once — a bare number is not accepted, and the origin is [0, 0, 0]. Field-capable, and resolved on the incoming points: a field moves the centre PER POINT, so distance can be measured from a per-cluster origin rather than from one place. A field may be scalar and broadcasts across all three axes when it is; a plain [x, y, z] is the ordinary case.`,acceptsField:!0},{name:`radius`,targets:[{node:`keep`,param:`value`}],description:`The distance the comparison is made against, in world units.`,min:0},{name:`comparison`,targets:[{node:`keep`,param:`comparison`}],description:`How the distance is tested: 'le' keeps what is inside the radius, 'ge' keeps what is outside it.`}]}),Z(`filter/by-distance-to`,{title:`Keep points by how far they are from another cloud`,description:"Measures each point's distance to the nearest point of a second cloud and keeps or drops it by that distance — 'no trees within 20m of the road', or 'cabins only near the lake'. This is the only way to ask how far anything is from anything: transferring the nearest value copies it but never reveals the distance. A point that finds nothing (an empty `features` cloud) is at distance Infinity, so 'le' drops it and 'ge' keeps it. Fully deterministic. Reads `P` on both inputs; writes nothing (the distance column is removed again).",tags:[`spatial`,`proximity`],nodes:[{id:`near`,type:`sampleNearestPoint`,params:{distanceAttr:`__nearDist`,indexAttr:``,attribute:``,outAttribute:``,maxDistance:0}},{id:`keep`,type:`filterByAttribute`,params:{attribute:`__nearDist`,comparison:`ge`,value:5,stringValue:``}},{id:`cleanup`,type:`removeAttribute`,params:{names:[`__nearDist`],domain:`point`,strict:!0}}],connections:[{from:[`near`,`out`],to:[`keep`,`in`]},{from:[`keep`,`out`],to:[`cleanup`,`in`]}],inputs:[{name:`in`,node:`near`,pin:`in`},{name:`features`,node:`near`,pin:`source`}],outputs:[{name:`out`,node:`cleanup`,pin:`out`}],params:[{name:`distance`,targets:[{node:`keep`,param:`value`}],description:`The band edge, in world units.`,min:0},{name:`comparison`,targets:[{node:`keep`,param:`comparison`}],description:`'ge' keeps what is far from the features (a clearance), 'le' keeps what is near them (a band).`}]}),Z(`filter/by-neighbor-count`,{title:`Keep points by how crowded they are`,description:"Counts each point's neighbours within a radius and keeps or drops it by that count — 'ge' removes lonely outliers and finds cluster cores, 'le' thins the dense middle out. Fully deterministic: it measures whatever arrives. Reads `P`; writes nothing (the count column is removed again).",tags:[`neighborhood`,`spatial`],nodes:[{id:`nbr`,type:`pointNeighborhood`,params:{radius:5,maxCount:0,includeSelf:!1,countAttr:`__nbrCount`,averageAttr:``,averageOutAttr:`nbrAvg`}},{id:`keep`,type:`filterByAttribute`,params:{attribute:`__nbrCount`,comparison:`ge`,value:2,stringValue:``}},{id:`cleanup`,type:`removeAttribute`,params:{names:[`__nbrCount`],domain:`point`,strict:!0}}],connections:[{from:[`nbr`,`out`],to:[`keep`,`in`]},{from:[`keep`,`out`],to:[`cleanup`,`in`]}],inputs:[{name:`in`,node:`nbr`,pin:`in`}],outputs:[{name:`out`,node:`cleanup`,pin:`out`}],params:[{name:`radius`,targets:[{node:`nbr`,param:`radius`}],description:`How far around each point counts as its neighbourhood, in world units. As a FIELD it is a PER-POINT radius, so each point measures the neighbourhood it asks for and the relation stops being symmetric — B within A's reach does not put A within B's.`,min:0},{name:`count`,targets:[{node:`keep`,param:`value`}],description:`How many neighbours the comparison is made against. The point itself is not counted.`,min:0},{name:`comparison`,targets:[{node:`keep`,param:`comparison`}],description:`'ge' keeps the crowded points (cluster cores), 'le' keeps the isolated ones.`}]}),Z(`filter/by-distance-to-curve`,{title:`Keep points by how far they are from a curve`,description:"Measures each point's distance to the supplied `curve` and keeps or drops it by that distance — a clearance either side of a road, a band of reeds along a river, a strip of lamps beside a path. The densification is the content: a polyline's own points can be tens of metres apart, and measuring to THEM instead of to the curve reports huge distances mid-segment and cuts scalloped bites out of the result. The curve is therefore sampled every `resolution` units first, and the measurement is against those samples, so `resolution` is the accuracy of the answer. PRECONDITION: `curve` must carry polyline topology (`shape/path-loop`, `shape/path-meander`, or a `pointsToPath` node) — a point cloud is rejected as having no polylines. Several separate paths are all measured against; the nearest one wins. Fully deterministic. Reads `P` on both inputs; writes nothing.",tags:[`path`,`spatial`,`proximity`],nodes:[{id:`dense`,type:`splineSample`,params:{mode:`spacing`,count:10,spacing:1}},{id:`dist`,type:`subgraph`,params:{},ref:{name:`filter/by-distance-to`}}],connections:[{from:[`dense`,`out`],to:[`dist`,`features`]}],inputs:[{name:`in`,node:`dist`,pin:`in`},{name:`curve`,node:`dense`,pin:`in`}],outputs:[{name:`out`,node:`dist`,pin:`out`}],params:[{name:`distance`,targets:[{node:`dist`,param:`distance`}],description:`The band edge, in world units — how far from the curve the decision flips.`,min:0},{name:`comparison`,targets:[{node:`dist`,param:`comparison`}],description:`'ge' keeps what is far from the curve (a clearance either side of it), 'le' keeps what runs alongside it (a band).`},{name:`resolution`,targets:[{node:`dense`,param:`spacing`}],description:"How finely the curve is sampled before distances are measured, in world units. It is the accuracy of the measurement, and it only ever UNDERSTATES the band — sampling a curve sparsely puts every point further from the nearest sample than it is from the curve, so points are lost from the edges, never gained. Measured against a 5-unit band on a straight curve: resolution 1 (a fifth of `distance`) loses 0.2% of the points, 2 loses 0.7%, 5 (equal to `distance`) loses 4%, 10 loses 18% and 20 loses half. A fifth of `distance` is the practical floor; below that it only costs sample points, one more per step along every path.",min:0}]})}var nn=$(`mul`,Jt(`radial`),$(`mul`,Wt,Q(`spread`))),rn=$(`add`,$(`mul`,$(`cos`,nn),Kt(`curveNormal`,3)),$(`mul`,$(`sin`,nn),Kt(`curveBinormal`,3)));function an(){Z(`place/on-surface`,{title:`Scatter points across a mesh with height and slope`,description:"Scatters points over a triangle mesh with probability proportional to triangle area, then stamps the two standard terrain quantities — `height` (world Y) and `slope` (1 - normal.y) — so downstream filters have something to test without re-deriving it. The points also carry the flat per-triangle `normal` the sampler writes. COUNT: `count` is the number of CANDIDATES; with the default density of 1 every one is kept, and a lower `density` keeps proportionally fewer. VARIATION: yes — two instances in one graph sample differently, and `seed` re-rolls one explicitly. Writes `height`, `slope`, `normal`, `density`.",tags:[`surface`,`terrain`,`scatter`],nodes:[{id:`sample`,type:`surfaceSample`,params:{count:1e3,seed:0,densityField:1}},{id:`terrain`,type:`subgraph`,params:{},ref:{name:`write/height-slope`}}],connections:[{from:[`sample`,`out`],to:[`terrain`,`in`]}],inputs:[{name:`surface`,node:`sample`,pin:`in`}],outputs:[{name:`out`,node:`terrain`,pin:`out`}],params:[{name:`count`,targets:[{node:`sample`,param:`count`}],description:`Candidates placed on the mesh before density acceptance.`,min:0},{name:`density`,targets:[{node:`sample`,param:`densityField`}],description:`Acceptance probability per candidate, 0..1 — where sampling is allowed at all. Pass a field spec to make it vary across the surface.`,acceptsField:!0},{name:`seed`,targets:[{node:`sample`,param:`seed`}],description:`Re-rolls the sampling. Two instances already differ without it.`}]}),Z(`place/plantable`,{title:`Scatter points only where vegetation could grow`,description:"Scatters points on a mesh and keeps only the ones on gentle enough ground below a height limit — the standard 'where can vegetation go' test, and the shape every forest in the demo corpus is built from. `maxSlope` is on the 0..1 scale `place/on-surface` writes, where 0 is dead flat: 0.3 is about a 45-degree limit. VARIATION: yes, through the scatter. Built on `place/on-surface`, so the output carries `height`, `slope`, `normal` and `density` too.",tags:[`surface`,`terrain`,`vegetation`],nodes:[{id:`pts`,type:`subgraph`,params:{},ref:{name:`place/on-surface`}},{id:`slope`,type:`filterByAttribute`,params:{attribute:`slope`,comparison:`le`,value:.3,stringValue:``}},{id:`height`,type:`filterByAttribute`,params:{attribute:`height`,comparison:`le`,value:60,stringValue:``}}],connections:[{from:[`pts`,`out`],to:[`slope`,`in`]},{from:[`slope`,`out`],to:[`height`,`in`]}],inputs:[{name:`surface`,node:`pts`,pin:`surface`}],outputs:[{name:`out`,node:`height`,pin:`out`}],params:[{name:`count`,targets:[{node:`pts`,param:`count`}],description:`Candidates placed on the mesh before the slope and height tests.`,min:0},{name:`density`,targets:[{node:`pts`,param:`density`}],description:`Acceptance probability per candidate, 0..1. Pass a field spec to make it vary across the surface.`,acceptsField:!0},{name:`seed`,targets:[{node:`pts`,param:`seed`}],description:`Re-rolls the sampling. Two instances already differ without it.`},{name:`maxSlope`,targets:[{node:`slope`,param:`value`}],description:"Steepest ground still plantable, on the `slope` scale `write/height-slope` writes — which is 1 - cos(angle), NOT a fraction of 90 degrees, so it is heavily compressed at the flat end and half the scale already covers two thirds of the range of real slopes. The default 0.3 is therefore a 45-degree limit, not the 27-degree one a linear reading gives. The anchors, measured: 10 degrees is 0.015, 20 is 0.060, 30 is 0.134, 45 is 0.293, 60 is 0.500, 75 is 0.741, 90 is 1. Inverted, for a limit of A degrees pass 1 - cos(A)."},{name:`maxHeight`,targets:[{node:`height`,param:`value`}],description:`Highest world Y still plantable — a tree line.`}]}),Z(`place/drop-to-surface`,{title:`Drop points onto a mesh and discard the misses`,description:"Casts a ray from every point along a direction, moves each one to where it hits the mesh, and DISCARDS the ones that hit nothing — which is what turns any flat scatter into a terrain-aware one. THREE nodes casting ONE ray: the transfer moves `P` to the hit AND reports per point whether it found anything, so the filter that discards the misses reads the outcome of the very ray that did the moving. It used to be five nodes and two rays, the second cast only to recover what the first already knew; there is nothing left to get out of order. A miss keeps its prior position and is filtered out. Fully deterministic. Reads and writes `P`; the internal `__onSurface` flag column (bool, tuple 1) is removed again. PRECONDITION: the input must not already carry `__onSurface` under a different shape — the hit flag refuses to delete an existing column, so it stops instead of cooking something plausible; an input holding the name as a bool is harmless, since every point is rewritten.",tags:[`surface`,`raycast`,`terrain`],nodes:[{id:`snap`,type:`transferAttribute`,params:{name:`P`,mapping:`raycast`,attrDomain:`point`,uvAttr:`uv`,direction:[0,-1,0],directionAttr:``,maxDistance:0,missCountAttr:``,hitAttr:`__onSurface`}},{id:`keep`,type:`filterByAttribute`,params:{attribute:`__onSurface`,comparison:`eq`,value:1,stringValue:``}},{id:`cleanup`,type:`removeAttribute`,params:{names:[`__onSurface`],domain:`point`,strict:!0}}],connections:[{from:[`snap`,`out`],to:[`keep`,`in`]},{from:[`keep`,`out`],to:[`cleanup`,`in`]}],inputs:[{name:`points`,node:`snap`,pin:`in`},{name:`surface`,node:`snap`,pin:`source`}],outputs:[{name:`out`,node:`cleanup`,pin:`out`}],params:[{name:`direction`,targets:[{node:`snap`,param:`direction`}],description:`Which way the ray travels. [0,-1,0] drops straight down; rays are forward-only, so points below the surface miss.`},{name:`maxDistance`,targets:[{node:`snap`,param:`maxDistance`}],description:`Longest drop that still counts as a landing, in world units. 0 means unlimited.`,min:0}]}),Z(`place/along-curve`,{title:`Space points along a curve and turn them to follow it`,description:"Places points at even arc-length steps along every path of the supplied `curve` and turns each one to face the way the curve is going — fence posts, streetlights, bollards, sleepers. Each path is measured and resampled on its OWN length, so several paths in one input stay separate and each gets its own run of points; `splineSample` would treat them as one concatenated curve instead. PRECONDITION: `curve` must carry polyline topology — `shape/path-loop`, `shape/path-meander` or a `pointsToPath` node, never a bare point cloud, and never anything that has been through a step that can REMOVE points: the `filter/*` family, `partitionByAttribute` and `mergePoints` all destroy topology, and `filterByAttribute` does so even when its predicate keeps every point. Category is not the rule — `projectToPlane` is a `filter` that preserves it, because it clones. The points are NEW: they carry `P`, the unit `tangent`, `curveU` (0..1 along their own path) and `rot`, plus the standard attributes at their defaults. Nothing written on the curve's own POINTS survives, which is what `write/orient-along-path` is for — but every PRIMITIVE attribute does: a post placed along a road carries that road's `roadWidth` and `roadKind`, because a sample inherits the primitive it was taken from. The output is still a path, so it can be resampled again. Fully deterministic.",tags:[`curve`,`path`,`instancing`],nodes:[{id:`resample`,type:`pathResample`,params:{mode:`count`,count:24,spacing:1}},{id:`orient`,type:`orientAlongVector`,params:{direction:Kt(`tangent`,3),up:[0,1,0],axis:`+z`}}],connections:[{from:[`resample`,`out`],to:[`orient`,`in`]}],inputs:[{name:`curve`,node:`resample`,pin:`in`}],outputs:[{name:`out`,node:`orient`,pin:`out`}],params:[{name:`mode`,targets:[{node:`resample`,param:`mode`}],description:"'count' puts exactly `count` points on each path whatever its length; 'spacing' steps every `spacing` world units, so longer paths get more points — the right one for evenly pitched props."},{name:`count`,targets:[{node:`resample`,param:`count`}],description:`Points per path in 'count' mode: exactly this many come out, whatever the path's length, and they are evenly spaced at length / (count - 1) — so a 40-unit path at count 5 pitches them every 10 units, and the two ends are always occupied. At least 2 (3 on a closed path); ignored in 'spacing' mode.`,min:2},{name:`spacing`,targets:[{node:`resample`,param:`spacing`}],description:"Distance between points in world units in 'spacing' mode — exact for every step except the LAST, which is the leftover. The walk starts at the beginning of each path, steps `spacing` until another step would overshoot, then puts a final point exactly on the end: a 40-unit path at spacing 7 comes out with gaps 7, 7, 7, 7, 7, 5. So the count per path is floor(length / spacing) + 2, or length / spacing + 1 when it divides exactly, and the far end is always the short one. For props that must be evenly pitched the whole way, pick a `spacing` that divides the path length. Must be greater than 0 and short enough to leave 2 points on the shortest path; ignored in 'count' mode.",min:0},{name:`axis`,targets:[{node:`orient`,param:`axis`}],description:`Which local axis of the asset points along the curve. '+z' is the forward axis assets face by convention.`},{name:`up`,targets:[{node:`orient`,param:`up`}],description:`Up hint fixing the roll around the curve; leave it at world up for props that stand on the ground.`}]}),Z(`place/radial-on-curve`,{title:`Place points along a curve and aim them radially`,description:"Places points at even arc-length steps along every path of the supplied `curve`, then rolls each one to a random angle AROUND the curve — spikes on a mace, bristles on a brush, brackets round a mast, leaves up a stem, buds on a branch. It is deliberately distinct from `place/along-curve`, which spaces points the same way but aims them ALONG the tangent with a CONSTANT world `up`: that fixes every asset in the same world orientation, and it flips them a half turn wherever the curve turns over. Here the up hint is per point — cos(a) * `curveNormal` + sin(a) * `curveBinormal`, the unit vector at a random angle a in the plane perpendicular to the tangent, taken from the rotation-minimizing frame `write/curve-frame` describes — so the assets fan out around the path and the fan follows the path however it bends. A constant up simply cannot express that, which is the whole reason to reach for this one. GEOMETRY: the asset's local +z runs along the curve and its local +y is the radial direction, so a prop that must stick OUT of the path wants its length on +y. The points are NEW: they carry `P`, the unit `tangent`, `curveU`, `curveNormal`, `curveBinormal` and `rot`, plus the standard attributes at their defaults, and nothing written on the curve's own POINTS survives — use `write/curve-frame` and an `orientAlongVector` of your own if it must. Every PRIMITIVE attribute does survive, since a sample inherits the polyline it was taken from. PRECONDITION: `curve` must carry polyline topology and must not have been through anything that can REMOVE points — the `filter/*` family, `partitionByAttribute` and `mergePoints` all destroy it, and `filterByAttribute` does so even when its predicate keeps every point. VARIATION: yes — the angle is drawn from the evaluation context, so two instances in one graph fan differently on their own; there is no seed knob, so an explicit re-roll means a different `spread`. A CLOSED path does not come back seamless: the frame is transported around the loop and returns rotated by that curve's residual angle, so the fan does not line up across the seam. The output is still a path and can be resampled again.",tags:[`curve`,`path`,`instancing`],nodes:[{id:`resample`,type:`pathResample`,params:{mode:`count`,count:24,spacing:1}},{id:`frame`,type:`writeCurveFrame`,params:{tangentName:`tangent`,normalName:`curveNormal`,binormalName:`curveBinormal`}},{id:`orient`,type:`orientAlongVector`,params:{direction:Kt(`tangent`,3),up:rn,axis:`+z`}}],connections:[{from:[`resample`,`out`],to:[`frame`,`in`]},{from:[`frame`,`out`],to:[`orient`,`in`]}],inputs:[{name:`curve`,node:`resample`,pin:`in`}],outputs:[{name:`out`,node:`orient`,pin:`out`}],params:[{name:`count`,targets:[{node:`resample`,param:`count`}],description:"Points per path: exactly this many come out whatever the path's length, evenly spaced at length / (count - 1), and both ends are always occupied. At least 2 (3 on a closed path). For a fixed pitch in world units instead, resample with `place/along-curve` in 'spacing' mode first and feed the result in here.",min:2},{name:`spread`,targets:[],default:1,description:"How much of a full turn around the curve the fan covers, as a fraction: 1 spreads uniformly over the whole circle, 0.25 over a quarter turn, and 0 aims every point the same way — straight along the transported `curveNormal`, which is a smooth ribbon rather than a fan, and the one setting that does NOT vary between two instances. The angle is drawn uniformly over 0..spread turns, so it is one-sided: the fan opens from the normal in one direction only, and a spread of 0.5 covers a half turn from it rather than a quarter turn either side. Values above 1 wrap and buy nothing.",min:0,acceptsField:!0}]}),Z(`place/align-to-surface`,{title:`Stand each point up along the surface under it`,description:"Casts a ray from every point onto a mesh, reads the surface `normal` where it lands, and turns the point so its chosen local axis stands along that normal — props lying on slopes instead of standing upright through them. PRECONDITION: the `surface` must carry a `normal` point attribute (f32, tuple 3); a mesh built by `meshPrimitive` carries uv and topology but no normal, so stamp one on it first. A point whose ray misses keeps the previous normal it had, and a zero-length one keeps its existing rotation. Fully deterministic. Writes `rot` and `normal`; `P` is not moved — pair it with `place/drop-to-surface` for that.",tags:[`surface`,`raycast`,`instancing`],nodes:[{id:`normal`,type:`transferAttribute`,params:{name:`normal`,mapping:`raycast`,attrDomain:`point`,uvAttr:`uv`,direction:[0,-1,0],directionAttr:``,maxDistance:0,missCountAttr:``,hitAttr:``}},{id:`orient`,type:`orientAlongVector`,params:{direction:Kt(`normal`,3),up:[0,1,0],axis:`+y`}}],connections:[{from:[`normal`,`out`],to:[`orient`,`in`]}],inputs:[{name:`points`,node:`normal`,pin:`in`},{name:`surface`,node:`normal`,pin:`source`}],outputs:[{name:`out`,node:`orient`,pin:`out`}],params:[{name:`axis`,targets:[{node:`orient`,param:`axis`}],description:`Which local axis of the asset stands along the surface normal. '+y' is upright.`},{name:`up`,targets:[{node:`orient`,param:`up`}],description:`Up hint fixing the roll around the normal.`},{name:`direction`,targets:[{node:`normal`,param:`direction`}],description:`Which way the ray travels to find the surface. [0,-1,0] looks straight down.`},{name:`maxDistance`,targets:[{node:`normal`,param:`maxDistance`}],description:`Longest ray that still counts as a find, in world units. 0 means unlimited.`,min:0}]})}function on(){return{id:`reset`,type:`setAttribute`,params:{name:`scale`,domain:`point`,type:`f32`,tupleSize:3,value:qt([1,1,1])}}}function sn(){return{id:`place`,type:`transformPoints`,params:{scale:[8,8,8],rotateEuler:[0,0,0],translate:[0,0,0]}}}function cn(e){return[{name:`size`,targets:[{node:`place`,param:`scale`}],description:e??`Size of the shape in world units — a radius for the round ones. A bare number is not accepted here: pass three numbers [8,8,8], or {"fn":"constant","value":8} for a uniform one. Unequal components give an ellipse or an ellipsoid.`,acceptsField:!0},{name:`rotate`,targets:[{node:`place`,param:`rotateEuler`}],description:`Rotation in degrees per world axis, applied about the origin before the shape is moved into place.`,acceptsField:!0},{name:`center`,targets:[{node:`place`,param:`translate`}],description:`Where the shape sits, in world units.`,acceptsField:!0}]}function ln(){return Xt(Gt(),0)}function un(){Z(`shape/ring`,{title:`Points evenly around a circle or an arc`,description:"Places points evenly around a circle in the XZ plane, optionally sweeping only part of the way round, then sizes, rotates and moves the result. COUNT: `count` is exactly the number of points emitted, whatever `sweep` and `includeEnd` are. The seam is handled by `includeEnd`, not by deleting a point: left false (the default) the samples divide the sweep and the last one stops one step short, which is what a full circle needs — the end of a full sweep IS its start. Set it true for an arc that must touch both ends. Emits a loose point CLOUD, not a path: for polyline topology use `shape/path-loop`, which is this primitive plus the closure. Fully deterministic: two instances with the same params are identical, which is what a ring should be. Writes `P`; leaves the per-point `scale` attribute at 1 so the ring's size does not become the asset's size.",tags:[`radial`,`outline`],nodes:[{id:`line`,type:`pointLine`,params:{count:24,start:[0,0,0],end:[1,0,0],includeEnd:!1}},{id:`ring`,type:`transformPoints`,params:{scale:[0,0,0],rotateEuler:[0,0,0],translate:Yt($(`cos`,$(`mul`,$(`mul`,ln(),Q(`sweep`)),Wt)),0,$(`sin`,$(`mul`,$(`mul`,ln(),Q(`sweep`)),Wt)))}},sn(),on()],connections:[{from:[`line`,`out`],to:[`ring`,`in`]},{from:[`ring`,`out`],to:[`place`,`in`]},{from:[`place`,`out`],to:[`reset`,`in`]}],outputs:[{name:`out`,node:`reset`,pin:`out`}],params:[{name:`count`,targets:[{node:`line`,param:`count`}],description:`How many points to place, and exactly how many come out — the sweep is divided into this many samples.`,min:1},{name:`sweep`,targets:[],default:1,description:`How far round to go: 1 is a closed circle, 0.5 a half-circle, 0.25 a quarter arc.`,min:0,max:1,acceptsField:!0},{name:`includeEnd`,targets:[{node:`line`,param:`includeEnd`}],description:`Whether the last point lands exactly on the end of the sweep. Leave it false for a full circle: the end is the start, so a point there would sit on top of the first one. Set it true for a partial sweep pinned at both ends (a quarter arc whose corners must be occupied). It never changes how many points come out, only where the last one sits.`},...cn()]}),Z(`shape/spiral`,{title:`Points winding outward over a number of turns`,description:"Winds points outward from the origin over a given number of turns in the XZ plane — an Archimedean spiral, evenly spaced in angle — then sizes, rotates and moves the result. `size` is the OUTER radius: the innermost point sits at the centre and the outermost exactly on the rim. Emits a loose point CLOUD, not a path. Fully deterministic: two instances with the same params are identical. Writes `P`; leaves the per-point `scale` attribute at 1.",tags:[`radial`,`outline`],nodes:[{id:`line`,type:`pointLine`,params:{count:160,start:[0,0,0],end:[1,0,0],includeEnd:!0}},{id:`spiral`,type:`transformPoints`,params:{scale:[0,0,0],rotateEuler:[0,0,0],translate:Yt($(`mul`,ln(),$(`cos`,$(`mul`,$(`mul`,ln(),Q(`turns`)),Wt))),0,$(`mul`,ln(),$(`sin`,$(`mul`,$(`mul`,ln(),Q(`turns`)),Wt))))}},sn(),on()],connections:[{from:[`line`,`out`],to:[`spiral`,`in`]},{from:[`spiral`,`out`],to:[`place`,`in`]},{from:[`place`,`out`],to:[`reset`,`in`]}],outputs:[{name:`out`,node:`reset`,pin:`out`}],params:[{name:`count`,targets:[{node:`line`,param:`count`}],description:`Points along the spiral, evenly spaced in angle (so they crowd near the centre).`,min:1},{name:`turns`,targets:[],default:3,description:`How many full revolutions the spiral makes between the centre and the outer radius.`,min:0,acceptsField:!0},...cn()]}),Z(`shape/path-loop`,{title:`A closed path around a circle`,description:"Builds a CLOSED PATH — polyline topology, not a loose point cloud — around a circle in the XZ plane, then sizes, rotates and moves it. This is the curve source a saved graph reaches for: feed it to `place/along-curve`, `filter/by-distance-to-curve`, `write/orient-along-path` or the `splineSample` / `pathResample` nodes, which all report finding no polylines when handed a point cloud. COUNT: `count` is the number of corner points and exactly the number of points emitted; closure is structural (a trailing vertex back to the first point), so there is no duplicated seam point to trip over. Built on `shape/ring`, so the points also carry `scale` at 1. Fully deterministic. TOPOLOGY IS FRAGILE: anything that can REMOVE points destroys it — the `filter/*` family, `partitionByAttribute` (categorised `attribute`, not `filter`) and `mergePoints` — so whatever must see a path has to come before them. The category is not the rule: `projectToPlane` is a `filter` that PRESERVES topology because it clones rather than gathers, and `filterByAttribute` drops it even when its predicate keeps every point.",tags:[`curve`,`path`,`radial`],nodes:[{id:`ring`,type:`subgraph`,params:{count:24,sweep:1,includeEnd:!1},ref:{name:`shape/ring`}},{id:`path`,type:`pointsToPath`,params:{closed:!0,groupAttr:``,orderAttr:``}}],connections:[{from:[`ring`,`out`],to:[`path`,`in`]}],outputs:[{name:`out`,node:`path`,pin:`out`}],params:[{name:`count`,targets:[{node:`ring`,param:`count`}],description:`Corner points around the loop. At least 3 — two points cannot enclose anything — and higher counts make the polygon read as a circle.`,min:3},{name:`size`,targets:[{node:`ring`,param:`size`}],description:`Radius of the loop in world units. A bare number is not accepted here: pass three numbers [8,8,8], or {"fn":"constant","value":8} for a uniform one. Unequal components give an ellipse.`,acceptsField:!0},{name:`rotate`,targets:[{node:`ring`,param:`rotate`}],description:`Rotation in degrees per world axis, applied about the origin before the loop is moved into place.`,acceptsField:!0},{name:`center`,targets:[{node:`ring`,param:`center`}],description:`Where the loop sits, in world units.`,acceptsField:!0}]}),Z(`shape/path-meander`,{title:`A wandering open path between two ends`,description:"Builds an open PATH — polyline topology — that runs along X and wanders off the straight line by a noise field, then evens the spacing out again by arc length. The resampling is the content: displacing a polyline sideways stretches the segments where the wander is steep, so points placed along it afterwards would bunch on the straight parts, and the fix cannot be seen in a picture until something is spawned on it. Use it for a road, a river, a fence line or a trail. COUNT: `count` is both the number of corners the wander is built from and the number of points emitted, evenly spaced along the finished curve. VARIATION: none by default — noise carries its own seed inside its field spec, so two instances wander IDENTICALLY unless their `variant` differs. Writes `P`, the unit `tangent` and `curveU` (0..1 along the path) on points the resample creates, so the recipe writes no working column at all and the per-point `scale` is 1. TOPOLOGY IS FRAGILE: anything that can REMOVE points destroys it — the `filter/*` family, `partitionByAttribute` and `mergePoints` — so a path has to reach its consumer before them. Being a `filter` is not the rule: `projectToPlane` PRESERVES topology (it clones), while `filterByAttribute` drops it even when its predicate keeps every point.",tags:[`curve`,`path`,`noise`],nodes:[{id:`line`,type:`pointLine`,params:{count:33,start:[-.5,0,0],end:[.5,0,0],includeEnd:!0}},{id:`wander`,type:`transformPoints`,params:{scale:[1,1,1],rotateEuler:[0,0,0],translate:Yt(0,0,$(`mul`,Q(`wander`),$(`remap`,$t(`frequency`,`variant`),0,1,-1,1)))}},{id:`place`,type:`transformPoints`,params:{scale:[40,1,40],rotateEuler:[0,0,0],translate:[0,0,0]}},{id:`path`,type:`pointsToPath`,params:{closed:!1,groupAttr:``,orderAttr:``}},{id:`even`,type:`pathResample`,params:{mode:`count`,count:33,spacing:1}}],connections:[{from:[`line`,`out`],to:[`wander`,`in`]},{from:[`wander`,`out`],to:[`place`,`in`]},{from:[`place`,`out`],to:[`path`,`in`]},{from:[`path`,`out`],to:[`even`,`in`]}],outputs:[{name:`out`,node:`even`,pin:`out`}],params:[{name:`count`,targets:[{node:`line`,param:`count`},{node:`even`,param:`count`}],description:`Points along the path, and the number of corners the wander is drawn from. A dozen or more before the wander reads as a curve rather than a zig-zag.`,min:2},{name:`wander`,targets:[],default:.15,description:"How far the path strays from the straight line between its ends. 0 is a straight line; above that the peak deviation is about 0.22 * wander * `size.z`, exactly linear in both — so at the default `size` [40,1,40], 0.15 strays about 1.3 units to each side and 0.5 about 4.4. Inverted, for a peak of a fraction f of `size.z`, ask for wander around 4.6 * f. This is a FRACTION OF A NOMINAL RANGE, not of the deviation you get: the value scales a noise term whose range is +/-1 in principle, but four octaves of normalized fBm sampled along one line only cover part of it, and `frequency` and `variant` move that coverage (measured 0.13 to 0.31 of wander * `size.z` across the usable range). The wander is sideways only — the path is a height field along X, so it NEVER doubles back on itself at any wander; for a curve that turns back, build the corners yourself and run `pointsToPath` over them.",acceptsField:!0},{name:`frequency`,targets:[],default:3,description:`How many bends over the length of the path, roughly: the noise sample position is multiplied by this, so smaller means longer, lazier curves.`,acceptsField:!0},{name:`variant`,targets:[],default:0,description:`Offset added to the noise sample position — the per-instance re-roll, and the ONLY one: no seed can move a noise field.`,acceptsField:!0},...cn(`Extent in world units: X is the end-to-end length, Z scales the wander. A bare number is not accepted here: pass three numbers [40,1,40], or {"fn":"constant","value":40}.`)]}),Z(`shape/disc`,{title:`Points scattered uniformly inside a circle`,description:"Scatters points uniformly inside a disc in the XZ plane by scattering a square and rejecting the corners — the circular counterpart to scattering a box, and the right answer when scattering a square and hoping is wrong. COUNT: `count` is the number of CANDIDATES; the disc keeps about 78.5% of them, so asking for 1000 gives roughly 785 points. VARIATION: yes — two instances in one graph scatter differently, and `seed` re-rolls one explicitly. Writes `P`; leaves the per-point `scale` attribute at 1.",tags:[`scatter`,`radial`],nodes:[{id:`scatter`,type:`pointScatterInBounds`,params:{count:600,boundsMin:[-1,0,-1],boundsMax:[1,0,1],seed:0}},{id:`keep`,type:`filterByExpression`,params:{predicate:$(`le`,$(`length`,Gt()),1),seed:0}},sn(),on()],connections:[{from:[`scatter`,`out`],to:[`keep`,`in`]},{from:[`keep`,`out`],to:[`place`,`in`]},{from:[`place`,`out`],to:[`reset`,`in`]}],outputs:[{name:`out`,node:`reset`,pin:`out`}],params:[{name:`count`,targets:[{node:`scatter`,param:`count`}],description:`Candidates scattered before the corners are rejected. About 78.5% survive.`,min:0},{name:`seed`,targets:[{node:`scatter`,param:`seed`}],description:`Re-rolls the scatter. Two instances already differ without it.`},...cn()]}),Z(`shape/sphere-points`,{title:`Points scattered uniformly on a sphere`,description:"Scatters points uniformly over the surface of a sphere, by rejecting a cube scatter down to the ball first and then pushing every survivor out to the surface. The rejection step is the content: normalizing a cube scatter directly piles points up toward the eight corner directions, and the result looks wrong without looking obviously wrong. COUNT: `count` is the number of CANDIDATES; the ball keeps about 52.4% of them. VARIATION: yes — two instances in one graph scatter differently, and `seed` re-rolls one explicitly. Writes `P`; leaves the per-point `scale` attribute at 1.",tags:[`scatter`,`radial`],nodes:[{id:`scatter`,type:`pointScatterInBounds`,params:{count:800,boundsMin:[-1,-1,-1],boundsMax:[1,1,1],seed:0}},{id:`keep`,type:`filterByExpression`,params:{predicate:$(`le`,$(`length`,Gt()),1),seed:0}},{id:`shell`,type:`transformPoints`,params:{scale:[0,0,0],rotateEuler:[0,0,0],translate:$(`normalize`,Gt())}},sn(),on()],connections:[{from:[`scatter`,`out`],to:[`keep`,`in`]},{from:[`keep`,`out`],to:[`shell`,`in`]},{from:[`shell`,`out`],to:[`place`,`in`]},{from:[`place`,`out`],to:[`reset`,`in`]}],outputs:[{name:`out`,node:`reset`,pin:`out`}],params:[{name:`count`,targets:[{node:`scatter`,param:`count`}],description:`Candidates scattered in the cube before rejection to the ball. About 52.4% survive.`,min:0},{name:`seed`,targets:[{node:`scatter`,param:`seed`}],description:`Re-rolls the scatter. Two instances already differ without it.`},...cn()]})}var dn=1,fn=1e-6;function pn(e,t){return $(`max`,Q(e),t)}var mn=$(`div`,$(`add`,$(`floor`,$(`mul`,Kt(`curveU`,1),pn(`bins`,dn))),.5),pn(`bins`,dn));function hn(){Z(`transform/displace-by-noise`,{title:`Push points up and down by a noise field`,description:"Displaces every point along +Y by a noise field centred on zero, so a flat scatter becomes a rolling one. The displacement is centred: the mean height does not move, and the peak is a FRACTION of `amount` — about 0.42 on a wide cloud and as little as 0.25 on a small patch, never `amount` itself. See that param for the law. LIMITATION: +Y only — the direction lives inside the field structure, not in a param slot, so displacing along another axis means rotating the whole result. VARIATION: none by default — noise carries its own seed inside its field spec, so two instances displace IDENTICALLY unless their `variant` differs, and this is the primitive most likely to be used twice in one graph. Reads and writes `P`; leaves every other attribute alone.",tags:[`noise`,`terrain`],nodes:[{id:`displace`,type:`transformPoints`,params:{scale:[1,1,1],rotateEuler:[0,0,0],translate:Yt(0,$(`mul`,Q(`amount`),$(`remap`,$t(`frequency`,`variant`),0,1,-1,1)),0)}}],connections:[],inputs:[{name:`in`,node:`displace`,pin:`in`}],outputs:[{name:`out`,node:`displace`,pin:`out`}],params:[{name:`amount`,targets:[],default:4,description:"How far points move along Y. It SCALES the displacement but does not bound it: on a cloud spanning many noise periods the peak is about 0.42 * amount, so the default 4 lifts and drops by roughly 1.7 units, and for a peak of h world units ask for amount around 2.4 * h. Exactly linear in amount — doubling it doubles every displacement — and the mean stays within 1% of 0, so the average height does not move. This is a FRACTION OF A NOMINAL RANGE, not of the displacement you get: the value scales a noise term whose range is +/-1 in principle, and four octaves of normalized fBm only ever reach the middle of it. How much of it depends on how much FIELD the cloud spans — extent x `frequency`, not the point count. Measured at amount 4: a 600-unit spread peaks at 0.42 to 0.49 of amount at every frequency from 0.01 up, while a 30-unit patch at the default frequency 0.05 spans barely one period and peaks at only 0.25, whether it holds 100 points or 20,000. On a small patch, either raise `frequency` or scale `amount` up to compensate.",acceptsField:!0},{name:`frequency`,targets:[],default:.05,description:`Feature size: the noise sample position is multiplied by this, so smaller means longer, gentler waves.`,acceptsField:!0},{name:`variant`,targets:[],default:0,description:`Offset added to the noise sample position — the per-instance re-roll, and the ONLY one: no seed can move a noise field.`,acceptsField:!0}]}),Z(`transform/snap-to-grid`,{title:`Move every point to the nearest grid corner`,description:"Snaps every point to the nearest corner of a regular 3D grid of the given pitch, so scattered positions become tile-aligned. Note that snapping can land two points on the SAME corner: follow it with `selfPrune` at a distance just under the pitch if the duplicates matter. Fully deterministic. Reads and writes `P`; leaves every other attribute alone, including `scale`.",tags:[`grid`,`align`],nodes:[{id:`snap`,type:`transformPoints`,params:{scale:[1,1,1],rotateEuler:[0,0,0],translate:$(`sub`,$(`mul`,$(`floor`,$(`add`,$(`div`,Gt(),pn(`cellSize`,fn)),.5)),pn(`cellSize`,fn)),Gt())}}],connections:[],inputs:[{name:`in`,node:`snap`,pin:`in`}],outputs:[{name:`out`,node:`snap`,pin:`out`}],params:[{name:`cellSize`,targets:[],default:4,description:`Grid pitch in world units, the same on all three axes. It is the DIVISOR of the snap, so it must be greater than 0 — declared as well as said: a plain value below ${fn} is refused by name, and a FIELD delivering one — which no declared bound can refuse — is clamped to ${fn} at the point of use rather than dividing by it. That clamp is the limit the pitch was heading for anyway: a millionth-of-a-unit grid moves a point by at most half of that, so a pitch of 0 or less leaves the cloud where it is instead of snapping every position to NaN. The clamp is a floor and nothing more: a field that returns NaN still snaps to NaN, because there is no value to floor it to.`,acceptsField:!0,min:fn}]}),Z(`transform/gather-on-path`,{title:`Gather a path's own points into clumps along it`,description:"Slides every point of a path along the curve it already sits on, toward the centre of its own bin, so an even distribution becomes clumps with bare runs between them — hedgerows, rock piles, bundled cables, knots in a crowd. Nothing is created or deleted: the count, the attributes and the polyline topology all survive, and each point is RE-EVALUATED on the curve rather than stepped along its tangent, so it stays exactly on the path however hard the path bends. Each point's target is the centre of the bin its own `curveU` falls into — `bins` equal bins per path — and `amount` is how far of the way there it travels, so 0 changes nothing and 1 collapses every bin onto a single spot. PRECONDITION: the input must carry `curveU` (f32, tuple 1) as well as polyline topology, so it has to have come from a sampler that writes one — `pathResample`, `splineSample`, `place/along-curve`, `place/radial-on-curve` or `shape/path-meander`. A path built straight out of `pointsToPath` has no parameterization to gather along, and the cook fails naming `curveU` rather than guessing one. The point sitting exactly at the end of a path does not move: its bin centre lies past the end and clamps back onto it. `curveU` and `tangent` are rewritten to where each point LANDS, so a second gather bins the new positions and not the old ones. Fully deterministic: two instances with the same params clump identically, and there is no seed — the clumps are where the bins are, not where a draw put them. Reads and writes `P`; every other attribute arrives untouched.",tags:[`curve`,`path`,`spacing`],nodes:[{id:`slide`,type:`pathPointAt`,params:{mode:`fraction`,parameter:$(`lerp`,Kt(`curveU`,1),mn,Q(`amount`))}}],connections:[],inputs:[{name:`in`,node:`slide`,pin:`in`}],outputs:[{name:`out`,node:`slide`,pin:`out`}],params:[{name:`bins`,targets:[],default:6,description:"How many clumps each path gets: its 0..1 parameter is cut into this many equal bins and every point heads for the centre of its own. Fewer bins means fewer, fatter clumps further apart, and the clumps land at (i + 0.5) / bins along each path — a fixed lattice, not a random one, so two paths of different lengths clump at the same RELATIVE places. It is per path rather than per world unit, so a long path and a short one both get `bins` clumps; for a fixed clump pitch, resample to a `spacing` first and scale this with the length. A fractional value leaves a short last bin at the far end. Field-capable like every knob here, but keep it UNIFORM unless you mean otherwise: a bin count that varies per point stops partitioning the path, because neighbouring points then head for the centres of bins that do not line up. A field over something constant along each path — a per-path attribute, say — is the coherent way to vary it; a field over `curveU` or position is not. Guarded at the point of use, because it is the DIVISOR that turns a 0..1 parameter into a bin: anything below the declared minimum of 1 is read as exactly 1 — one bin, the whole path gathering onto a single spot — and that covers the values a bound cannot refuse, since a field's are never range-checked. A plain value below 1 is still an error rather than a clamp.",min:1,acceptsField:!0},{name:`amount`,targets:[],default:.7,description:"How far of the way to its bin centre each point travels, 0..1: 0 leaves the distribution exactly as it arrived, 1 collapses every bin onto a single point, and the travel is exactly linear in between — 0.5 halves every gap to the centre. Field-capable, resolved on the points BEFORE they move, so a field over `curveU` can gather one end of a path harder than the other.",min:0,max:1,acceptsField:!0}]}),Z(`transform/relax-spacing`,{title:`Even out spacing without deleting anything`,description:"Nudges every point away from the centroid of its neighbours, so crowded regions spread out and the spacing evens up while the COUNT stays exactly the same. This is the alternative to `selfPrune`, which enforces spacing by deleting: use this one when the count is fixed (a fleet, a crowd, a fixed budget of props). A strength near 0.5 is one relaxation step; run the primitive twice for a stronger effect rather than pushing the strength past 1, which overshoots and oscillates. Isolated points do not move. Fully deterministic. Reads and writes `P`; leaves every other attribute alone.",tags:[`neighborhood`,`spacing`],nodes:[{id:`nbr`,type:`pointNeighborhood`,params:{radius:4,maxCount:0,includeSelf:!1,countAttr:``,averageAttr:`P`,averageOutAttr:`__nbrP`}},{id:`push`,type:`transformPoints`,params:{scale:[1,1,1],rotateEuler:[0,0,0],translate:$(`mul`,Q(`strength`),$(`sub`,Gt(),Kt(`__nbrP`,3)))}},{id:`cleanup`,type:`removeAttribute`,params:{names:[`__nbrP`],domain:`point`,strict:!0}}],connections:[{from:[`nbr`,`out`],to:[`push`,`in`]},{from:[`push`,`out`],to:[`cleanup`,`in`]}],inputs:[{name:`in`,node:`nbr`,pin:`in`}],outputs:[{name:`out`,node:`cleanup`,pin:`out`}],params:[{name:`radius`,targets:[{node:`nbr`,param:`radius`}],description:`How far around each point counts as its neighbourhood, in world units. Points with nothing inside it do not move. As a FIELD it is a PER-POINT radius, so each point measures the neighbourhood it asks for and the relation stops being symmetric — B within A's reach does not put A within B's.`,min:0},{name:`strength`,targets:[],default:.5,description:"How far along the push each point travels: the point moves exactly strength * (its own position - the mean position of its neighbours inside `radius`), so 0 changes nothing and the travel is exactly linear in strength. It is a fraction of a LOCAL offset, not a world distance, so `radius` sets the scale and this sets the fraction of it: measured on 300 points in a 30x30 box, strength 0.5 moves the average point 0.42 units at radius 4 and 1.5 units at radius 12, and strength 1 moves it exactly twice as far. 0.5 is one relaxation step; run the primitive twice rather than pushing past 1, which overshoots and oscillates.",acceptsField:!0}]})}var gn=$(`lerp`,Q(`min`),Q(`max`),Jt(`size`));function _n(){Z(`write/height-slope`,{title:`Stamp height and slope from a surface normal`,description:"Writes the two standard terrain quantities onto points that already carry a surface normal: `height` (f32) is the world Y of each point, and `slope` (f32) is 1 - normal.y, so 0 is dead flat and 1 is a vertical wall. That scale is 1 - cos(angle) and so is NOT linear in degrees — it is compressed at the flat end, where 30 degrees is only 0.134 and 45 is 0.293, and half the scale (0.5) is already 60 degrees. Downstream filters can then test ground suitability without re-deriving either. PRECONDITION: the input must carry a `normal` point attribute (f32, tuple 3) — `surfaceSample` writes one, and `place/align-to-surface` transfers one from a mesh; without it the cook fails naming the missing attribute. Fully deterministic: two instances of this primitive produce identical output.",tags:[`terrain`,`attributes`],nodes:[{id:`height`,type:`setAttribute`,params:{name:`height`,domain:`point`,type:`f32`,tupleSize:1,value:Xt(Gt(),1)}},{id:`slope`,type:`setAttribute`,params:{name:`slope`,domain:`point`,type:`f32`,tupleSize:1,value:$(`sub`,1,Xt(Kt(`normal`,3),1))}}],connections:[{from:[`height`,`out`],to:[`slope`,`in`]}],inputs:[{name:`in`,node:`height`,pin:`in`}],outputs:[{name:`out`,node:`slope`,pin:`out`}]}),Z(`write/density-from-noise`,{title:`Write the standard density attribute from a noise field`,description:'Writes `density` (f32, 0..1) from four octaves of normalized Perlin fBm — the exact input `filterByDensity` and the density-aware samplers expect, separated from applying it so one pattern can drive a thin, a colour and a scale. VARIATION: noise does not vary per instance. Two instances of this primitive with the same params write the IDENTICAL pattern, and this primitive exposes no seed that could change it — its field writes no `opts.seed`, and that slot admits only an integer or the tagged `{"from": "node", "variant": N}` form whose `variant` may be an inline `param`, never an arbitrary expression, because a field column is f32 and a seed rounded through one avalanches to an unrelated noise. Pass a different `variant` to move the sample position to an unrelated part of the field, which is the per-instance re-roll. Writes `density`; reads nothing.',tags:[`noise`,`density`],nodes:[{id:`densityAttr`,type:`setAttribute`,params:{name:`density`,domain:`point`,type:`f32`,tupleSize:1,value:$t(`frequency`,`variant`)}}],connections:[],inputs:[{name:`in`,node:`densityAttr`,pin:`in`}],outputs:[{name:`out`,node:`densityAttr`,pin:`out`}],params:[{name:`frequency`,targets:[],default:.02,description:`Feature size: the noise sample position is multiplied by this, so smaller means broader blobs. 0.02 gives features tens of world units across.`,acceptsField:!0},{name:`variant`,targets:[],default:0,description:`Offset added to the noise sample position. This is the per-instance re-roll this primitive offers: any two different values give unrelated patterns, and the same value always reproduces.`,acceptsField:!0}]}),Z(`write/random-scale`,{title:`Write one uniform random size per point`,description:"Writes the standard `scale` attribute (f32, tuple 3) as ONE random size per point, drawn uniformly between min and max — both ends reachable — and written the same on all three axes. It REPLACES `scale` rather than multiplying it, so anything an earlier node wrote there is discarded and two of these in a chain do not compound. The uniformity is the content: a hand-written version reaches for a separate random per axis and gets an asset stretched differently in x, y and z, which reads as a modelling error rather than as variety. VARIATION: yes — the draw comes from the evaluation context, so two instances in one graph differ automatically, and `seed` re-rolls one of them explicitly. Writes `scale`; reads nothing.",tags:[`scatter`,`instancing`],nodes:[{id:`scaleAttr`,type:`setAttribute`,params:{name:`scale`,domain:`point`,type:`f32`,tupleSize:3,seed:0,value:Yt(gn,gn,gn)}}],connections:[],inputs:[{name:`in`,node:`scaleAttr`,pin:`in`}],outputs:[{name:`out`,node:`scaleAttr`,pin:`out`}],params:[{name:`min`,targets:[],default:.7,description:`Smallest size a point can get, as a multiple of the asset's own size.`,acceptsField:!0},{name:`max`,targets:[],default:1.4,description:`Largest size a point can get, as a multiple of the asset's own size.`,acceptsField:!0},{name:`seed`,targets:[{node:`scaleAttr`,param:`seed`}],description:`Re-rolls which point gets which size. 0 uses the node's own derived seed.`}]}),Z(`write/random-yaw`,{title:`Turn each point to face a random direction`,description:"Writes the standard `rot` attribute so every point faces a uniformly random direction around the vertical axis, so repeated assets do not all point the same way. The field is the content: the direction an author reaches for instead — a noise vector — is SPATIALLY CORRELATED, which reads as a combed field rather than randomness and is a bug that is hard to diagnose from the result. VARIATION: yes — the draw comes from the evaluation context, so two instances in one graph differ automatically. Writes `rot`; reads nothing.",tags:[`scatter`,`instancing`],nodes:[{id:`yaw`,type:`orientAlongVector`,params:{direction:Yt($(`cos`,$(`mul`,Jt(`yaw`),Wt)),0,$(`sin`,$(`mul`,Jt(`yaw`),Wt))),up:[0,1,0],axis:`+z`}}],connections:[],inputs:[{name:`in`,node:`yaw`,pin:`in`}],outputs:[{name:`out`,node:`yaw`,pin:`out`}],params:[{name:`axis`,targets:[{node:`yaw`,param:`axis`}],description:`Which local axis of the asset turns to face the random direction. '+z' is the forward axis assets face by convention.`},{name:`up`,targets:[{node:`yaw`,param:`up`}],description:`Up hint fixing the roll; leave it at world up unless the assets stand along another axis.`}]}),Z(`write/orient-along-path`,{title:`Turn a path's own points to follow the path`,description:"Writes a unit `tangent` along the polyline at every point of a path, then sets `rot` so each point faces that way — the points, their attributes and the topology all arrive and leave untouched. That is the whole difference from `place/along-curve`, which resamples: use this one when the points already mean something (a species, a scale, a colour, a point index other geometry refers to) and must survive being oriented. It is also the only way to orient the points of a path that was hand-built with `pointsToPath`, since a tangent otherwise exists only on points a sampler created. The tangent is the normalized central difference between each point's neighbours along the path, so it stays smooth through corners, and it wraps on a closed path. PRECONDITION: the input must carry polyline topology; a point on no polyline gets a zero tangent and deliberately keeps the `rot` it had. Run it BEFORE anything that can REMOVE points — the `filter/*` family, `partitionByAttribute` and `mergePoints` all drop topology, and this node would then find no paths. Category is not the predicate: `filterByAttribute` drops it even when its predicate keeps every point, while `projectToPlane` is a `filter` that preserves it by cloning. Fully deterministic. Writes `tangent` and `rot`; `P` and the count are untouched.",tags:[`curve`,`path`,`instancing`],nodes:[{id:`tangents`,type:`writeTangents`,params:{name:`tangent`}},{id:`orient`,type:`orientAlongVector`,params:{direction:Kt(`tangent`,3),up:[0,1,0],axis:`+z`}}],connections:[{from:[`tangents`,`out`],to:[`orient`,`in`]}],inputs:[{name:`in`,node:`tangents`,pin:`in`}],outputs:[{name:`out`,node:`orient`,pin:`out`}],params:[{name:`axis`,targets:[{node:`orient`,param:`axis`}],description:`Which local axis of the asset points along the path. '+z' is the forward axis assets face by convention.`},{name:`up`,targets:[{node:`orient`,param:`up`}],description:`Up hint fixing the roll around the path; leave it at world up for props that stand on the ground.`}]}),Z(`write/curve-frame`,{title:`Write a smoothly carried frame along a path`,description:"Writes a full orthonormal frame at every point of a path — `tangent`, `curveNormal` and `curveBinormal` (f32, tuple 3) — with the normal CARRIED along the curve by rotation-minimizing transport rather than rebuilt from a world axis at each point. That is the thing a constant `up` cannot do: as a curve turns over, a tangent passing through the up vector flips the roll a half turn and everything placed there snaps round with it. Feed `curveNormal` back into `orientAlongVector`'s `up` — field-capable for exactly this — and the roll varies smoothly however the curve bends; combine the normal and the binormal with cos and sin of an angle to aim anything RADIALLY around the path, which is what `place/radial-on-curve` does. It sits beside `write/orient-along-path`: that one turns the points to face ALONG the curve, this one hands over the two perpendicular axes as well and writes no `rot` at all. The points, their attributes and the topology all arrive and leave untouched. PRECONDITION: the input must carry polyline topology; a point on no polyline gets [0,0,0] on all three columns. ORDER: run it AFTER any resample — `pathResample` and `splineSample` build NEW points and do not carry the input's point attributes, so a frame written before one is silently dropped there — and BEFORE anything that can remove points, since the `filter/*` family, `partitionByAttribute` and `mergePoints` all destroy the topology it needs. THE FRAME IS NOT LOCAL: a point's normal depends on every point before it along its own path, so a curve that arrives split across two cook cells gets two unrelated frames, and a CLOSED path does not come back seamless — transport around a loop returns rotated by that curve's own residual angle, either side of the seam differs, and no local rule can fix it. Fully deterministic: two instances write identical frames. Writes `tangent`, `curveNormal` and `curveBinormal`; `P` and the count are untouched.",tags:[`curve`,`path`,`instancing`],nodes:[{id:`frame`,type:`writeCurveFrame`,params:{tangentName:`tangent`,normalName:`curveNormal`,binormalName:`curveBinormal`}}],connections:[],inputs:[{name:`in`,node:`frame`,pin:`in`}],outputs:[{name:`out`,node:`frame`,pin:`out`}],params:[{name:`tangentName`,targets:[{node:`frame`,param:`tangentName`}],description:"Attribute the unit tangent is written to. The default 'tangent' is what `pathResample` and `splineSample` emit and what an `orientAlongVector` direction field reads, so leave it alone unless two frames have to coexist on one cloud."},{name:`normalName`,targets:[{node:`frame`,param:`normalName`}],description:"Attribute the transported normal is written to. Do NOT rename it to 'normal': `surfaceSample` writes a surface `normal` of the very same shape (f32, tuple 3), and a matching shape is exactly the case an attribute writer ACCEPTS — so a graph that samples a surface and frames a curve would have one silently overwrite the other."},{name:`binormalName`,targets:[{node:`frame`,param:`binormalName`}],description:`Attribute the binormal — tangent cross normal, the frame's third axis — is written to. It ships as a column rather than being recomputed downstream, because recomputing it from two f32 columns is where a frame stops being exactly orthonormal.`}]}),Z(`write/color-from-attribute`,{title:`Turn a scalar attribute into a colour gradient`,description:"Rescales any numeric point attribute to 0..1 over its OWN observed range, then maps it through a blue-green-yellow-red heat ramp into the standard `color` attribute (f32, tuple 4, alpha 1). Fitting to the observed range is what makes it work on an invented quantity — a neighbour count, a hand-built score — without knowing its scale in advance. Fully deterministic: two instances produce identical output. Writes `color`; reads the attribute named by `source`, which must exist on the point domain.",tags:[`color`,`debug`],nodes:[{id:`fit`,type:`attributeRemap`,params:{name:`density`,outName:`__t`,domain:`point`,mode:`fit`,inMin:-1,inMax:1,outMin:0,outMax:1,clamp:!0}},{id:`color`,type:`setAttribute`,params:{name:`color`,domain:`point`,type:`f32`,tupleSize:4,value:Yt(Zt(Kt(`__t`),[[0,.05],[.4,.1],[.7,.9],[1,1]]),Zt(Kt(`__t`),[[0,.1],[.4,.7],[.7,.9],[1,.2]]),Zt(Kt(`__t`),[[0,.5],[.4,.4],[.7,.1],[1,.05]]),1)}},{id:`cleanup`,type:`removeAttribute`,params:{names:[`__t`],domain:`point`,strict:!0}}],connections:[{from:[`fit`,`out`],to:[`color`,`in`]},{from:[`color`,`out`],to:[`cleanup`,`in`]}],inputs:[{name:`in`,node:`fit`,pin:`in`}],outputs:[{name:`out`,node:`cleanup`,pin:`out`}],params:[{name:`source`,targets:[{node:`fit`,param:`name`}],description:`Numeric point attribute to colour by. It is fitted to its own minimum and maximum, so any scale works — 'density', 'height', 'slope' or anything you computed yourself.`}]}),Z(`write/local-density`,{title:`Write how crowded each point is as a density`,description:"Counts each point's neighbours within a radius and rescales the counts to 0..1 over their own observed range, writing the standard `density` attribute — so crowding can drive size, colour, or a proportional thinning through `filter/thin-by-density`. Fully deterministic: two instances produce identical output, since this measures whatever arrives rather than inventing anything. Writes `density`; reads `P`.",tags:[`neighborhood`,`density`],nodes:[{id:`nbr`,type:`pointNeighborhood`,params:{radius:5,maxCount:0,includeSelf:!1,countAttr:`__nbrCount`,averageAttr:``,averageOutAttr:`nbrAvg`}},{id:`fit`,type:`attributeRemap`,params:{name:`__nbrCount`,outName:`density`,domain:`point`,mode:`fit`,inMin:-1,inMax:1,outMin:0,outMax:1,clamp:!0}},{id:`cleanup`,type:`removeAttribute`,params:{names:[`__nbrCount`],domain:`point`,strict:!0}}],connections:[{from:[`nbr`,`out`],to:[`fit`,`in`]},{from:[`fit`,`out`],to:[`cleanup`,`in`]}],inputs:[{name:`in`,node:`nbr`,pin:`in`}],outputs:[{name:`out`,node:`cleanup`,pin:`out`}],params:[{name:`radius`,targets:[{node:`nbr`,param:`radius`}],description:"How far around each point counts as its neighbourhood, in world units. It changes the ORDERING of the densities, never their range: the counts are refitted to 0..1 over whatever spread they happen to have, so the emptiest point is always exactly 0 and the fullest always exactly 1 at every radius, and even a perfectly uniform scatter comes out spanning the full 0..1 (measured mean 0.56). Two consequences an agent has to plan around: the values are NOT comparable between two clouds, two radii or two cooks with different counts, and a threshold like 0.8 means 'the top of THIS cloud' rather than any absolute crowding. For an absolute measure, read the raw neighbour count with a `pointNeighborhood` node instead. As a FIELD it is a PER-POINT radius, so each point measures the neighbourhood it asks for and the relation stops being symmetric — B within A's reach does not put A within B's.",min:0}]}),Z(`write/instances-by-species`,{title:`Pick one asset per point and emit the instances`,description:"Chooses an asset id per point from a list and emits the instance batches — the multi-asset spawn. The point of the primitive is the string coupling: one param names BOTH the attribute the choice is written to and the attribute the spawner reads it back from, so the two can never drift apart. The built-in selector spreads uniformly over FOUR entries, so pass exactly four assets and repeat one to weight it (['pine','pine','birch','bush'] is 50/25/25). VARIATION: yes — the choice comes from the evaluation context, so two instances differ automatically, and `seed` re-rolls one explicitly. Writes the species attribute; reads `P`, `rot` and `scale` through the spawner.",tags:[`spawn`,`instancing`],nodes:[{id:`species`,type:`setAttribute`,params:{name:`species`,domain:`point`,type:`string`,tupleSize:1,seed:0,value:$(`mul`,Jt(`species`),4),values:[`pine`,`pine`,`birch`,`bush`],stringValue:``}},{id:`spawn`,type:`spawnInstances`,params:{assetId:`pine`,assetAttr:`species`}}],connections:[{from:[`species`,`out`],to:[`spawn`,`in`]}],inputs:[{name:`in`,node:`species`,pin:`in`}],outputs:[{name:`instances`,node:`spawn`,pin:`instances`},{name:`points`,node:`spawn`,pin:`points`}],params:[{name:`assets`,targets:[{node:`species`,param:`values`}],description:"Asset ids to choose among, as four entries. Repeat an entry to weight it; an empty entry falls back to `assetId`."},{name:`speciesAttr`,targets:[{node:`species`,param:`name`},{node:`spawn`,param:`assetAttr`}],description:`Name of the string point attribute holding the per-point asset id. One knob writes it and reads it, so the writer and the spawner cannot disagree.`},{name:`assetId`,targets:[{node:`spawn`,param:`assetId`}],description:`Asset id used for any point whose species entry is empty.`},{name:`seed`,targets:[{node:`species`,param:`seed`}],description:`Re-rolls which point gets which asset. 0 uses the node's own derived seed.`}]})}Rt(),_n(),tn(),un(),en(),hn(),Ut(),an();var vn=class extends D{half;r;constructor(e,t){super(),this.half=e,this.r=t}getPoint(e,t=new E){let n=this.half*2,r=Math.PI*this.r,i=e*(2*n+2*r);if(i<n)return t.set(this.r,-this.half+i,0);if(i-=n,i<r){let e=i/this.r;return t.set(Math.cos(e)*this.r,this.half+Math.sin(e)*this.r,0)}if(i-=r,i<n)return t.set(-this.r,this.half-i,0);i-=n;let a=i/this.r;return t.set(-Math.cos(a)*this.r,-this.half-Math.sin(a)*this.r,0)}},yn=.62,bn=.085,xn=.055;function Sn(e,t,n=0){return new Ye(e.map(([e,t])=>new ut(e,t)),t,n)}function Cn(e={}){let t=e.mono===!0,n=e=>new Ge({color:e,roughness:.75}),r={cone:{geometry:new ge(.22,.9,10).translate(0,.45,0),material:n(t?11579568:8374170)},box:{geometry:new j(.42,.42,.42).translate(0,.21,0),material:n(t?15000804:7320063)},sphere:{geometry:new me(.3,14,10).translate(0,.3,0),material:n(t?9079434:16044894)},pine:{geometry:Sn([[0,0],[.05,0],[.04,.18],[.3,.22],[.2,.46],[.26,.49],[.15,.73],[.19,.76],[0,1.15]],9),material:n(t?10132122:3046747)},birch:{geometry:Sn([[0,0],[.04,0],[.028,.42],[.06,.47],[.24,.62],[.26,.76],[.17,.93],[0,1]],10),material:n(t?13684944:11063147)},bush:{geometry:Sn([[0,0],[.22,.02],[.3,.13],[.27,.25],[.15,.32],[0,.35]],10),material:n(t?7763574:5940042)},willow:{geometry:Sn([[0,0],[.045,0],[.035,.28],[.3,.34],[.36,.55],[.3,.75],[.16,.88],[0,.95]],10),material:n(t?12369084:8367736)},boulder:{geometry:new ke(.32,0).scale(1,.7,.86).translate(0,.2,0),material:n(t?9342606:9209208)},house:{geometry:Sn([[0,0],[.42,0],[.42,.5],[.56,.53],[0,.95]],4,Math.PI/4),material:n(t?13158600:14203026)},hall:{geometry:Sn([[0,0],[.46,0],[.46,.78],[.6,.82],[0,1.5]],4,Math.PI/4).scale(1.5,1,1),material:n(t?11053224:13224912)},barn:{geometry:Sn([[0,0],[.44,0],[.44,.34],[.52,.37],[0,.78]],4,Math.PI/4).scale(1.7,1,.95),material:n(t?7368816:10769215)},post:{geometry:Sn([[0,0],[.055,0],[.045,.86],[0,.9]],4,Math.PI/4),material:n(t?10263708:9071173)},lamp:{geometry:Sn([[0,0],[.05,0],[.03,1.02],[.16,1.08],[.15,1.2],[.05,1.24],[0,1.26]],7),material:n(t?14474460:15254106)},tube:{geometry:new st(1,1,1,8),material:n(9082022)},chainLink:{geometry:new v(new vn(.19,yn/2),26,Math.min(bn,yn*.42),5,!0),material:n(11581636)},rod:{geometry:new st(.045,.03,1.6,6).translate(0,.8,0),material:n(14704700)},bar:{geometry:new j(.1,.17,1.5),material:n(4636832)},log:{geometry:new st(.11,.085,1,7).rotateX(Math.PI/2).translate(0,.1,0),material:n(9072466)},panel:{geometry:new j(.42,.3,.66),material:n(14266940)},clamp:{geometry:new re(xn*2.1,xn*.8,5,12),material:n(8023264)}},a=new Map,o=[e=>new ge(.18+.1*e,.7+.6*e,9).translate(0,(.7+.6*e)/2,0),e=>new j(.34,.5+.5*e,.34).translate(0,(.5+.5*e)/2,0),e=>new me(.22+.14*e,12,8).translate(0,.22+.14*e,0)];return{known:r,fallbackFor(e){let n=a.get(e);if(n!==void 0)return n;let r=i(e),s=(r>>>8&65535)/65535,c=(r>>>4&255)/255,l={geometry:o[r%o.length](s),material:new Ge({color:t?new se().setHSL(0,0,.42+.46*c):new se().setHSL(c,.55,.55),roughness:.75})};return a.set(e,l),l}}}function wn(e,t){let n={};for(let r of e)n[r.assetId]=t.known[r.assetId]??t.fallbackFor(r.assetId);return n}function Tn(e){return e instanceof Error?e.message:String(e)}function En(e){let t=(e?.description??``)===``?e?.device:e?.description;return[e?.vendor,e?.architecture,t].filter(e=>typeof e==`string`&&e!==``).join(` · `)||`adapter (no info exposed)`}async function Dn(e){let t=navigator.gpu;if(t===void 0)return{error:`navigator.gpu is missing — this browser has no WebGPU.`};try{let n=await t.requestAdapter();if(n===null)return{error:`navigator.gpu.requestAdapter() returned null — no compatible GPU adapter.`};let r=n.info,i=await n.requestDevice(),a=i.lost;return a!==void 0&&a.then(t=>{e(`${t?.reason??`unknown`}: ${t?.message??`no detail`}`)}),{device:i,info:r,label:En(r)}}catch(e){return{error:`requestDevice() failed: ${Tn(e)}`}}}function On(e){return{cacheSalt:e.cacheSalt,acceptDerivedSpecs:e.acceptDerivedSpecs,resolveField:(t,n,r)=>e.resolveField(t,n,r),planRun:()=>null,executeRun:(t,n,r)=>e.executeRun(t,n,r)}}var kn=[`cpu`,`gpu-per-node`,`gpu-fused`];function An(e){return e===`cpu`?`cpu`:e===`gpu-per-node`?`gpu · per-node`:`gpu · fused`}function jn(e){let t=new Mt(e.device,{...e.info===void 0?{}:{adapterInfo:e.info}});return{fused:t,perNode:On(t),dispose:()=>t.dispose()}}function Mn(e){let t={},n=0;for(let[r,i]of Object.entries(e))r===`run-partially-fused`?n+=i:t[r]=i;return{real:t,partial:n}}function Nn(e){let t=Object.entries(e).sort(([e],[t])=>e.localeCompare(t));return t.length===0?`none`:t.map(([e,t])=>`${e} ×${t}`).join(`, `)}function Pn(e,t){if(e.kind===`value`)return{objects:[],report:{kind:`value`,drew:[],skipped:`values have no scene representation`}};if(e.kind===`instances`){if(ne(e))return{objects:[],report:{kind:`instances`,drew:[],skipped:`device-resident batches`}};let n=wn(e.batches,t.assets);if(t.instanceMaterial!==void 0){let e=t.instanceMaterial,r={};for(let[t,i]of Object.entries(n))r[t]={geometry:i.geometry,material:e};n=r}let r=Nt(e.batches,n),i=0,a={};for(let e of r)i+=e.count,a[e.name]=(a[e.name]??0)+e.count;return{objects:r,report:{kind:`instances`,drew:[`instances`],instances:i,batches:a}}}let n=e.geo,r=ee(n),i=n.primitiveCount>0&&Object.keys(r).length===0,a=[],o=[],s=[],c=(e,t)=>{try{t(),o.push(e)}catch(e){i||s.push(e instanceof Error?e.message:String(e))}};((r.poly??0)>0||i)&&c(`mesh`,()=>{let e=It(n);a.push(new ye(e,t.materials.mesh(e.hasAttribute(`color`))))}),((r.polyline??0)>0||i&&o.length===0)&&c(`lines`,()=>{let e=Ft(n);a.push(new A(e,t.materials.line(e.hasAttribute(`color`))))});let l=n.pointCount>0&&o.length>0&&t.points!==!0;return n.pointCount>0&&!l&&(a.push(Pt(n,{size:t.pointSize??.1})),o.push(`points`)),{objects:a,report:{kind:`geometry`,drew:o,points:n.pointCount,primitives:r,...i?{untagged:!0}:{},...l?{pointsSuppressed:!0}:{},...s.length>0?{skipped:s.join(` | `)}:{}}}}function Fn(e){for(let t of e){if(t instanceof he){t.dispose();continue}if(t instanceof ye||t instanceof A||t instanceof ze){t.geometry.dispose();let e=t.material;if(Array.isArray(e))for(let t of e)t.dispose();else e.dispose()}}}function In(e){e.updateMatrixWorld(!0);let t=new qe().setFromObject(e);if(t.isEmpty())return null;let n=t.getCenter(new E),r=t.getSize(new E),i=e=>Number.isFinite(e.x)&&Number.isFinite(e.y)&&Number.isFinite(e.z);return!i(n)||!i(r)?null:(r.length()<1e-9&&(t.expandByScalar(.5),t.getSize(r)),{box:t,center:n,radius:r.length()/2,footprint:Math.max(r.x,r.z),height:r.y})}function Ln(e){let t=(22+16*(e.footprint<=1e-9?0:1-Math.min(e.height/e.footprint,1)))*Math.PI/180,n=Math.PI/4,r=Math.cos(t);return new E(Math.sin(n)*r,Math.sin(t),Math.cos(n)*r).normalize()}function Rn(e,t,n,r){let i=Math.tan(r.fov*Math.PI/180/2),a=i*r.aspect,o=n.clone().negate(),s=new E().crossVectors(o,new E(0,1,0));s.lengthSq()<1e-12&&s.set(1,0,0),s.normalize();let c=new E().crossVectors(s,o).normalize(),l=new E,u=0;for(let r=0;r<8;r++){l.set(r&1?e.max.x:e.min.x,r&2?e.max.y:e.min.y,r&4?e.max.z:e.min.z).sub(t);let o=l.dot(n);u=Math.max(u,o+Math.abs(l.dot(s))/a,o+Math.abs(l.dot(c))/i)}return u}function zn(e,t){let n=Math.max(t+e.radius*4,e.radius*8,.001);return{near:Math.max(n/1e5,1e-5),far:n}}function Bn(e,t,n){let r=Ln(e),i=e.box.clone(),a=e.box.getSize(new E).multiplyScalar(.050000000000000044/2);i.expandByVector(a);let o=Math.max(Rn(i,e.center,r,t),e.radius*.5,.001);t.position.copy(e.center).addScaledVector(r,o);let{near:s,far:c}=zn(e,o);t.near=s,t.far=c,t.updateProjectionMatrix(),t.lookAt(e.center),n&&(n.target.copy(e.center),n.update())}function Vn(e){if(!(e>0))return 1;let t=10**Math.floor(Math.log10(e)),n=e/t;return(n<=1?1:n<=2?2:n<=5?5:10)*t}function Hn(e){let t=Math.max(e.footprint*1.35,1),n=Vn(t/24),r=Math.ceil(t/n),i=Number((n*r).toPrecision(12)),a=e.box.getCenter(new E),o=i*.002;return{size:i,divisions:r,x:Math.round(a.x/n)*n,z:Math.round(a.z/n)*n,y:Math.min(0,e.box.min.y)-o,lift:o*.4}}function Un(e){let t=!1,n=!1,r=()=>{if(t){n=!0;return}t=!0,e().catch(e=>console.error(`recook failed:`,e)).finally(()=>{t=!1,n&&(n=!1,r())})};return r}function Wn(e){if(e.mode===`field`)return e.specText===null?`ƒ`:`ƒ ${e.specText.replace(/\s+/g,` `)}`;let t=e.value;return typeof t==`number`?Ct(t):typeof t==`boolean`?String(t):typeof t==`string`?t===``?`–`:t:Array.isArray(t)?t.length===0?`–`:t.map(e=>typeof e==`number`?Ct(e):String(e)).join(`, `):t==null?`–`:String(t)}function Gn(e){let t=e.filter(e=>e.mode!==`items`).map(e=>({key:e.key,value:Wn(e),field:e.mode===`field`}));return xt(t)}function Kn(){let e=new Map,t=new Map,n=e=>e.outputs.some(e=>e.kind===`instances`)?`spawners`:e.outputs.length>0&&e.outputs.every(e=>e.kind===`value`)?`values`:e.inputs.length===0?`sources`:`operators`,r=(e,t,n)=>{let r=e.get(t);r||e.set(t,r=[]),r.push(n)};for(let i of Fe()){if(i.type===`subgraph`)continue;let a={type:i.type,description:i.description};i.category===void 0?r(t,`other · ${n(i)}`,a):r(e,i.category,a)}return[...e.entries(),...t.entries()].map(([e,t])=>({name:e,entries:t}))}function qn(e,t){let n=0,r=`${e}_${n}`;for(;t.has(r);)n++,r=`${e}_${n}`;return r}var Jn=JSON.stringify({formatVersion:1,seed:1,nodes:[{id:`scatter`,type:`pointScatterInBounds`,params:{count:350,boundsMin:[-12,0,-12],boundsMax:[12,0,12],seed:0}},{id:`jitter`,type:`jitterPoints`,params:{amount:{fn:`mul`,args:[{fn:`remap`,args:[{fn:`fbm`,base:`perlinNoise`,opts:{frequency:.18,octaves:4}},-1,1,0,1]},1.4]},seed:0}},{id:`orient`,type:`orientAlongVector`,params:{direction:{fn:`normalize`,args:[{fn:`vec`,args:[{fn:`perlinNoise`,opts:{frequency:.15}},.9,{fn:`perlinNoise`,opts:{frequency:.15,offset:[37.7,11.3,0]}}]}]},up:[0,1,0],axis:`+y`}},{id:`spawn`,type:`spawnInstances`,params:{assetId:`cone`,assetAttr:``}}],connections:[{from:[`scatter`,`out`],to:[`jitter`,`in`]},{from:[`jitter`,`out`],to:[`orient`,`in`]},{from:[`orient`,`out`],to:[`spawn`,`in`]}],outputs:[{id:`spawn`,pin:`instances`,name:`spawn.instances`},{id:`spawn`,pin:`points`,name:`spawn.points`}]},null,2),Yn=new Set([`subgraph`,`forEach`]);function Xn(e){return e instanceof Error?e.message:String(e)}function Zn(e,t){e.dispatches+=t.dispatches,e.pipelinesCompiled+=t.pipelinesCompiled,e.pipelineCacheHits+=t.pipelineCacheHits,e.residentRuns+=t.residentRuns,e.fusedNodes+=t.fusedNodes,e.readbacksSaved+=t.readbacksSaved;for(let[n,r]of Object.entries(t.fallbacks)){let t=e.fallbacks[n]??0;e.fallbacks[n]=n===`run-partially-fused`?Math.max(t,r):t+r}}function Qn(e){return typeof e==`object`&&!!e&&!Array.isArray(e)}function $n(e){return Array.isArray(e)?[...e]:e}function er(e){return Array.isArray(e)&&e.length>0&&e.every(e=>typeof e==`number`)}function tr(e,t){let n=t;return(e.type===`i32`||e.type===`u32`)&&(n=Math.round(n)),e.type===`u32`&&(n=Math.max(0,n)),e.min!==void 0&&(n=Math.max(e.min,n)),e.max!==void 0&&(n=Math.min(e.max,n)),n}function nr(e){return{inputs:e.inputs.map(e=>({...e})),outputs:e.outputs.map(e=>({...e}))}}var rr=150,ir=class{hooks;mirror=new Te(0);pins=new Map;subgraphs=new Map;imported=new Map;outputNames=[];structureRev=0;freshGraph=!1;gpu;bail;cookTimer;recook;listener;constructor(e){this.hooks=e,this.recook=Un(()=>this.cookAll())}setGpuResolver(e){this.gpu!==e&&(this.gpu=e,this.scheduleCook())}abandonCook(e){this.bail?.(Error(e))}setStatusListener(e){this.listener=e}describeNode(e,t){if(Yn.has(t)){let n=this.subgraphs.get(e);return{label:n?.ref??t,description:n?.description??``}}return{label:t,description:ae(t).info.description}}addNode(e,t){if(Yn.has(t))return{error:`the registered "${t}" type is metadata-only; ${t} nodes enter the editor via import (their inner graph travels in the serialized payload)`};try{this.mirror.add(ae(t).def,void 0,e)}catch(e){return{error:Xn(e)}}let n=s(t);return this.pins.set(e,n),this.afterStructuralEdit(),nr(n)}connectEdge(e){try{this.mirror.connect({id:e.from},e.fromPin,{id:e.to},e.toPin)}catch(e){return Xn(e)}return this.afterStructuralEdit(),null}disconnectEdge(e){let t;try{t=this.mirror.disconnect({id:e.from},e.fromPin,{id:e.to},e.toPin)}catch(e){return Xn(e)}return t&&this.afterStructuralEdit(),null}deleteNode(e){try{this.mirror.removeNode({id:e})}catch(e){return Xn(e)}return this.pins.delete(e),this.subgraphs.delete(e),this.afterStructuralEdit(),null}setSeed(e){this.mirror.setSeed(e),this.scheduleCook()}paramViews(e,t){let n=Yn.has(t)?Object.fromEntries((this.subgraphs.get(e)?.params??[]).map(e=>[e.name,e.schema])):ae(t).info.params,r;try{r=this.mirror.getParams({id:e})}catch{r={}}return Object.entries(n).map(([e,t])=>{let n=r[e];if(t.type===`items`)return{key:e,schema:t,mode:`items`,value:null,specText:null};if(Je(n)){let r=null;try{r=fe(Me(n))}catch{r=null}return{key:e,schema:t,mode:`field`,value:null,specText:r}}return{key:e,schema:t,mode:`constant`,value:$n(n),specText:null}})}knobs(){let e=new Map(this.mirror.describe().nodes.map(e=>[e.id,this.subgraphs.get(e.id)?.ref??e.defType??``]));return le(this.mirror).map(t=>t.scope===`graph`?{key:t.key,scope:`graph`,nodeLabel:``,name:t.name,schema:t.schema,value:$n(t.value),isField:!1,exposed:t.exposed}:{key:t.key,scope:`node`,...t.scope===`driven`?{drivenBy:t.driver}:{},node:t.node,nodeLabel:e.get(t.node)??``,name:t.param,...t.fieldParam===void 0?{}:{fieldParam:t.fieldParam},schema:t.schema,value:t.holdsField?this.paramValue(t.node,t.param):$n(t.value),isField:t.holdsField,exposed:t.exposed})}paramValue(e,t){try{return this.mirror.getParams({id:e})[t]}catch{return}}applyKnobPatch(e){let t=new Map(this.knobs().map(e=>[e.key,e])),n=[],r=0;for(let[i,a]of Object.entries(e)){if(i===`seed`){typeof a==`number`&&Number.isFinite(a)?(this.setSeed(a>>>0),r++):n.push(`seed: expected a number, got ${JSON.stringify(a)}`);continue}let e=t.get(i);if(e===void 0){n.push(`${i}: this graph exposes no such knob`);continue}try{this.writeKnob(e,a),r++}catch(e){n.push(`${i}: ${Xn(e)}`)}}return r>0&&this.scheduleCook(),{applied:r,problems:n}}setKnob(e,t){try{this.writeKnob(e,t)}catch(e){return Xn(e)}return this.scheduleCook(),null}writeKnob(e,t){if(e.scope===`node`&&e.drivenBy!==void 0)throw Error(`"${e.node}".${e.name} is driven by the graph param "$${e.drivenBy}", so a value written here would be overwritten the next time the graph is opened — and the graph would refuse to open, because a node literal that disagrees with its driver is an error. Turn "$${e.drivenBy}" instead; it moves every slot it drives.`);if(e.scope===`graph`){if(typeof t!=`number`&&!er(t))throw Error(`graph param "${e.name}" takes a number or an array of numbers, got ${JSON.stringify(t)}`);this.mirror.setGraphParam(e.name,t);return}let n={id:e.node};if(e.fieldParam===void 0){this.mirror.setParam(n,e.name,$n(t));return}let r=this.mirror.getParams(n)[e.name],i=Je(r)?Ue(r):void 0;if(i===void 0)throw Error(`param "${e.name}" of node "${e.node}" no longer holds a field expression, so its "${e.fieldParam}" value has nowhere to be written — reopen the graph`);if(typeof t!=`number`&&!er(t))throw Error(`field-spec param "${e.fieldParam}" takes a number or an array of numbers, got `+JSON.stringify(t));let a;try{a=at(Pe(i,e.fieldParam,t))}catch(t){throw Error(`node "${e.node}" param "${e.name}": ${Xn(t)}`)}this.mirror.setParam(n,e.name,a)}setPlainParam(e,t,n){try{this.mirror.setParam({id:e},t,$n(n))}catch(e){return Xn(e)}return this.scheduleCook(),null}applyFieldParam(e,t,n){let r;try{r=St(n)}catch(e){return Xn(e)}let i;try{i=at(r)}catch(e){return Xn(e)}try{this.mirror.setParam({id:e},t,i)}catch(e){return Xn(e)}return this.scheduleCook(),null}exportText(){return JSON.stringify(bt(this.mirror),null,2)}importText(e){let t;try{t=JSON.parse(e)}catch(e){return{error:Xn(e)}}try{xe(t)}catch(e){return{error:Xn(e)}}let n=t,r=new Te(n.seed>>>0);r.setGraphParams(n.params??[]);let i=g(r.graphParams),a=new Map,o=new Map,s=[];try{for(let e of n.nodes){let t=this.addImportedNode(r,e,o,i);a.set(e.id,t);let n=nr(t),c=ce(e.type);s.push({id:e.id,type:e.type,...e.ref===void 0?{}:{label:e.ref.name},...c===void 0?{}:{category:c},x:0,y:0,inputs:n.inputs,outputs:n.outputs})}for(let e of n.connections??[])r.connect({id:e.from[0]},e.from[1],{id:e.to[0]},e.to[1])}catch(e){return{error:Xn(e)}}try{r.setGraphParams(Le(r,r.graphParams,new Map))}catch(e){return{error:Xn(e)}}this.mirror=r,this.pins.clear();for(let[e,t]of a)this.pins.set(e,t);this.subgraphs.clear();for(let[e,t]of o)this.subgraphs.set(e,t);this.imported=new Map((n.outputs??[]).map(e=>[e.name,{id:e.id,pin:e.pin}])),this.freshGraph=!0,this.afterStructuralEdit();let c=(n.connections??[]).map(e=>({from:e.from[0],fromPin:e.from[1],to:e.to[0],toPin:e.to[1]}));return de(s,c,new Map(s.map(e=>[e.id,Gn(this.paramViews(e.id,e.type)).length]))),{structure:{seed:n.seed>>>0,nodes:s,edges:c}}}addImportedNode(e,t,n,r){if(Yn.has(t.type)){let i=t.ref===void 0?t.subgraph:Dt(t.ref.name).subgraph;if(!i)throw Error(`node "${t.id}": subgraph node with neither a payload nor a ref`);let a=e=>({name:e.name,node:{id:e.node},pin:e.pin}),o=xe(i.graph),s=(i.params??[]).map(e=>c(o,{name:e.name,targets:e.targets.map(e=>({node:{id:e.node},param:e.param})),description:e.description,default:e.default,...e.min===void 0?{}:{min:e.min},...e.max===void 0?{}:{max:e.max}})),l=t.type===`forEach`?wt(o,i.inputs.map(a),i.outputs.map(a),s):nt(o,i.inputs.map(a),i.outputs.map(a),s),u={};for(let[e,n]of Object.entries(t.params??{}))u[e]=s.find(t=>t.name===e)?.schema?.acceptsField===!0&&Qn(n)?at(n,r):$n(n);e.add(l,u,t.id),n.set(t.id,{params:s,...t.ref===void 0?{}:{ref:t.ref.name},description:(t.ref===void 0?void 0:Dt(t.ref.name).meta?.description)??i.graph.meta?.description??``});let d=gt(l),f=e=>({name:e.name,kind:e.kind,multi:!1});return{inputs:(d?.inputs??[]).map(f),outputs:(d?.outputs??[]).map(f)}}let i=ae(t.type),a={};for(let[e,n]of Object.entries(t.params??{})){let t=i.info.params[e];t&&(a[e]=t.acceptsField===!0&&Qn(n)?at(n,r):$n(n))}return e.add(i.def,a,t.id),s(t.type)}afterStructuralEdit(){this.structureRev++,this.reconcileOutputs(),this.scheduleCook()}reconcileOutputs(){let e=this.mirror.describe(),t=(t,n)=>e.connections.some(e=>e.from[0]===t&&e.from[1]===n),n=new Map;for(let[e,{id:t,pin:r}]of this.imported)this.pins.get(t)?.outputs.some(e=>e.name===r)===!0&&n.set(`${t}.${r}`,{id:t,pin:r,name:e});for(let r of e.nodes)for(let e of this.pins.get(r.id)?.outputs??[]){let i=`${r.id}.${e.name}`;t(r.id,e.name)||n.has(i)||n.set(i,{id:r.id,pin:e.name,name:i})}let r=new Map([...n.values()].map(e=>[e.name,e])),i=new Set;for(let t of e.outputs)r.has(t.name)?i.add(t.name):this.mirror.removeOutput(t.name);for(let[e,{id:t,pin:n}]of r)i.has(e)||this.mirror.output({id:t},n,e);this.outputNames=[...r.keys()]}scheduleCook(){this.cookTimer!==void 0&&clearTimeout(this.cookTimer),this.cookTimer=setTimeout(()=>{this.cookTimer=void 0,this.recook()},rr)}async cookAll(){let e=this.mirror,t=this.structureRev,n=[...this.outputNames],i=()=>e!==this.mirror||t!==this.structureRev,a=[],o=[],s=[],c=0,l=0,u=this.gpu,d=u===void 0?void 0:r(),f=new Promise((e,t)=>{this.bail=t});f.catch(()=>{});let p=performance.now();try{for(let t of n){if(i())return;try{let n=await Promise.race([Ke(e,{outputs:[t],...u===void 0?{}:{gpu:u}}),f]);c+=n.stats.cooked,l+=n.stats.cached,d!==void 0&&n.stats.gpu!==void 0&&Zn(d,n.stats.gpu);let r=n.outputs[t];if(r){o.push(...r);for(let e=0;e<r.length;e++)s.push(t)}}catch(e){a.push(Xn(e))}}}finally{this.bail=void 0}let m=performance.now()-p,h=0,g=0,_=2166136261,v=new Float32Array(1),y=new Uint32Array(v.buffer),b=e=>{_=Math.imul(_^e>>>0,16777619)},x=e=>{v[0]=e,b(y[0])},S=e=>{for(let t=0;t<e.length;t++)b(e.charCodeAt(t))};for(let e of o)if(e.kind===`geometry`){let t=e.geo.pointCount;h+=t,b(t);let n=e.geo.attrs.point.get(`P`);if(n){let e=t*n.tupleSize;for(let t=0;t<e;t++)x(n.data[t])}}else if(e.kind===`instances`)for(let t of e.batches){g+=t.count,S(t.assetId),b(t.count);for(let e=0;e<t.transforms.length;e++)x(t.transforms[e])}else S(JSON.stringify(e.value));let C={errors:a,cooked:c,cached:l,elapsedMs:m,points:h,instances:g,outputs:n.length,hash:(_>>>0).toString(16).padStart(8,`0`),...d===void 0?{}:{gpu:d}};if(!i()){let e=this.freshGraph;this.freshGraph=!1,this.hooks.render(o,{fresh:e,outputs:s})}this.hooks.status(C),this.listener?.(C)}},ar=I(`<g class="edge svelte-xak2oi"><path class="edge-hit svelte-xak2oi" role="button" tabindex="-1"><title> </title></path><path class="edge-casing svelte-xak2oi"></path><path></path></g>`),or=I(`<path class="edge-casing svelte-xak2oi"></path><path></path>`,1),sr=I(`<text class="blank svelte-xak2oi" x="50%" y="50%">press Tab to add a node</text>`),cr=I(`<svg class="canvas svelte-xak2oi" role="application" aria-label="node graph canvas"><g><!><!><!></g><!></svg>`);function lr(e,n){we(n,!0);let r=W(void 0),i=W(oe({x:0,y:0,z:1})),a=null,o=W(null),s=W(null);function c(e){return L(r)?$e(L(i),L(r).getBoundingClientRect(),e.clientX,e.clientY):{x:0,y:0}}function u(e){L(r)&&(e.preventDefault(),t(i,Ve(L(i),L(r).getBoundingClientRect(),e.clientX,e.clientY,e.deltaY),!0))}function d(e){a={px:e.clientX,py:e.clientY,ox:L(i).x,oy:L(i).y},L(r)?.setPointerCapture(e.pointerId)}function f(e,t){return c({clientX:e,clientY:t})}let m=K(()=>new Map([...n.previews].map(([e,t])=>[e,t.length])));function h(){return Ne(n.model.nodes,L(m))}function g(e={}){if(!L(r))return;let n=h();if(n===null){t(i,{x:0,y:0,z:1},!0);return}let a=L(r).getBoundingClientRect(),o=Ie(n,a);t(i,e.preferActual===!0&&o>=1?Ze(1,n,a):Ze(_t(o),n,a),!0)}function v(){if(!L(r))return;let e=h();t(i,e===null?{x:0,y:0,z:1}:Ze(1,e,L(r).getBoundingClientRect()),!0)}let y=K(()=>new Map(n.model.nodes.map(e=>[e.id,e])));function b(e){return Ae(L(y),e)}function x(e){return ft(L(y),e)}function C(e,n){let r=c(n);t(o,{id:e.id,offX:r.x-e.x,offY:r.y-e.y},!0)}function E(e,n,r,i){let a=e.x+168,o=e.y+p(r),l=c(i),u=e.outputs.find(e=>e.name===n)?.kind??`any`;t(s,{from:e.id,fromPin:n,kind:u,x1:a,y1:o,x2:l.x,y2:l.y,hover:null},!0)}function D(e){if(a){t(i,{...L(i),x:a.ox+(e.clientX-a.px),y:a.oy+(e.clientY-a.py)},!0);return}if(L(o)){let t=c(e);n.onMove(L(o).id,t.x-L(o).offX,t.y-L(o).offY)}if(L(s)){let t=c(e);L(s).x2=t.x,L(s).y2=t.y}}function k(){a=null,L(s)&&(L(s).hover&&n.onConnect({from:L(s).from,fromPin:L(s).fromPin,to:L(s).hover.to,toPin:L(s).hover.toPin}),t(s,null)),t(o,null)}var A={graphPointAt:f,resetView:g,actualSize:v},j=cr();S(`pointermove`,T,D),S(`pointerup`,T,k);var M=V(j),N=V(M);F(N,17,()=>n.model.edges,Xe,(e,t,r)=>{let i=K(()=>b(L(t)));var a=w(),o=B(a),s=e=>{var a=ar(),o=V(a),s=V(o),c=V(s);q(s),q(o);var l=H(o),u=H(l);q(a),O(e=>{J(o,`d`,L(i)),J(o,`aria-label`,`disconnect ${L(t).from??``}.${L(t).fromPin??``} → ${L(t).to??``}.${L(t).toPin??``}`),P(c,`${L(t).from??``}.${L(t).fromPin??``} → ${L(t).to??``}.${L(t).toPin??``} — click to disconnect`),J(l,`d`,L(i)),Y(u,0,`edge-line k-${e??``}`,`svelte-xak2oi`),J(u,`d`,L(i))},[()=>x(L(t))]),R(`click`,o,()=>n.onDeleteEdge(r)),R(`keydown`,o,e=>e.key===`Enter`&&n.onDeleteEdge(r)),_(e,a)};G(o,e=>{L(i)&&e(s)}),_(e,a)});var ee=H(N),te=e=>{let t=K(()=>rt({x:L(s).x1,y:L(s).y1},{x:L(s).x2,y:L(s).y2}));var n=or(),r=B(n),i=H(r);let a;O(()=>{J(r,`d`,L(t)),a=Y(i,0,`wire k-${L(s).kind??``}`,`svelte-xak2oi`,a,{live:L(s).hover!==null}),J(i,`d`,L(t))}),_(e,n)};G(ee,e=>{L(s)&&e(te)});var ne=H(ee);F(ne,17,()=>n.model.nodes,e=>e.id,(e,t)=>{{let r=K(()=>L(t).id===n.selectedId),i=K(()=>n.previews.get(L(t).id));be(e,{get node(){return L(t)},get selected(){return L(r)},get params(){return L(i)},onSelect:()=>n.onSelect(L(t).id),onBodyDown:e=>C(L(t),e),onOutDown:(e,n,r)=>E(L(t),n,r,e),onInEnter:e=>{L(s)&&(L(s).hover={to:L(t).id,toPin:e})},onInLeave:()=>{L(s)&&(L(s).hover=null)}})}}),q(M);var re=H(M),ie=e=>{var t=sr();_(e,t)};return G(re,e=>{n.model.nodes.length===0&&e(ie)}),q(j),He(j,e=>t(r,e),()=>L(r)),O(e=>{J(M,`transform`,`translate(${L(i).x??``} ${L(i).y??``}) scale(${L(i).z??``})`),l(M,`--hairline: ${e??``}`)},[()=>Math.max(1,1/L(i).z)]),S(`wheel`,j,u),R(`contextmenu`,j,e=>e.preventDefault()),R(`pointerdown`,j,e=>{if(e.button===2||e.button===1){e.preventDefault(),d(e);return}e.target===L(r)&&n.onSelect(null)}),_(e,j),We(A)}o([`contextmenu`,`pointerdown`,`click`,`keydown`]);var ur=140,dr=210,fr=96,pr=18,mr=14,hr=14,gr=6,_r=64,vr=12,yr=13,br=18;function xr(e){return 23+e*hr+gr}function Sr(e){return 23+e*hr+hr/2}function Cr(e){return Sr(e)+3}function wr(e,t){return e.length<=t?e:`${e.slice(0,t-1)}…`}function Tr(e){return String(!Number.isFinite(e)||Number.isInteger(e)?e:Number(e.toFixed(2)))}function Er(e){return typeof e==`number`?Tr(e):typeof e==`string`?e:typeof e==`boolean`?String(e):Array.isArray(e)?`[${e.map(e=>typeof e==`number`?Tr(e):String(e)).join(`, `)}]`:String(e)}function Dr(e){return typeof e==`object`&&!!e&&!Array.isArray(e)}function Or(e){return Dr(e)&&typeof e.fn==`string`}function kr(e){return typeof e==`number`||typeof e==`string`||typeof e==`boolean`||Array.isArray(e)&&e.every(e=>typeof e==`number`)}var Ar=null;function jr(e,t){Ar===null&&(Ar=new Map(Qe().map(e=>[e.fn,(e.args??[]).map(e=>e.name)])));let n=Ar.get(e),r=n!==void 0&&n.length>0&&n[n.length-1].endsWith(`…`)?n.length-1:1/0;return t.index>=r?`[${t.index}]`:n?.[t.index]??t.path}function Mr(e,t){return t.kind===`arg`?jr(e,t):t.kind===`case`?t.key:t.kind}var Nr=new Set([`fn`,`args`,`opts`,`cases`,`default`]);function Pr(e,t,n){let r=String(e.fn),i=[],a=[];for(let o of kt(e)){let e=wr(Mr(r,o),vr);if(Or(o.value)){if(n+1>=_r){i.push({label:e,value:`…`,pin:!1});continue}i.push({label:e,value:null,pin:!0});let r=t===``?o.path:`${t}.${o.path}`;a.push({row:i.length-1,node:Pr(o.value,r,n+1)})}else i.push({label:e,value:wr(Er(o.value),yr),pin:!1})}for(let[t,n]of Object.entries(e))Nr.has(t)||kr(n)&&i.push({label:wr(t,vr),value:wr(Er(n),yr),pin:!1});let o=e.opts;if(Dr(o))for(let[e,t]of Object.entries(o))e!==`position`&&(e===`seed`&&Dr(t)||kr(t)&&i.push({label:wr(e,vr),value:wr(Er(t),yr),pin:!1}));return{id:t,fn:r,depth:n,rows:i,kids:a,slot:-1}}function Fr(e){if(!Or(e))return{nodes:[],edges:[],width:0,height:0};let t=Pr(e,``,0),n=[],r=0,i=0,a=0;function o(e){e.depth>a&&(a=e.depth);let t=xr(e.rows.length),s;if(e.kids.length===0){let e=Math.max(r*fr,i);s=e+t/2,r+=1,i=e+t+pr}else{let t=e.kids.map(e=>o(e.node));s=(t[0]+t[t.length-1])/2}return e.slot=n.length,n.push({built:e,cy:s,h:t}),s}o(t);let s=new Map;for(let e of n){let t=s.get(e.built.depth);t===void 0?s.set(e.built.depth,[e]):t.push(e)}let c=1/0,l=-1/0;for(let e of s.values()){e.sort((e,t)=>e.cy-t.cy);let t=-1/0;for(let n of e){let e=Math.max(n.cy-n.h/2,t);n.cy=e+n.h/2,t=e+n.h+pr,c=Math.min(c,e),l=Math.max(l,e+n.h)}}let u=mr-c,d=n.map(e=>({id:e.built.id,fn:wr(e.built.fn,br),x:mr+(a-e.built.depth)*dr,y:e.cy-e.h/2+u,w:ur,h:e.h,rows:e.built.rows})),f=[];for(let[e,t]of n.entries()){let n=d[e];for(let e of t.built.kids){let t=d[e.node.slot];f.push({from:t.id,label:n.rows[e.row].label,ax:t.x+t.w,ay:t.y+t.h/2,bx:n.x,by:n.y+Sr(e.row)})}}return{nodes:d,edges:f,width:28+a*dr+ur,height:28+(l-c)}}var Ir=I(`<text class="note svelte-13d730a" y="19"> </text>`),Lr=I(`<path class="wire-casing svelte-13d730a"></path><path class="wire svelte-13d730a"><title> </title></path>`,1),Rr=I(`<text class="rval svelte-13d730a"> </text>`),zr=I(`<circle class="pin svelte-13d730a" cx="0" r="4.5"></circle>`),Br=I(`<text class="rkey svelte-13d730a"> </text><!><!>`,1),Vr=I(`<circle class="pin svelte-13d730a" r="4.5"></circle>`),Hr=I(`<g><title> </title><rect class="body svelte-13d730a" rx="4"></rect><line class="sep svelte-13d730a" x1="0"></line><text class="title svelte-13d730a"> </text><!><!></g>`),Ur=I(`<!><!>`,1),Wr=I(`<svg class="fieldtree svelte-13d730a" role="img" aria-label="field expression diagram"><!></svg>`);function Gr(e,t){we(t,!0);let n=K(()=>t.spec===null?null:Fr(t.spec)),r=K(()=>L(n)===null?`this is not valid JSON — the editor below says where`:L(n).nodes.length===0?`no field expression here: the JSON has no "fn"`:null);function i(e){let t=Math.max(46,Math.min(130,Math.abs(e.bx-e.ax)*.5));return`M ${e.ax} ${e.ay} C ${e.ax+t} ${e.ay}, ${e.bx-t} ${e.by}, ${e.bx} ${e.by}`}var a=Wr(),o=V(a),s=e=>{var t=Ir(),n=V(t,!0);q(t),O(()=>{J(t,`x`,8),P(n,L(r))}),_(e,t)},c=e=>{var t=Ur(),r=B(t);F(r,17,()=>L(n).edges,e=>e.from,(e,t)=>{let n=K(()=>i(L(t)));var r=Lr(),a=B(r),o=H(a),s=V(o),c=V(s,!0);q(s),q(o),O(()=>{J(a,`d`,L(n)),J(o,`d`,L(n)),P(c,L(t).label)}),_(e,r)});var a=H(r);F(a,17,()=>L(n).nodes,e=>e.id,(e,t)=>{var n=Hr(),r=V(n),i=V(r);q(r);var a=H(r),o=H(a),s=H(o),c=V(s,!0);q(s);var l=H(s);F(l,19,()=>L(t).rows,(e,t)=>e.label+t,(e,n,r)=>{var i=Br(),a=B(i),o=V(a,!0);q(a);var s=H(a),c=e=>{var i=Rr(),a=V(i,!0);q(i),O(e=>{J(i,`x`,L(t).w-8),J(i,`y`,e),P(a,L(n).value)},[()=>Cr(L(r))]),_(e,i)};G(s,e=>{L(n).value!==null&&e(c)});var l=H(s),u=e=>{var t=zr();O(e=>J(t,`cy`,e),[()=>Sr(L(r))]),_(e,t)};G(l,e=>{L(n).pin&&e(u)}),O(e=>{J(a,`x`,8),J(a,`y`,e),P(o,L(n).label)},[()=>Cr(L(r))]),_(e,i)});var u=H(l),d=e=>{var n=Vr();O(()=>{J(n,`cx`,L(t).w),J(n,`cy`,L(t).h/2)}),_(e,n)};G(u,e=>{L(t).id!==``&&e(d)}),q(n),O(()=>{J(n,`transform`,`translate(${L(t).x??``}, ${L(t).y??``})`),P(i,`${L(t).fn??``} — ${(L(t).id===``?`root`:L(t).id)??``}`),J(a,`width`,L(t).w),J(a,`height`,L(t).h),J(o,`y1`,23),J(o,`x2`,L(t).w),J(o,`y2`,23),J(s,`x`,8),J(s,`y`,15),P(c,L(t).fn)}),_(e,n)}),_(e,t)};G(o,e=>{L(r)===null?L(n)!==null&&e(c,1):e(s)}),q(a),O(()=>{J(a,`width`,L(r)===null&&L(n)!==null?L(n).width:420),J(a,`height`,L(r)===null&&L(n)!==null?L(n).height:30)}),_(e,a),We()}var Kr=k(`<input type="number" step="any" class="svelte-yfws2t"/>`),qr=k(`<div class="vec svelte-yfws2t"></div>`),Jr=k(`<div class="reads svelte-yfws2t"> </div>`),Yr=k(`<div class="error svelte-yfws2t"> </div>`),Xr=k(`<div class="ft-backdrop svelte-yfws2t" role="presentation"><div class="ft-modal svelte-yfws2t" role="dialog" aria-label="field expression diagram"><div class="ft-head svelte-yfws2t"><span>field expression — read-only</span> <button class="svelte-yfws2t">close</button></div> <div class="ft-scroll svelte-yfws2t"><!></div></div></div>`),Zr=k(`<textarea rows="7" spellcheck="false" class="svelte-yfws2t"></textarea> <div class="apply-row svelte-yfws2t"><button class="diagram svelte-yfws2t">diagram</button> <button class="apply svelte-yfws2t">apply field</button></div> <!> <!> <!>`,1),Qr=k(`<div class="fieldparam"><div class="modes svelte-yfws2t"><button>constant</button> <button>field</button></div> <!></div>`);function $r(e,n){we(n,!0);function r(e){return document.body.appendChild(e),{destroy(){e.remove()}}}let i=W(oe(n.view.mode===`field`?`field`:`constant`)),a=W(oe(n.view.specText??o()));function o(){try{return fe({fn:`constant`,value:n.view.schema.default})}catch{return`0`}}let s=W(null),c=W(!1),l=K(()=>{try{return St(L(a))}catch{return null}}),u=K(()=>L(l)!==null),d=K(()=>L(l)===null?[]:vt(L(l))),f=e=>Array.isArray(e)?e.map(e=>Number(e)):typeof e==`number`?[e]:[];function p(){t(i,`field`),t(s,null)}function m(){t(i,`constant`),t(s,null);let e=n.view.mode===`constant`?n.view.value:void 0,r=n.view.schema.default,a=e??(Array.isArray(r)?[...r]:r);t(s,n.onPlain(a),!0)}function h(){t(s,n.onField(L(a)),!0)}let g=K(()=>n.view.schema.type===`vec4`?4:3),v=K(()=>{let e=f(n.view.value??n.view.schema.default);return e.length===L(g)?e:Array(L(g)).fill(e.length===1?e[0]:0)});function y(e){let r=e.currentTarget.valueAsNumber;Number.isFinite(r)&&t(s,n.onPlain(tr(n.view.schema,r)),!0)}function b(e,r){let i=r.currentTarget.valueAsNumber;if(!Number.isFinite(i))return;let a=[...L(v)];a[e]=tr(n.view.schema,i),t(s,n.onPlain(a),!0)}function x(e){L(c)&&e.key===`Escape`&&t(c,!1)}var C=Qr();S(`keydown`,T,x);var E=V(C),D=V(E);let k;var A=H(D,2);let j;q(E);var M=H(E,2),N=e=>{var t=w(),r=B(t),i=e=>{var t=qr();F(t,21,()=>L(v),Xe,(e,t,r)=>{var i=Kr();U(i),O(()=>{J(i,`min`,n.view.schema.min),J(i,`max`,n.view.schema.max),Oe(i,L(t))}),R(`change`,i,e=>b(r,e)),_(e,i)}),q(t),_(e,t)},a=e=>{var t=Kr();U(t),O(e=>{J(t,`min`,n.view.schema.min),J(t,`max`,n.view.schema.max),Oe(t,e)},[()=>typeof n.view.value==`number`?n.view.value:Number(n.view.schema.default)]),R(`change`,t,y),_(e,t)};G(r,e=>{n.view.schema.type===`vec3`||n.view.schema.type===`vec4`?e(i):e(a,-1)}),_(e,t)},ee=e=>{var n=Zr(),i=B(n);dt(i);var o=H(i,2),f=V(o),p=H(f,2);q(o);var m=H(o,2),g=e=>{var t=Jr(),n=V(t);q(t),O(e=>P(n,`reads ${e??``} — supplied by an exposed param of that
        name on an enclosing subgraph, never by this graph's own knobs`),[()=>L(d).map(e=>`"${e}"`).join(`, `)]),_(e,t)};G(m,e=>{L(d).length>0&&e(g)});var v=H(m,2),y=e=>{var t=Yr(),n=V(t,!0);q(t),O(()=>P(n,L(s))),_(e,t)};G(v,e=>{L(s)!==null&&e(y)});var b=H(v,2),x=e=>{var n=Xr(),i=V(n),a=V(i),o=H(V(a),2);q(a);var s=H(a,2);Gr(V(s),{get spec(){return L(l)}}),q(s),q(i),q(n),pe(n,e=>r?.(e)),R(`click`,n,e=>e.target===e.currentTarget&&t(c,!1)),R(`click`,o,()=>t(c,!1)),_(e,n)};G(b,e=>{L(c)&&e(x)}),O(()=>f.disabled=!L(u)),Re(i,()=>L(a),e=>t(a,e)),R(`click`,f,()=>t(c,!0)),R(`click`,p,h),_(e,n)};G(M,e=>{L(i)===`constant`?e(N):e(ee,-1)}),q(C),O(()=>{k=Y(D,1,`svelte-yfws2t`,null,k,{active:L(i)===`constant`}),j=Y(A,1,`svelte-yfws2t`,null,j,{active:L(i)===`field`})}),R(`click`,D,m),R(`click`,A,p),_(e,C),We()}o([`click`,`change`]);var ei=k(`<p class="note svelte-19tleln">composite: its inner graph is not editable here, but the params it exposes are — each one
        writes into the inner nodes it was declared over</p>`),ti=k(`<div class="readonly svelte-19tleln">runtime-injected (bound via graph.setParam; never serialized)</div>`),ni=k(`<input type="number" class="svelte-19tleln"/>`),ri=k(`<input type="checkbox" class="svelte-19tleln"/>`),ii=k(`<option> </option>`),ai=k(`<select class="svelte-19tleln"></select>`),oi=k(`<input type="text" class="svelte-19tleln"/>`),si=k(`<input type="number" step="any" class="svelte-19tleln"/>`),ci=k(`<div class="vec svelte-19tleln"></div>`),li=k(`<div class="list-row svelte-19tleln"><input type="text" class="svelte-19tleln"/> <button class="svelte-19tleln">✕</button></div>`),ui=k(`<div class="list"><!> <button class="add svelte-19tleln">+ add</button></div>`),di=k(`<div class="param svelte-19tleln"><div class="label svelte-19tleln"> <span class="ptype svelte-19tleln"> </span></div> <!></div>`),fi=k(`<div class="head svelte-19tleln"><div class="who svelte-19tleln"><div class="type svelte-19tleln"> </div> <div class="id svelte-19tleln"> </div></div> <button class="danger svelte-19tleln">delete</button></div> <p class="desc svelte-19tleln"> </p> <!> <!>`,1),pi=k(`<div class="inspector"><!></div>`);function mi(e,t){we(t,!0);let r=K(()=>(t.paramsRev,t.controller.paramViews(t.node.id,t.node.type))),i=K(()=>t.controller.describeNode(t.node.id,t.node.type)),a=e=>Array.isArray(e)?e.map(e=>Number(e)):typeof e==`number`?[e]:[],o=e=>Array.isArray(e)?e.map(e=>String(e)):[],s=e=>typeof e==`string`?e:String(e??``),c=e=>typeof e==`number`?e:0,l=e=>e===!0,u=(e,t)=>tr(e.schema,t);function d(e,n){let r=n.currentTarget.valueAsNumber;Number.isFinite(r)&&t.onPlain(t.node.id,e.key,u(e,r))}function f(e,n,r){let i=r.currentTarget.valueAsNumber;if(!Number.isFinite(i))return;let o=a(e.value);o[n]=u(e,i),t.onPlain(t.node.id,e.key,o)}function p(e,n){t.onPlain(t.node.id,e.key,n.currentTarget.value)}function m(e,n){t.onPlain(t.node.id,e.key,n.currentTarget.value)}function h(e,n){t.onPlain(t.node.id,e.key,n.currentTarget.checked)}function g(e,n,r){let i=o(e.value);i[n]=r.currentTarget.value,t.onPlain(t.node.id,e.key,i)}function v(e,n){let r=o(e.value);r.splice(n,1),t.onPlain(t.node.id,e.key,r)}function y(e){t.onPlain(t.node.id,e.key,[...o(e.value),``])}var b=pi(),x=V(b);n(x,()=>t.node.id,e=>{var n=fi(),u=B(n),b=V(u),x=V(b),S=V(x,!0);q(x);var C=H(x,2),w=V(C,!0);q(C),q(b);var T=H(b,2);q(u);var E=H(u,2),D=V(E,!0);q(E);var k=H(E,2),A=e=>{var t=ei();_(e,t)};G(k,e=>{t.node.type===`subgraph`&&e(A)});var j=H(k,2);F(j,17,()=>L(r),e=>e.key,(e,n)=>{var r=di(),i=V(r),u=V(i),b=H(u),x=V(b);q(b),q(i);var S=H(i,2),C=e=>{var t=ti();_(e,t)},w=e=>{$r(e,{get view(){return L(n)},onPlain:e=>t.onPlain(t.node.id,L(n).key,e),onField:e=>t.onFieldApply(t.node.id,L(n).key,e)})},T=e=>{var t=ni();U(t),O(e=>{Oe(t,e),J(t,`min`,L(n).schema.min),J(t,`max`,L(n).schema.max),J(t,`step`,L(n).schema.type===`f32`?`any`:1)},[()=>c(L(n).value)]),R(`change`,t,e=>d(L(n),e)),_(e,t)},E=e=>{var t=ri();U(t),O(e=>Ot(t,e),[()=>l(L(n).value)]),R(`change`,t,e=>h(L(n),e)),_(e,t)},D=e=>{var t=ai();F(t,20,()=>L(n).schema.enum??[],e=>e,(e,t)=>{var n=ii(),r=V(n,!0);q(n);var i={};O(()=>{P(r,t),i!==(i=t)&&(n.value=(n.__value=t)??``)}),_(e,n)}),q(t);var r;Tt(t),O(e=>{r!==(r=e)&&(t.value=(t.__value=e)??``,ue(t,e))},[()=>s(L(n).value)]),R(`change`,t,e=>m(L(n),e)),_(e,t)},k=e=>{var t=oi();U(t),O(e=>Oe(t,e),[()=>s(L(n).value)]),R(`change`,t,e=>p(L(n),e)),_(e,t)},A=e=>{var t=ci();F(t,21,()=>a(L(n).value),Xe,(e,t,r)=>{var i=si();U(i),O(()=>Oe(i,L(t))),R(`change`,i,e=>f(L(n),r,e)),_(e,i)}),q(t),_(e,t)},j=e=>{var t=ui(),r=V(t);F(r,17,()=>o(L(n).value),Xe,(e,t,r)=>{var i=li(),a=V(i);U(a);var o=H(a,2);q(i),O(()=>Oe(a,L(t))),R(`change`,a,e=>g(L(n),r,e)),R(`click`,o,()=>v(L(n),r)),_(e,i)});var i=H(r,2);q(t),R(`click`,i,()=>y(L(n))),_(e,t)};G(S,e=>{L(n).mode===`items`?e(C):L(n).schema.acceptsField?e(w,1):L(n).schema.type===`f32`||L(n).schema.type===`i32`||L(n).schema.type===`u32`?e(T,2):L(n).schema.type===`bool`?e(E,3):L(n).schema.type===`enum`?e(D,4):L(n).schema.type===`string`?e(k,5):L(n).schema.type===`vec3`||L(n).schema.type===`vec4`?e(A,6):L(n).schema.type===`stringList`&&e(j,7)}),q(r),O(()=>{J(i,`title`,L(n).schema.description),P(u,`${L(n).key??``} `),P(x,`${L(n).schema.type??``}${L(n).schema.acceptsField?` · field`:``}`)}),_(e,r)}),O(()=>{P(S,L(i).label),P(w,t.node.id),P(D,L(i).description)}),R(`click`,T,()=>t.onDelete(t.node.id)),_(e,n)}),q(b),_(e,b),We()}o([`click`,`change`]);var hi=k(`<button class="svelte-10at963"> </button>`),gi=k(`<button class="primary svelte-10at963">import</button>`),_i=k(`<div class="error svelte-10at963"> </div>`),vi=k(`<div class="backdrop svelte-10at963" role="presentation"><div class="modal svelte-10at963" role="dialog"><div class="head svelte-10at963"><span> </span> <div class="buttons svelte-10at963"><!> <button class="svelte-10at963">close</button></div></div> <textarea rows="18" spellcheck="false" class="svelte-10at963"></textarea> <!></div></div>`);function yi(e,n){we(n,!0);let r=W(oe(n.initial)),i=W(null),a=W(!1);async function o(){await navigator.clipboard.writeText(L(r)),t(a,!0),setTimeout(()=>t(a,!1),1200)}function s(){n.onApply&&(t(i,n.onApply(L(r)),!0),L(i)===null&&n.onClose())}function c(e){e.key===`Escape`&&n.onClose()}var l=vi();S(`keydown`,T,c);var u=V(l),d=V(u),f=V(d),p=V(f,!0);q(f);var m=H(f,2),h=V(m),g=e=>{var t=hi(),n=V(t,!0);q(t),O(()=>P(n,L(a)?`copied!`:`copy`)),R(`click`,t,o),_(e,t)},v=e=>{var t=gi();R(`click`,t,s),_(e,t)};G(h,e=>{n.mode===`export`?e(g):e(v,-1)});var y=H(h,2);q(m),q(d);var b=H(d,2);dt(b);var x=H(b,2),C=e=>{var t=_i(),n=V(t,!0);q(t),O(()=>P(n,L(i))),_(e,t)};G(x,e=>{L(i)!==null&&e(C)}),q(u),q(l),O(()=>{J(u,`aria-label`,n.title),P(p,n.title),b.readOnly=n.mode===`export`,J(b,`placeholder`,n.mode===`import`?`paste serialized graph JSON here…`:``)}),R(`click`,l,e=>e.target===e.currentTarget&&n.onClose()),R(`click`,y,function(...e){n.onClose?.apply(this,e)}),Re(b,()=>L(r),e=>t(r,e)),_(e,l),We()}o([`click`]);function bi(e,t){let n=e;if(t.kind===`flagGrid`||t.kind===`numberGrid`){let e=n[t.key];Ci(e)&&(e[t.item]=t.value);return}n[t.key]=t.value}function xi(e){let t={};for(let[n,r]of Object.entries(e))t[n]=Array.isArray(r)?[...r]:Ci(r)?{...r}:r;return t}function Si(e,t,n){let r=e,i=n;for(let[e,n]of Object.entries(t))if(Array.isArray(n)){let t=i[e];Array.isArray(t)&&t.length===n.length&&n.every((e,n)=>Object.is(e,t[n]))||(r[e]=[...n])}else if(Ci(n)){let t=r[e],a=i[e];if(!Ci(t)||!Ci(a)){r[e]={...n};continue}for(let[e,r]of Object.entries(n))Object.is(r,a[e])||(t[e]=r)}else Object.is(n,i[e])||(r[e]=n);return xi(t)}function Ci(e){return typeof e==`object`&&!!e&&!Array.isArray(e)}function wi(e){if(e>=1)return 0;let t=String(e),n=t.indexOf(`.`);return n<0?0:t.length-n-1}function Ti(e,t,n){return`${e.toFixed(wi(t))}${n??``}`}function Ei(e,t,n,r){return Math.min(n,Math.max(t,r>=1?Math.round(e):e))}var Di=k(`<button role="tab"> </button>`),Oi=k(`<div class="tabs svelte-lxjm1w" role="tablist" aria-label="controls"></div>`),ki=k(`<h2 class="svelte-lxjm1w"> </h2>`),Ai=k(`<label class="row svelte-lxjm1w"><span class="svelte-lxjm1w"> </span> <input type="range" class="svelte-lxjm1w"/> <em class="svelte-lxjm1w"> </em></label>`),ji=k(`<em class="unit svelte-lxjm1w"> </em>`),Mi=k(`<label class="row svelte-lxjm1w"><span class="svelte-lxjm1w"> </span> <input class="num svelte-lxjm1w" type="number"/> <!></label>`),Ni=k(`<label class="row svelte-lxjm1w"><span class="svelte-lxjm1w"> </span> <input class="num svelte-lxjm1w" type="text"/></label>`),Pi=k(`<input class="num svelte-lxjm1w" type="number"/>`),Fi=k(`<div class="row svelte-lxjm1w"><span class="svelte-lxjm1w"> </span> <div class="vec svelte-lxjm1w"></div></div>`),Ii=k(`<option> </option>`),Li=k(`<label class="row svelte-lxjm1w"><span class="svelte-lxjm1w"> </span> <select class="svelte-lxjm1w"></select></label>`),Ri=k(`<label class="check svelte-lxjm1w"><input type="checkbox" class="svelte-lxjm1w"/> <span> </span></label>`),zi=k(`<div class="row svelte-lxjm1w"><span class="svelte-lxjm1w"> </span> <!></div>`),Bi=k(`<div class="row stack svelte-lxjm1w"><span class="svelte-lxjm1w"> </span> <div class="checks svelte-lxjm1w"></div></div>`),Vi=k(`<div class="gridhead svelte-lxjm1w"> </div>`),Hi=k(`<label class="cell svelte-lxjm1w"><span class="svelte-lxjm1w"> </span> <input class="num svelte-lxjm1w" type="number"/></label>`),Ui=k(`<div class="grid svelte-lxjm1w"><!> <div class="gridrow svelte-lxjm1w"></div></div>`),Wi=k(`<div class="group svelte-lxjm1w"><!> <!> <!></div>`),Gi=k(`<!> <!>`,1);function Ki(e,t){we(t,!0);let n=et(t,`tab`,15,``),r=et(t,`tabbed`,3,!1),i=e=>typeof e==`object`&&!!e&&!Array.isArray(e),a=e=>typeof t.values[e]==`number`?t.values[e]:0,o=e=>typeof t.values[e]==`string`?t.values[e]:``,s=e=>t.values[e]===!0,c=e=>i(t.values[e])?t.values[e]:{},u=e=>i(t.values[e])?t.values[e]:{},d=e=>Array.isArray(t.values[e])?t.values[e]:[];function f(e,n,r){let i=Number(n);if(!Number.isFinite(i))return;let a={kind:`slider`,control:e,key:e.key,value:i};t.onInput(a),(r||e.live===!0)&&t.onCommit(a)}function p(e,n){let r=Number(n);if(!Number.isFinite(r))return;let i=Ei(r,e.min??-1/0,e.max??1/0,e.step??0),a={kind:`number`,control:e,key:e.key,value:i};t.onInput(a),t.onCommit(a)}function m(e,n){let r={kind:`text`,control:e,key:e.key,value:n};t.onInput(r),t.onCommit(r)}function g(e,n,r){let i=Number(r);if(!Number.isFinite(i))return;let a=[...d(e.key)];a[n]=Ei(i,e.min??-1/0,e.max??1/0,e.step??0);let o={kind:`vector`,control:e,key:e.key,value:a};t.onInput(o),t.onCommit(o)}function v(e,n){let r={kind:`select`,control:e,key:e.key,value:n};t.onInput(r),t.onCommit(r)}function y(e,n,r){let i={kind:`flag`,control:e,key:n,value:r};t.onInput(i),t.onCommit(i)}function b(e,n,r){let i={kind:`flagGrid`,control:e,key:e.key,item:n,value:r};t.onInput(i),t.onCommit(i)}function x(e,n,r){let i=Number(r);if(!Number.isFinite(i))return;let a=Ei(i,e.min,e.max,e.step),o={kind:`numberGrid`,control:e,key:e.key,item:n,value:a};t.onInput(o),t.onCommit(o)}function S(e,t){return e.kind===`flags`?`flags:${t}`:`${e.kind}:${e.key}`}var C=Gi(),T=B(C),E=e=>{var r=Oi();F(r,21,()=>t.sections,e=>e.title,(e,t)=>{var r=Di();let i;var a=V(r,!0);q(r),O(()=>{i=Y(r,1,`tab svelte-lxjm1w`,null,i,{on:n()===L(t).title}),J(r,`aria-selected`,n()===L(t).title),P(a,L(t).title)}),R(`click`,r,()=>n(L(t).title)),_(e,r)}),q(r),_(e,r)};G(T,e=>{r()&&e(E)});var D=H(T,2);F(D,17,()=>t.sections,e=>e.title,(e,i)=>{var C=Wi(),T=V(C),E=e=>{var t=ki(),n=V(t,!0);q(t),O(()=>P(n,L(i).title)),_(e,t)};G(T,e=>{!r()&&L(i).title!==``&&e(E)});var D=H(T,2);h(D,()=>t.extra??je,()=>L(i).title);var k=H(D,2);F(k,19,()=>L(i).controls,(e,t)=>S(e,t),(e,t)=>{var n=w(),r=B(n),i=e=>{var n=Ai(),r=V(n),i=V(r,!0);q(r);var o=H(r,2);U(o);var s=H(o,2),c=V(s,!0);q(s),q(n),O((e,r)=>{J(n,`title`,L(t).description),P(i,L(t).label),J(o,`min`,L(t).min),J(o,`max`,L(t).max),J(o,`step`,L(t).step),Oe(o,e),P(c,r)},[()=>a(L(t).key),()=>Ti(a(L(t).key),L(t).step,L(t).unit)]),R(`input`,o,e=>f(L(t),e.currentTarget.value,!1)),R(`change`,o,e=>f(L(t),e.currentTarget.value,!0)),_(e,n)},h=e=>{var n=Mi(),r=V(n),i=V(r,!0);q(r);var o=H(r,2);U(o);var s=H(o,2),c=e=>{var n=ji(),r=V(n,!0);q(n),O(()=>P(r,L(t).unit)),_(e,n)};G(s,e=>{L(t).unit!==void 0&&e(c)}),q(n),O(e=>{J(n,`title`,L(t).description),P(i,L(t).label),J(o,`min`,L(t).min),J(o,`max`,L(t).max),J(o,`step`,L(t).step??`any`),Oe(o,e)},[()=>a(L(t).key)]),R(`change`,o,e=>p(L(t),e.currentTarget.value)),_(e,n)},S=e=>{var n=Ni(),r=V(n),i=V(r,!0);q(r);var a=H(r,2);U(a),q(n),O(e=>{J(n,`title`,L(t).description),P(i,L(t).label),Oe(a,e)},[()=>o(L(t).key)]),R(`change`,a,e=>m(L(t),e.currentTarget.value)),_(e,n)},C=e=>{var n=Fi(),r=V(n),i=V(r,!0);q(r);var a=H(r,2);F(a,21,()=>d(L(t).key),Xe,(e,n,r)=>{var i=Pi();U(i),O(()=>{J(i,`min`,L(t).min),J(i,`max`,L(t).max),J(i,`step`,L(t).step??`any`),Oe(i,L(n))}),R(`change`,i,e=>g(L(t),r,e.currentTarget.value)),_(e,i)}),q(a),q(n),O(()=>{J(n,`title`,L(t).description),P(i,L(t).label)}),_(e,n)},T=e=>{var n=Li(),r=V(n),i=V(r,!0);q(r);var a=H(r,2);F(a,21,()=>L(t).options,e=>e.value,(e,t)=>{var n=Ii(),r=V(n,!0);q(n);var i={};O(()=>{P(r,L(t).label),i!==(i=L(t).value)&&(n.value=(n.__value=L(t).value)??``)}),_(e,n)}),q(a);var s;Tt(a),q(n),O(e=>{J(n,`title`,L(t).description),P(i,L(t).label),s!==(s=e)&&(a.value=(a.__value=e)??``,ue(a,e))},[()=>o(L(t).key)]),R(`change`,a,e=>v(L(t),e.currentTarget.value)),_(e,n)},E=e=>{var n=zi(),r=V(n),i=V(r,!0);q(r);var a=H(r,2);F(a,17,()=>L(t).items,e=>e.key,(e,n)=>{var r=Ri(),i=V(r);U(i);var a=H(i,2),o=V(a,!0);q(a),q(r),O(e=>{Ot(i,e),P(o,L(n).label)},[()=>s(L(n).key)]),R(`change`,i,e=>y(L(t),L(n).key,e.currentTarget.checked)),_(e,r)}),q(n),O(()=>{J(n,`title`,L(t).description),P(i,L(t).label)}),_(e,n)},D=e=>{var n=Bi(),r=V(n),i=V(r,!0);q(r);var a=H(r,2);F(a,21,()=>L(t).items,e=>e.item,(e,n)=>{var r=Ri(),i=V(r);U(i);var a=H(i,2),o=V(a,!0);q(a),q(r),O(e=>{Ot(i,e),P(o,L(n).label)},[()=>u(L(t).key)[L(n).item]===!0]),R(`change`,i,e=>b(L(t),L(n).item,e.currentTarget.checked)),_(e,r)}),q(a),q(n),O(()=>{J(n,`title`,L(t).description),P(i,L(t).label),l(a,`--columns: ${L(t).columns??2??``}`)}),_(e,n)},k=e=>{var n=Ui(),r=V(n),i=e=>{var n=Vi(),r=V(n,!0);q(n),O(()=>P(r,L(t).note)),_(e,n)};G(r,e=>{L(t).note!==void 0&&e(i)});var a=H(r,2);F(a,21,()=>L(t).items,e=>e.item,(e,n)=>{var r=Hi(),i=V(r),a=V(i,!0);q(i);var o=H(i,2);U(o),q(r),O(e=>{P(a,L(n).label),J(o,`min`,L(t).min),J(o,`max`,L(t).max),J(o,`step`,L(t).step),Oe(o,e)},[()=>c(L(t).key)[L(n).item]??L(t).min]),R(`change`,o,e=>x(L(t),L(n).item,e.currentTarget.value)),_(e,r)}),q(a),q(n),O(()=>{J(n,`title`,L(t).description),l(a,`--columns: ${L(t).columns??4??``}`)}),_(e,n)};G(r,e=>{L(t).kind===`slider`?e(i):L(t).kind===`number`?e(h,1):L(t).kind===`text`?e(S,2):L(t).kind===`vector`?e(C,3):L(t).kind===`select`?e(T,4):L(t).kind===`flags`?e(E,5):L(t).kind===`flagGrid`?e(D,6):L(t).kind===`numberGrid`&&e(k,7)}),_(e,n)}),q(C),O(()=>J(C,`hidden`,r()&&n()!==L(i).title)),_(e,C)}),_(e,C),We()}o([`click`,`input`,`change`]);var qi=1600;function Ji(e){let t=`idle`,n,r=n=>{t=n,e(n)};return{state:()=>t,async copy(e,i){try{await navigator.clipboard.writeText(e),r(`done`)}catch{i(),r(`manual`)}return n!==void 0&&clearTimeout(n),n=setTimeout(()=>r(`idle`),qi),t}}}function Yi(e,t,n=`select it below ↓`){return e===`done`?`copied!`:e===`manual`?n:t}var Xi=Object.assign({"../graphs/panels/basics-attribute-from-noise.json":()=>X(()=>import(`./basics-attribute-from-noise-D6cMjQtF.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/basics-attribute-remap.json":()=>X(()=>import(`./basics-attribute-remap-DkJ1bmdZ.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/basics-compose-primitives.json":()=>X(()=>import(`./basics-compose-primitives-BMSGHlyJ.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/basics-connect-by-reach.json":()=>X(()=>import(`./basics-connect-by-reach-MMlp0sfH.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/basics-even-spacing.json":()=>X(()=>import(`./basics-even-spacing-Go_gYIpM.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/basics-extrude-polygon.json":()=>X(()=>import(`./basics-extrude-polygon-DRSOvMZG.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/basics-field-params.json":()=>X(()=>import(`./basics-field-params-9EuL_9So.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/basics-filter-by-attribute.json":()=>X(()=>import(`./basics-filter-by-attribute-DIqyD4Z7.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/basics-filter-by-density.json":()=>X(()=>import(`./basics-filter-by-density-n6IAhiYs.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/basics-filter-by-expression.json":()=>X(()=>import(`./basics-filter-by-expression-D-fr9VEZ.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/basics-filter-primitives-by-attribute.json":()=>X(()=>import(`./basics-filter-primitives-by-attribute-DQTvcPfR.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/basics-foreach-per-group.json":()=>X(()=>import(`./basics-foreach-per-group-B2ypTHco.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/basics-gather-on-path.json":()=>X(()=>import(`./basics-gather-on-path-BdkDHSa8.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/basics-inline-field-params.json":()=>X(()=>import(`./basics-inline-field-params-DGeGG-PI.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/basics-jitter-points.json":()=>X(()=>import(`./basics-jitter-points-Dc69TIle.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/basics-merge-points.json":()=>X(()=>import(`./basics-merge-points-BpGlrWHR.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/basics-mesh-primitive.json":()=>X(()=>import(`./basics-mesh-primitive-BqSs4SiE.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/basics-neighborhood-count.json":()=>X(()=>import(`./basics-neighborhood-count-BLoBHgZ8.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/basics-neighborhood-varying-radius.json":()=>X(()=>import(`./basics-neighborhood-varying-radius-CBlT_T2s.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/basics-orient-along-vector.json":()=>X(()=>import(`./basics-orient-along-vector-DE0CifsK.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/basics-partition-by-attribute.json":()=>X(()=>import(`./basics-partition-by-attribute-3OaTD5Ib.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/basics-paths-by-group.json":()=>X(()=>import(`./basics-paths-by-group-Cj3eYKyT.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/basics-point-grid.json":()=>X(()=>import(`./basics-point-grid-DveL4za3.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/basics-promote-attribute.json":()=>X(()=>import(`./basics-promote-attribute-CPTqAS6f.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/basics-reseed-a-noise.json":()=>X(()=>import(`./basics-reseed-a-noise-3tPQk9eF.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/basics-scatter-in-bounds.json":()=>X(()=>import(`./basics-scatter-in-bounds-BZmhJHRr.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/basics-scatter-in-world.json":()=>X(()=>import(`./basics-scatter-in-world-BaHEodMY.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/basics-spawn-by-species.json":()=>X(()=>import(`./basics-spawn-by-species-Cah4E6aT.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/basics-spawn-instances.json":()=>X(()=>import(`./basics-spawn-instances-hratXIhN.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/basics-surface-sample.json":()=>X(()=>import(`./basics-surface-sample-DySW3Sd0.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/basics-sweep-profile.json":()=>X(()=>import(`./basics-sweep-profile-oS0pYHhK.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/basics-transfer-attribute.json":()=>X(()=>import(`./basics-transfer-attribute-DaAuF4L-.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/basics-transform-points.json":()=>X(()=>import(`./basics-transform-points-BQtfDVF6.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/basics-volume-scatter.json":()=>X(()=>import(`./basics-volume-scatter-CaKTpCCA.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/examples-forest.json":()=>X(()=>import(`./examples-forest-CYWCN4bX.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/examples-gpu-fields.json":()=>X(()=>import(`./examples-gpu-fields-DDcBZw_D.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/examples-rig.json":()=>X(()=>import(`./examples-rig-CC2fyv9N.js`).then(e=>e.default),[],import.meta.url),"../graphs/panels/examples-streamed-terrain.json":()=>X(()=>import(`./examples-streamed-terrain-ha4fILVA.js`).then(e=>e.default),[],import.meta.url)});async function Zi(e){let t=Xi[`../graphs/panels/${e}.json`];if(t===void 0)return;let n=JSON.parse(await t());if(typeof n!=`object`||!n||!Array.isArray(n.sections))throw Error(`panel spec "${e}": expected an object with a "sections" array`);return n}var Qi=`$graph`;function $i(e,t){return Array.isArray(e)&&Array.isArray(t)?e.length===t.length&&e.every((e,n)=>Object.is(e,t[n])):Object.is(e,t)}function ea(e,t,n){let r={};n!==void 0&&n.current!==n.loaded&&(r.seed=n.current);for(let[n,i]of Object.entries(e))$i(i,t[n])||(r[n]=i);return r}function ta(e,t,n){if(e.type===`i32`||e.type===`u32`)return 1;let r=Math.abs(n-t);if(!(r>0))return .01;let i=r/200,a=10**Math.floor(Math.log10(i));for(let e of[1,2,5])if(i<=e*a)return e*a;return 10*a}function na(e,t){let n=e.key,r=t?.label??(e.scope===`graph`||e.fieldParam===void 0?e.name:`${e.name}.${e.fieldParam}`),i=e.schema,a=t?.min??i.min,o=t?.max??i.max,s=t?.description??i.description,c=s===void 0?{}:{description:s};switch(i.type){case`f32`:case`i32`:case`u32`:{if(a!==void 0&&o!==void 0)return{kind:`slider`,key:n,label:r,...c,min:a,max:o,step:t?.step??ta(i,a,o),...t?.unit===void 0?{}:{unit:t.unit}};let e=t?.step??(i.type===`f32`?void 0:1);return{kind:`number`,key:n,label:r,...c,...a===void 0?{}:{min:a},...o===void 0?{}:{max:o},...e===void 0?{}:{step:e},...t?.unit===void 0?{}:{unit:t.unit}}}case`bool`:return{kind:`flags`,label:r,...c,items:[{key:n,label:``}]};case`enum`:return{kind:`select`,key:n,label:r,...c,options:(i.enum??[]).map(e=>({value:e,label:e}))};case`string`:return{kind:`text`,key:n,label:r,...c};case`vec3`:case`vec4`:return{kind:`vector`,key:n,label:r,...c,...a===void 0?{}:{min:a},...o===void 0?{}:{max:o},...t?.step===void 0?{}:{step:t.step}};default:return}}function ra(e){return e.scope===`graph`?{scope:`graph`,name:e.name}:{scope:`node`,node:e.node,name:e.name,...e.fieldParam===void 0?{}:{fieldParam:e.fieldParam},...e.drivenBy===void 0?{}:{drivenBy:e.drivenBy}}}function ia(e){return new Map(e.map(e=>[e.key,ra(e)]))}function aa(e){let t={};for(let n of e){if(n.isField)continue;let e=oa(n);e!==void 0&&(t[n.key]=e)}return t}function oa(e){let t=e.value;if(typeof t==`number`||typeof t==`string`||typeof t==`boolean`)return t;if(Array.isArray(t)&&t.every(e=>typeof e==`number`))return[...t]}function sa(e,t){let n={},r=[],i=[],a={},o=new Map(e.map(e=>[e.key,e])),s=e=>{if(e.isField)return r.push({key:e.key,reason:`holds a field — edit it in the node inspector`}),!1;let t=oa(e);return t===void 0?(r.push({key:e.key,reason:`no widget for ${e.schema.type}`}),!1):(n[e.key]=t,!0)};if(t!==void 0){let e=[];for(let c of t.sections){let t=[];for(let e of c.controls){let c=o.get(e.param);if(c===void 0){i.push(e.param);continue}if(!s(c))continue;let l=na(c,e);if(l===void 0){r.push({key:c.key,reason:`no widget for ${c.schema.type}`}),delete n[c.key];continue}let u=ca(e,c,o,r,i);u.length>0&&(a[c.key]=u),t.push(l)}t.length>0&&e.push({title:c.title,controls:t})}return{sections:e,values:n,mirrors:a,skipped:r,unknown:i,authored:!0}}let c=new Map,l=new Map;for(let t of e){if(!t.exposed||!s(t))continue;let e=na(t);if(e===void 0){r.push({key:t.key,reason:`no widget for ${t.schema.type}`}),delete n[t.key];continue}let i=t.scope===`graph`?Qi:t.node,a=c.get(i);a||(c.set(i,a=[]),l.set(i,t.scope===`graph`?Qi:t.nodeLabel===``?t.node:`${t.node} · ${t.nodeLabel}`)),a.push(e)}return{sections:[...c.entries()].sort((e,t)=>e[0]===Qi?-1:+(t[0]===Qi)).map(([e,t])=>({title:l.get(e)??e,controls:t})),values:n,mirrors:a,skipped:r,unknown:i,authored:!1}}function ca(e,t,n,r,i){let a=[];for(let o of e.also??[]){let e=n.get(o);if(e===void 0){i.push(o);continue}if(e.isField){r.push({key:o,reason:`mirrored by ${t.key}, but holds a field`});continue}if(e.schema.type!==t.schema.type){r.push({key:o,reason:`mirrored by ${t.key}, which is ${t.schema.type}, not ${e.schema.type}`});continue}a.push(o)}return a}var la=k(`<p class="note svelte-18k9idv">This graph exposes no knobs. Params live on its nodes — select one on the canvas to edit it.</p>`),ua=k(`<p class="note svelte-18k9idv"><!> Every edit is a <code class="svelte-18k9idv">setParam</code> on the node that holds it, then a recook.</p> <!>`,1),da=k(`<input class="url svelte-18k9idv" type="text" readonly=""/> <pre class="patch svelte-18k9idv"> </pre> <p class="hint svelte-18k9idv">The link reopens this graph with these values. Everything else comes from the graph itself.</p>`,1),fa=k(`<div class="share svelte-18k9idv"><button class="svelte-18k9idv"> </button> <button class="svelte-18k9idv">reset</button> <button class="link svelte-18k9idv"> </button></div> <!>`,1),pa=k(`<p class="warn svelte-18k9idv"> </p>`),ma=k(`<div class="svelte-18k9idv"><b class="svelte-18k9idv"> </b> </div>`),ha=k(`<details class="skipped svelte-18k9idv"><summary class="svelte-18k9idv"> </summary> <!></details>`),ga=k(`<div class="overview"><h2 class="graph-title svelte-18k9idv"> </h2> <!> <!> <!> <!></div>`);function _a(e,n){we(n,!0);let r=K(()=>(n.rev,n.controller.knobs())),i=K(()=>sa(L(r),n.spec)),a=K(()=>ia(L(r))),o=W(oe({})),s={};lt(()=>{let e=L(i).values;b(()=>{c(L(o),e)?s=Si(L(o),e,s):(t(o,e,!0),s=xi(e))})});let c=(e,t)=>{let n=Object.keys(e);return n.length===Object.keys(t).length&&n.every(e=>e in t)},l=W(``);lt(()=>{let e=L(i).sections.map(e=>e.title);e.includes(L(l))||t(l,e[0]??``,!0)});function u(e){bi(L(o),e)}function d(e){bi(L(o),e);let t=String(e.key);if(L(a).get(t)!==void 0){for(let r of[t,...L(i).mirrors[t]??[]]){let t=L(a).get(r);t!==void 0&&n.controller.setKnob(t,e.value)}n.onEdit()}}let f=K(()=>ea(aa(L(r)),n.baseline,{current:n.seed,loaded:n.loadedSeed})),p=K(()=>JSON.stringify(L(f),null,2)),m=K(()=>Object.keys(L(f)).length),h=W(!1),g=W(`idle`),v=Ji(e=>t(g,e,!0)),y=K(()=>Yi(L(g),`copy link`)),x=()=>v.copy(n.shareUrl(L(f)),()=>t(h,!0)),C=e=>{e.currentTarget?.select()};var w=ga(),T=V(w),E=V(T,!0);q(T);var D=H(T,2),k=e=>{var t=la();_(e,t)},A=e=>{var n=ua(),r=B(n),a=V(r),s=e=>{var t=At(`Curated for this graph.`);_(e,t)},c=e=>{var t=At(`Derived from the graph's exposed params, grouped by the node that exposes each one.`);_(e,t)};G(a,e=>{L(i).authored?e(s):e(c,-1)}),it(3),q(r);var f=H(r,2);{let e=K(()=>L(i).sections.length>1);Ki(f,{get sections(){return L(i).sections},get values(){return L(o)},onInput:u,onCommit:d,get tabbed(){return L(e)},get tab(){return L(l)},set tab(e){t(l,e,!0)}})}_(e,n)};G(D,e=>{L(i).sections.length===0?e(k):e(A,-1)});var j=H(D,2),M=e=>{var r=fa(),i=B(r),a=V(i),o=V(a,!0);q(a);var s=H(a,2),c=H(s,2),l=V(c);q(c),q(i);var u=H(i,2),d=e=>{var t=da(),r=B(t);U(r);var i=H(r,2),a=V(i,!0);q(i),it(2),O(e=>{Oe(r,e),P(a,L(p))},[()=>n.shareUrl(L(f))]),S(`focus`,r,C),_(e,t)};G(u,e=>{L(h)&&e(d)}),O(()=>{P(o,L(y)),s.disabled=L(m)===0,J(c,`aria-expanded`,L(h)),P(l,`${L(m)??``} changed`)}),R(`click`,a,x),R(`click`,s,function(...e){n.onReset?.apply(this,e)}),R(`click`,c,()=>t(h,!L(h))),_(e,r)};G(j,e=>{L(i).sections.length>0&&e(M)});var N=H(j,2),ee=e=>{var t=pa(),n=V(t);q(t),O(e=>P(n,`panel spec names ${L(i).unknown.length??``} param(s) this graph does not expose: ${e??``}`),[()=>L(i).unknown.join(`, `)]),_(e,t)};G(N,e=>{L(i).unknown.length>0&&e(ee)});var te=H(N,2),ne=e=>{var t=ha(),n=V(t),r=V(n);q(n);var a=H(n,2);F(a,17,()=>L(i).skipped,e=>e.key,(e,t)=>{var n=ma(),r=V(n),i=V(r,!0);q(r);var a=H(r);q(n),O(()=>{P(i,L(t).key),P(a,` — ${L(t).reason??``}`)}),_(e,n)}),q(t),O(()=>P(r,`${L(i).skipped.length??``} not shown`)),_(e,t)};G(te,e=>{L(i).skipped.length>0&&e(ne)}),q(w),O(()=>P(E,n.title)),_(e,w),We()}o([`click`]);var va=I(`<svg class="gicon svelte-1khlusp" aria-hidden="true"><path></path></svg>`),ya=k(`<button> </button>`),ba=k(`<div class="group svelte-1khlusp"><!> </div> <!>`,1),xa=k(`<div class="empty svelte-1khlusp"> </div>`),Sa=k(`<div class="menu svelte-1khlusp" role="dialog" aria-label="add a node" tabindex="-1"><input class="search svelte-1khlusp" type="search" placeholder="search nodes…"/> <div class="list svelte-1khlusp"><!> <!></div></div>`);function Ca(e,n){we(n,!0);let r=W(``),i=W(0),a=W(void 0),o=W(void 0);function s(e,t){let n=e.type.toLowerCase();return n.startsWith(t)?0:n.includes(t)?1:e.description.toLowerCase().includes(t)?2:-1}let c=K(()=>{let e=L(r).trim().toLowerCase();if(e===``)return n.groups;let t=n.groups.flatMap(t=>t.entries.map(n=>({group:t.name,entry:n,score:s(n,e)}))).filter(e=>e.score>=0).sort((e,t)=>e.score-t.score),i=new Map;for(let e of t){let t=i.get(e.group);t?t.push(e.entry):i.set(e.group,[e.entry])}return[...i.entries()].map(([e,t])=>({name:e,entries:t}))}),u=K(()=>L(c).flatMap(e=>e.entries));lt(()=>{n.at!==null&&(t(r,``),t(i,0),_e().then(()=>L(a)?.focus()))}),lt(()=>{L(i)>=L(u).length&&t(i,Math.max(0,L(u).length-1),!0)});function d(e){n.onAdd(e),n.onDismiss()}function f(e){if(e.key===`Escape`){e.preventDefault(),n.onDismiss();return}if(e.key===`ArrowDown`||e.key===`ArrowUp`){if(e.preventDefault(),L(u).length===0)return;let n=e.key===`ArrowDown`?1:-1;t(i,(L(i)+n+L(u).length)%L(u).length),_e().then(()=>{L(o)?.querySelector(`.entry.on`)?.scrollIntoView({block:`nearest`})});return}if(e.key===`Enter`){e.preventDefault();let t=L(u)[L(i)];t&&d(t.type)}}function p(e){n.at!==null&&L(o)!==void 0&&(L(o).contains(e.target)||n.onDismiss())}var m=w();S(`pointerdown`,T,p);var h=B(m),g=e=>{var s=Sa(),p=V(s);U(p),He(p,e=>t(a,e),()=>L(a));var m=H(p,2),h=V(m);F(h,17,()=>L(c),e=>e.name,(e,t)=>{var n=ba(),r=B(n),a=V(r),o=e=>{var n=va(),r=V(n);q(n),O(()=>{J(n,`viewBox`,Et),J(r,`d`,yt[L(t).name])}),_(e,n)};G(a,e=>{yt[L(t).name]!==void 0&&e(o)});var s=H(a);q(r);var c=H(r,2);F(c,17,()=>L(t).entries,e=>e.type,(e,t)=>{var n=ya();let r;var a=V(n,!0);q(n),O(()=>{r=Y(n,1,`entry svelte-1khlusp`,null,r,{on:L(u)[L(i)]?.type===L(t).type}),J(n,`title`,L(t).description),P(a,L(t).type)}),R(`click`,n,()=>d(L(t).type)),_(e,n)}),O(()=>P(s,` ${L(t).name??``}`)),_(e,n)});var g=H(h,2),v=e=>{var t=xa(),n=V(t);q(t),O(()=>P(n,`no node type matches "${L(r)??``}"`)),_(e,t)};G(g,e=>{L(c).length===0&&e(v)}),q(m),q(s),He(s,e=>t(o,e),()=>L(o)),O((e,t)=>l(s,`left: ${e??``}px; top: ${t??``}px`),[()=>Math.min(n.at.x,Math.max(8,window.innerWidth-260)),()=>Math.min(n.at.y,Math.max(8,window.innerHeight-340))]),R(`keydown`,s,f),Re(p,()=>L(r),e=>t(r,e)),_(e,s)};G(h,e=>{n.at!==null&&e(g)}),_(e,m),We()}o([`keydown`,`click`]);var wa={title:`write an attribute from a noise field`,description:'A field-capable param takes a field expression instead of a constant: `setAttribute`\'s `value` here is four octaves of Perlin fBm, resolved once per point and stored into a new `height` attribute. `normalized: true` maps the noise\'s own raw range onto [0, 1], so no remap wrapper is needed. Noise carries its own `seed` inside the spec, so a literal there is a number the graph seed cannot reach; what makes this one answer the seed box is `"seed": { "from": "node", "variant": 0 }`, which derives the noise\'s seed from the cooking node\'s own and which `basics-reseed-a-noise` explains in full.',tags:[`basics`,`fields`,`noise`,`attributes`]},Ta={title:`rescale an attribute to a new range`,description:"`attributeRemap` in mode 'fit' measures an attribute's actual minimum and maximum over the domain and stretches them onto [outMin, outMax], which is how a quantity of unknown scale — a raw noise value, a neighbour count, an invented score — becomes something a density or a colour can consume. `outName` writes the result beside the original instead of over it, so both columns survive for inspection.",tags:[`basics`,`attributes`,`remap`]},Ea={title:`compose several primitives into a scatter`,description:"Four primitives and one terminal node build a complete placement pass: scatter with a guaranteed spacing, cut it to noise-defined regions, turn every point a random way, give every point one uniform random size, then spawn. Each step is a name from the catalog rather than a hand-built cluster of nodes, which is what keeps the graph readable and its behaviour documented. Note what varies: the scatter and the two write steps differ per instance, while the noise mask does not — two masks with the same params cut identically unless their `variant` differs.",tags:[`basics`,`primitives`,`composition`,`spawn`]},Da={title:`connect a cloud by each point's own reach`,description:"`connectPoints`' `radius` as a FIELD: every point carries its own `reach`, written here as 1.8 plus the SQUARE of three octaves of Perlin fBm, so the reach spans 1.8 to 8.2 against a median nearest-neighbour distance of 1.7, and the network that comes out is a web of mean degree six with starbursts where the reach spikes — the busiest point carries twenty-five edges, and exactly one point of the 220 reaches past 8 at all. Squaring the noise is what makes the pair rule VISIBLE rather than merely true: the long spokes exist only because the big end asked for them, and the small point at the far end of one could never have reached back. THE RULE OF THE PAIR IS max(rA, rB): a pair becomes an edge when it is closer than the LARGER of the two reaches, which is what keeps the relation symmetric. Neither alternative agrees with the same number passed plainly — the SMALLER would let a big point be crowded by a small one, and the SUM would double the spacing of an evenly-sized cloud — and without a stated rule 'A is near B' and 'B is near A' become two different tests, so an edge would depend on which endpoint asked. It is the same rule `selfPrune.minDistance` has always used. `color` is written from the same expression as a BRIGHTNESS rather than a hue, purely so the picture shows the reach its edges were drawn from: the viewer reads a point-domain `color` onto the line vertices and multiplies it into its own material tint, so a monochrome ramp survives that multiply where a hue would be swallowed by it. Per-point `scale` would say nothing here — it is read for instance transforms, never for a bare cloud or a network. TWO COSTS travel with a field here and neither is a correctness risk. The candidate scan runs at the WIDEST reach in the cloud, since either endpoint may be the larger, so the edge ceiling is measured on candidates rather than on the edges that survive. And under a partitioned cook the halo is no longer `radius` but the GLOBAL MAXIMUM the field can return ANYWHERE in the world — a bound to be DERIVED and not measured, because the cloud a cell sees has already been clipped by the halo being sized. Derive it from the expression: a `clamp` states the bound outright — `1.8 + 7 * u^2` with `u` clamped to [0, 1] maxes at 8.8 and cannot exceed it, which is why the remap here ends in one. Without the clamp the bound would have to come from the noise's own documented range instead, and a normalized fBm only actually spans about two fifths of its nominal [0, 1] — derive the halo from the nominal range and it is safe but loose; measure it from a cook and it is tight and wrong. The derived bound is the number to widen a cell by. Underestimating does not throw; it drops the long edges at the seams only. A non-finite reach is REFUSED here, naming the offending element, where `pointNeighborhood`'s radius reads NaN and Infinity as documented values — the distinction is which mistake is likelier, and a reach is arithmetic an author writes, so a NaN there is a broken expression rather than a request for no edges. `degreeAttr` is what makes the hubs readable downstream: filter on it to find the dead ends, or promote it to size a junction.",tags:[`basics`,`topology`,`fields`,`network`,`halo`]},Oa={title:`give every copy the attributes of the point it landed on`,description:"`copyToPoints` stamps the whole `source` cloud onto every `target` point and composes the transforms per copy — P, rot and scale fold the target's frame into the source's, and each copied seed is hashCombine(sourceSeed, targetSeed) so the copies of one clump stay distinguishable. What the copies do NOT get by default is any idea of WHICH target they landed on, which makes them identical in everything but placement. `targetNames` is the fix: each named target point attribute arrives as a column on the copies, with the target's type, tuple size and default, and every copy in a target's block holds that target's value. Here the clump of nine props is a bare `pointGrid` around the origin, and the two things that vary between clumps — `species` and `vigour` — are computed once per plot and carried. `species` is the case nothing else reaches: it is a STRING, so `spawnInstances`' `assetAttr` can split the copies into one batch per asset id, and no amount of transform composition can decide an asset id. `vigour` is the case that shows why a carried value beats a composed one — the `scale` written after the copy multiplies the plot's vigour by a per-copy `randomField`, so a clump's props agree on how well the plot is doing and still differ from each other. The transform attributes are refused by name rather than resolved silently: carrying the target's `P` would put all nine props on top of the plot and the cook would stay clean, so `targetNames` rejects P, rot, scale and seed, and rejects a name the source already carries instead of letting statement order decide the winner.",tags:[`basics`,`copy`,`instancing`,`attributes`]},ka={title:`a mask, a decay and a compression`,description:"THREE CURVES, THREE DIFFERENT JOBS — not three ways of doing one, which is what makes this the companion to `basics-field-shaping` rather than a repeat of it. Each panel reads the same input, the distance from its own centre, and each answers a question the other two answer badly. LEFT, `smoothstep(2, 8, d)` inverted: a MASK with stated ends. It is flat at 1 out to d = 2, flat at 0 beyond d = 8, and smooth in between — and the flatness at BOTH ends is the whole point, because that is what a linear falloff cannot give you. A `lerp` mask creases visibly where it starts and stops; this one does not, which is why it is the shape to reach for when a region has to fade out without announcing its own boundary. The expansion is emitted rather than the WGSL builtin, whose result is undefined when the edges cross, and a zero span is guarded into the step the curve approaches instead of a division by zero. `ramp` remains the choice when the knees belong anywhere other than the ends; `smoothstep` is the two-edge case worth a name. MIDDLE, `6 * exp(-0.45 * d)`: DECAY, and the one curve here that never reaches zero. Every 1.54 units it halves — that is `log(2) / 0.45`, which is the number to reason with rather than the exponent — so across this panel it is small everywhere and zero nowhere, and a threshold rather than the curve is what ends it. Push it far enough and f32 does end it: `exp` underflows to exactly 0 below about -103.9, which is the format's floor and not the curve's. This is the honest shape for anything physical that falls off: light, heat, density away from a source. RIGHT, `2.4 * log(1 + d)`: COMPRESSION, and the only one that rises. It grows without bound and ever more slowly, which is what turns a quantity spanning orders of magnitude into one a height or a colour can show. The `1 +` is insurance rather than a fix for something this cook hits: `log(0)` is -Infinity, and no grid point here lands exactly on the centre — the nearest sits 0.283 away — so the offset is what makes the panel safe to re-spacing rather than what rescues it now. For a base other than e, divide — `div(log(x), log(2))` — since the grammar carries the natural logarithm only. `exp` AND `log` ARE BUDGETED rather than exact on the GPU — with `distance`, which carries 1 ULP, they are the three of the seven additions that are: the device has its own transcendentals and the CPU has the host's, and they are simply not the same function. `smoothstep` is bit-exact, which was not a given for a five-operation interior — it holds only because the CPU rounds each of those operations to f32 in the order the kernel does. `distance` supplies the input to all three, and is exactly `length(sub(a, b))` by construction.",tags:[`basics`,`fields`,`remap`,`composition`]},Aa={title:`place an exact number of points along a path, bunched where a density says`,description:"Scattering in proportion to a density usually means rejection sampling: draw a candidate, keep it with probability density, and accept whatever count comes out. The count is then binomial — ask for ninety and get eighty-one this cook and ninety-six the next — which is fine for grass and useless for anything an author counts. `pathScan` buys the other trade: it writes the RUNNING TOTAL of a point attribute along each polyline in the path's own walk order, and a running total of a density is a cumulative distribution. Sample the inverse of that at ninety places and you get ninety points, every one of them placed in proportion to the density, with no draw to be unlucky in. This is the operation a field structurally cannot express at any length: a field resolves each element from that element alone, so 'how much density lies BEHIND me along this curve' has no formulation in the grammar — which is why it is a node.\n\nThe pieces, in the order they appear. `density` is any expression of `curveU`, here one hump per lap, and it is FLOORED at 0.02 rather than allowed to reach zero: across a dead stretch the distribution is flat, the inverse is ambiguous, and the nearest-point lookup below picks arbitrarily within it. `mode: \"exclusive\"` starts the first sample at zero — that is the mode that makes the first bucket reachable, since an inclusive scan gives the first sample its own whole value and nothing can land below it. `totalAttr` reports each path's whole total to the PRIMITIVE domain, `promoteAttribute` brings it back to the points, and dividing gives a cdf in [0, 1). Both ends matter and neither is more correct: exclusive is exact at the start, inclusive at the end.\n\nThe lookup is the part with no primitive behind it. Finding the sample whose cdf bucket contains a given u is a scalar-keyed search, and the library has no node for one, so the cdf is laid out AS GEOMETRY: each sample is re-embedded at (cdf, 0, 0) — `onCurve` saves its real position first — and `sampleNearestPoint` answers the question with a spatial query. Read the approximation honestly: nearest-in-cdf is not the containing bucket, it is the nearer of the two bucket edges, so a point can sit up to half a bucket off — a tenth of a percent of the lap at the 480 samples used here, and invisible. THE SAMPLE COUNT IS NOT FREE, though, and the rule is worth carrying: a sample's cdf bucket is as wide as its share of the total, so the widest one must stay NARROWER than the anchor spacing 1 / count, or two anchors fall in one bucket and land two points on the same spot. This graph at 240 samples did exactly that — peak bucket 0.0128 against a spacing of 0.0111, four coincident pairs out of ninety — and a graph teaching an exact count has no business emitting a doubled point. Halving the bucket fixed it. Denser density humps need more samples, and the check is arithmetic, not taste. The anchors themselves come from `pointLine` between [0,0,0] and [1,0,0] with `includeEnd` false, which is a stratified sample of the half-open range and needs no random number at all: point i sits at exactly i / count. A golden-ratio or uniform-random u substitutes here unchanged — the machinery downstream does not care where u came from.",tags:[`basics`,`path`,`density`,`sampling`,`scan`]},ja={title:`enforce a minimum distance between points`,description:"`selfPrune` scans points in index order and keeps one only when every already-kept point is at least `minDistance` away, which turns a clumpy uniform scatter into evenly spaced points for anything with physical extent. Over-scatter deliberately: the output count is emergent, capped by the area divided by minDistance squared, so raising `count` past that adds nothing and the real knob is `minDistance`.",tags:[`basics`,`filter`,`spacing`]},Ma={title:`turn a footprint into massing`,description:"A closed polyline is already a plan; `extrudePolygon` gives it a third dimension. The boundary is swept along a direction into three-vertex 'poly' triangles — walls, a floor and a roof — and until this node existed a graph that had computed its plots could only draw them as hairlines, which is why a settlement pipeline puts a pre-made house on a lot CENTRE while the lot's own shape goes unshown. `distance` is field-capable and resolves on the INPUT points, so a per-point value gives a SLOPED top rather than a flat one: the noise here lifts each corner of the plan by a different amount. Walls, roof and floor keep SEPARATE points, so the eaves stay a crease instead of being shaded round, and the winding is derived from the polygon's own Newell normal against the direction — a footprint wound either way comes out facing outward. Caps are fan-triangulated from the boundary's first point, which is exact for a CONVEX plan and is all the topology records. An OPEN polyline is refused by name: extrusion is not defined on one, and the fix is `pointsToPath` with `closed: true`.",tags:[`basics`,`surface`,`mesh`,`extrude`,`fields`]},Na={title:`read a field's shaping numbers from a knob`,description:'A field expression can read a named value instead of baking one: `{ "fn": "param", "name": "amplitude" }` inside the body\'s `translate` spec takes whatever the wrapping node\'s `amplitude` knob holds, so the two numbers that shape this surface are knobs rather than literals a caller would have to edit the graph to move. Every exposed param binds its name into its body\'s field scope, which is why both declarations here list no `targets` at all — neither writes into an inner param slot, so their type comes from the shape of `default` (a number is f32, a 3-number array vec3, a 4-number array vec4). The value is SUBSTITUTED before the field is built, so what cooks is exactly the field the literal would have built, cache key included: turning the knob invalidates precisely what editing the number would have. `frequency` multiplies the sample position rather than sitting in `opts.frequency`, because that option is read as a plain number and cannot hold a spec — folding the scale into the position is the same move every noise-bearing primitive makes.',tags:[`basics`,`fields`,`subgraph`,`params`]},Pa={title:`shape one falloff six ways`,description:"SIX SHAPING FUNCTIONS ON ONE INPUT. Every panel is the same grid and the same scalar `t` — 1 at the panel's centre, falling to 0 at its rim, written `1 - clamp(distance(P, centre) / 6, 0, 1)` — lifted by `6 * f(t)` and differing only in `f`. The six sit in two rows of three, named by coordinate rather than by which one faces you, since that depends on where the camera is: at z = -10, in increasing x, `t` itself (a cone, the control), `sqrt(t)` (a dome, steep at the rim) and `t * t` (a rounded spire); at z = +10, `pow(t, 3)` (a sharper spire), `ramp(t)` through four stops (the S-curve, flat at both ends) and `step(0.5, t)` (a flat-topped mesa — a hard cut, not a curve, and the only panel here that leaves the plane it started on). Read across and the choice of `f` is the whole difference between a cone and a mesa, which is what makes a falloff an authored decision rather than whatever the arithmetic happened to give. THREE RULES ARE VISIBLE HERE RATHER THAN STATED. `step` takes its EDGE FIRST — `step(edge, x)` is exactly `ge(x, edge)` with the operands swapped, and it exists to buy the name a shader author reaches for; getting the order backwards gives the complement, silently. `pow` HAS A NARROWER DOMAIN than a host-language power: every negative base is NaN, as are `pow(0, 0)` and `pow(x, 0)` for a zero, negative, infinite or NaN `x`, because the measured device implements it as `exp2(b * log2(a))` exactly and the CPU adopts that domain rather than letting the two paths disagree over a whole quadrant. The `clamp` inside `t` is therefore LOAD-BEARING — and in TWO panels rather than one, because `sqrt` answers a negative on exactly the same rule. Strip the clamp and this graph fails at the `sqrt` panel before it ever reaches `pow`: `transformPoints: param \"translate\" resolved to NaN at element 0`, 254 of that panel's 961 points non-finite. Nor is it decorative in the other four. Only `ramp` and `step` clamp by construction; `t` and `t * t` just keep going, the cone dipping 2.5 units BELOW its own plane at a corner and the square lifting a raised rim. The corners are the whole of it either way — a 12-unit panel around a radius-6 disc leaves nothing else outside. And `pow` carries the widest GPU parity budget of the grammar's ALGEBRAIC fns — 8 ULP, against bit-exact for `mul` and 1 for `sqrt`, with only the trigonometric family wider — which is why the square is spelled `t * t` rather than `pow(t, 2)`: `mul` for a square, `sqrt` for a root, `ramp` for a falloff, and `pow` only when the exponent is genuinely arbitrary. `ramp` and `smoothstep` divide the smooth-edge job between them: `smoothstep` when the knees belong at the two ends, which is the common case and now has its own fn, and `ramp` when they belong anywhere else, because a ramp says where they are instead of hiding them in a cubic. `basics-curve-shaping` puts `smoothstep` beside `exp` and `log` for that comparison. ONE MORE THING THIS FILE SHOWS BY BEING LONG: the `t` subexpression is written out SEVEN times across six panels — the square panel spells it twice, once per factor of `t * t` — because nothing yet lets one expression bind a name to a subexpression it uses more than once. Each of those seven used to be `length(sub(P, centre))` and is now `distance`, which is the same number by construction and thirty-five fewer lines of file; the repetition it does NOT fix is the point of this paragraph. That is the A3 entry in PLAN.md, and this graph is now its second worked case.",tags:[`basics`,`fields`,`remap`,`composition`]},Fa={title:`keep points by an attribute comparison`,description:"The first of the three ways to remove points: write a scalar column, then compare it. `filterByAttribute` tests one named point attribute against `value` with one of eq/ne/lt/le/gt/ge and keeps the survivors with every attribute carried. The scratch column stays on the output — `removeAttribute` is what takes it off again — which is the cost this idiom pays and `filterByExpression` avoids.",tags:[`basics`,`filter`,`attributes`]},Ia={title:`thin a cloud by the density attribute`,description:"The standard thinning idiom: write the standard `density` attribute from a 0..1 noise field, then let `filterByDensity` in mode 'probabilistic' keep each point with probability equal to its own density. The result is soft-edged — dense regions stay full, sparse ones fade out, with no visible boundary. Mode 'threshold' on the same input gives the hard-edged version instead.",tags:[`basics`,`filter`,`density`,`noise`]},La={title:`keep points with a predicate expression`,description:"`filterByExpression` decides per point from a field expression, so a test that would otherwise need a scratch attribute plus a comparison node becomes one node with no leftover column. The comparison functions emit 1 and 0, `mul` combines them as AND (and `max` as OR): this predicate keeps points inside a radius of 20 AND where a value-noise field rises above 0.4. NaN never passes, so a predicate that fails to compute drops the point.",tags:[`basics`,`filter`,`fields`,`predicate`]},Ra={title:`keep whole primitives by an attribute comparison`,description:"`filterByAttribute` one domain up. `connectPoints` writes each edge's length onto the PRIMITIVE domain as `edgeLength`, and `filterPrimitivesByAttribute` compares that column with the same six operators and keeps WHOLE PRIMITIVES: vertices, vertex and primitive columns, and the points they share all survive, so a network that goes in comes out a network. Every point filter would rebuild the point domain instead and the topology would go with it. The same column can be read after a sampler has flattened it onto points — that is how such graphs were written before this node existed — and the difference is that everything downstream then pays for the edges that were always going to be dropped.",tags:[`basics`,`filter`,`primitives`,`topology`]},za={title:`flatten a cloud onto a plane and keep the height it lost`,description:'`projectToPlane` drops every point orthogonally onto the plane through `origin` with normal `normal`, and with `keepOffset` it writes each point\'s SIGNED pre-projection distance into a `planeOffset` attribute before moving it. That pairing is the whole idiom: the geometry becomes a plan view, and the third dimension survives as data rather than being thrown away. Here a relief grid flattens to y = 0 and the height it had drives its colour, so the map still says where the hills were. The ramp is fitted to the relief that actually arrives (about ±1.8 units) rather than to the amplitude the transform asks for: a normalized fBm spans only the middle stretch of its nominal range, so a ramp cut to the nominal number leaves its ends unreachable and the map reads washed out. Signed, so the sign is the side — points below the plane come back negative, and a rule that wants only the high ground reads `planeOffset > 0` rather than needing a second node to tell it which way is up.\n\nThe params are field-capable and that changes what the node is. As plain vectors they describe ONE plane and the normal must be non-zero. As FIELDS they are read per point, so each point falls onto the plane IT was given: a per-point `origin` with a constant normal is an OFFSET along that normal, which is how a stepped or terraced flattening is written; a per-point normal — `attribute("N")` — puts every point onto its own surface plane instead of onto one shared one. One safety worth knowing before relying on it: where a per-point normal resolves to zero there is no plane to project onto, so that point is left exactly where it stands rather than being collapsed to the origin.',tags:[`basics`,`project`,`plane`,`attributes`]},Ba={title:`treat each group on its own`,description:"`partitionByAttribute` splits the cloud into one geometry per district, and `forEach` cooks its inner graph once per group instead of once — so each district shakes loose on its own seed rather than all four sharing one. Exactly one exposed input must be named `each` (one iteration per item) or `eachPoint` (one per point); every other exposed input is broadcast whole to every iteration. Each iteration is seeded on its group's own CONTENT, never on where the group sat in the collection, so reordering the input reorders the output and re-rolls none of it. The `groups` output is the four separate results, still tagged `district=<value>`; `points` is the same four put back together with `mergePoints`, which is how you return to a single cloud.",tags:[`basics`,`foreach`,`partition`,`composite`]},Va={title:`gather evenly spaced points into clumps along a curve`,description:"`pathPointAt` answers the question the library could not: where is this curve at u = 0.37. Resampling steps a whole curve at even intervals, so anything needing one arbitrary parameter had to walk along the tangent and accept leaving the curve wherever it bent. This node moves each point to a parameter along ITS OWN polyline, keeping the points, their attributes and the topology — it slides points along the curve they already sit on, so the path stays the same path and only its parameterization changes. The parameter is field-capable and resolves BEFORE anything moves, which is what lets it read `curveU` and express a move relative to where the point already is. `transform/gather-on-path` is that idiom packaged: each point slides `amount` of the way toward the centre of its own bin, so an even distribution becomes clumps with bare runs between, and because nothing is removed the count is exactly what arrived. It needs `curveU` on its input, which pathResample, splineSample and the shape/path-* primitives write and a bare pointsToPath does not.",tags:[`basics`,`curve`,`path`,`spacing`]},Ha={title:`put a field's shaping numbers on knobs without a wrapper`,description:'The dunes of `basics-field-params` with the wrapper deleted. A `param` reference inside a field spec may carry its own value — `{ "fn": "param", "name": "amplitude", "value": 24 }` — so a plain `transformPoints` node holds both the expression and the numbers that shape it, where before a subgraph had to exist for the sole purpose of carrying them. The value is SUBSTITUTED before the field is built, exactly as a binding is, so what cooks is the field the literal would have built, cache key included. The key is optional and that is the whole of its safety: omit it and the reference is unbound and refuses to evaluate, with the same error as ever, so a default exists only where somebody wrote one. An outer binding still wins, so wrapping this node in a subgraph that exposes `amplitude` overrides the 24 without editing it. Two details are inherited rather than invented: `frequency` multiplies the sample position instead of sitting in `opts.frequency`, because that option is read as a plain number and cannot hold a spec; and the noise takes its seed from the node, `{ "from": "node", "variant": 0 }`, because a literal `opts.seed` is a number the graph seed cannot otherwise move. The grid is sized so the knobs are legible rather than merely wired: 20 units of ground at quarter-unit spacing, three octaves, and an amplitude that lands about 10 units of relief. A normalized fBm only spans about two fifths of its nominal range, so a wide grid under a modest amplitude reads as a flat field of dots and the graph fails to show its own effect; the ratio of relief to footprint is also what the viewer\'s framing reads to pick its elevation angle, so a flat cook is photographed from a flatter angle and hides itself twice.',tags:[`basics`,`fields`,`params`]},Ua={title:`break up a lattice with deterministic jitter`,description:"`jitterPoints` offsets each point by a random vector drawn per axis from (seed, point index, axis), so the result is reproducible and independent of cook order — the lattice stops reading as a lattice without giving up determinism. `amount` is the maximum offset per axis and is field-capable, so the jitter can itself vary across space; here y is left at 0 to keep the cloud flat.",tags:[`basics`,`jitter`,`determinism`]},Wa={title:`let a string attribute drive a field`,description:"`attributeIs` is the only way a field can read a STRING attribute: it resolves to 1 on the elements whose named string attribute equals the literal and 0 on every other one, so `species` — itself painted spatially here, because `setAttribute`'s string mode takes a field as the selector into `values` and this one is a noise — becomes an ordinary scalar field. It is a PREDICATE rather than an index on purpose. A string column stores positions in a per-geometry table that clone, filter and merge rebuild to first-encounter order, so the same value sits at different indices depending on what happened upstream, and in different cells of one partitioned world; the predicate resolves the index against the geometry in hand and never exposes it. The same fact makes a literal that is absent from the table read as all zeros instead of throwing — a cell holding no pines legitimately has no `pine`, so a misspelled literal reads as 'nothing matches'. Feeding the 0/1 column to `lerp` as the blend factor is what makes the point: both endpoints are continuous fields of `moisture` and both are evaluated for every point, and the mask chooses between them. It is a mask, not a branch — swap the `lerp` for a `mul` and the same column gates instead. The `remap` on the selector is only bookkeeping: Perlin's values bunch around the middle of its range, so a bare `moisture * 3` would put nearly nine points in ten in the middle band, and stretching the band the noise actually occupies across the three names is what makes the split roughly even. It needs no clamp of its own: `setAttribute` floors the selector and clamps it into range, and NaN picks entry 0, so no per-point value can miss.",tags:[`basics`,`fields`,`attributes`,`strings`]},Ga={title:`concatenate two clouds into one`,description:"`mergePoints` has a multi input: every connected geometry is concatenated in connection order into a single point cloud. The output carries the union of all point attributes — one missing on an input fills with its default over that input's range — and attributes sharing a name must agree on type and tuple size, so a scratch column left on one side can break a merge that used to work. Topology is not carried: the result is points only.",tags:[`basics`,`merge`,`compose`]},Ka={title:`join an authored path to a generated network`,description:"`mergePrimitives` is `mergePoints` with the topology kept: points, vertices AND primitives are concatenated, and each input's vertex and primitive references are renumbered onto its place in the result — so an authored boundary path and a generated trail network come out one geometry that is still a network. Send the same two inputs through `mergePoints` instead and both survive as loose points with every primitive gone, which is what blocked mixing authored geometry with generated geometry at all. Each domain carries the union of its attributes, an input missing one filling with that column's default over its own range. `primtype` is the exception, because it is a type tag rather than a value: each input's primitives keep their own tag, and primitives from an input carrying no tag come out with an empty one instead of inheriting another input's. Mixed primitive kinds in one geometry are fine — every consumer selects what it understands, so a mesh unioned with a network is coherent.",tags:[`basics`,`merge`,`topology`,`compose`]},qa={title:`build a mesh a saved graph can cook`,description:"`meshPrimitive` is the only mesh source that survives serialization — `dataInput`'s items are injected at runtime and a saved graph carries none — so a graph that must cook from JSON alone gets its surface from here. The output carries P and a `uv` point attribute, plus one three-vertex 'poly' primitive per triangle: exactly the topology `surfaceSample`, `promoteAttribute`, and the 'uv' and 'raycast' transfer mappings need.",tags:[`basics`,`mesh`,`source`,`serialization`]},Ja={title:`measure how crowded each point is`,description:"`pointNeighborhood` writes how many other points lie within `radius` into a u32 attribute, using a uniform spatial grid so it stays fast well beyond a few thousand points. The count is a measured quantity rather than an authored one, which is what a later filter, colour or scale can react to. A point with no neighbours gets 0 and keeps its own value as the neighbour average, so a displacement built from that average is zero rather than undefined.",tags:[`basics`,`attributes`,`neighborhood`,`measure`]},Ya={title:`measure a neighbourhood at each point's own scale`,description:"`pointNeighborhood`'s `radius` as a FIELD. The cloud is UNIFORM — a plain scatter, the same density everywhere — and `reach` rises left to right from 1.5 to 9 world units, so the count that comes out rises with it. Nothing about the geometry changes across the frame; what changes is the QUESTION each point asks, and that is the whole reading a per-point radius buys: 'how crowded am I, at my own scale', a big point surveying a big neighbourhood and a small one a small neighbourhood in the same cook. NEIGHBOURHOOD IS THEN NOT SYMMETRIC, and that is the point rather than a defect — B lying inside A's radius does not put A inside B's, so two points can disagree about whether they are neighbours and `countAttr` counts what each point can SEE. That asymmetry is also why this param needs no pair rule where `connectPoints.radius` needs max(rA, rB): an EDGE is one thing shared by two points, so it would depend on which endpoint asked, while a count is one point's own measurement and belongs to it alone. `reach` is written with `remap` on the x component rather than from noise so the gradient is legible as a gradient — swap in a noise or an authored size and the reading is the same. COST, NOT CORRECTNESS, is what a mixed set of radii affects: the uniform grid is sized from the largest FINITE radius, and a query wider than that scans more cells rather than returning a different answer. An INFINITE radius is legal here and means the whole cloud, falling back to a full scan at O(n) per point — but a graph FILE cannot carry one, because JSON has no infinity and the serializer refuses a non-finite param outright, and a live param patch cannot carry one either: that route gates on the param declaring `acceptsInfinite`, and this one does not — in the whole standard library only `filterByBounds`' and `filterPrimitivesByBounds`' bounds do, so `setParam` here answers 'expected a finite number, got null'. What is left is an expression that COMPUTES one at cook time, and `div(1, 0)` in this slot duly gives every point a count of 1199 — every other point in the cloud. Under a partitioned cook the halo a cell needs is the GLOBAL MAXIMUM this field can return anywhere in the world, DERIVED rather than measured — the far neighbour that would have set it is precisely the one a clipped cell cannot see. Here the `clamp` states it: 9. The remap alone would not, because `remap` is UNCLAMPED — the same expression returns 67.75 at x = 500, and 'anywhere in the world' is precisely the range a halo has to survive rather than the range this one cook happens to sample. Underestimating does not throw; it silently misses neighbours, at the seams only. THE LIFT IS THE PICTURE, not part of the idiom: `nbrCount * 0.25` raises each point by what it measured, turning a flat plane into a wedge that rises left to right even though the cloud under it is uniform. The wedge is CURVED, and that is the arithmetic being honest: `reach` is linear in x, but the disc it sweeps grows with its SQUARE, so a linearly widening question gets a quadratically growing answer: about 4 neighbours across the first six units and about 52 across the last. THE WEDGE TURNS OVER before it gets there, peaking at a band mean of 58 just inside the far edge and falling back — and that is the boundary rather than the arithmetic. A point near the edge sweeps a disc that runs off the end of the cloud, so it counts less than an interior point asking the identical question. It is the same effect a partitioned cook sizes a halo to defeat, visible here because nothing feeds this cloud from outside. Delete that node and the graph is unchanged in everything it teaches — but a plane of 1200 dots four pixels wide cannot show a number, and a corpus graph that cannot be seen fails at its only job. `color` carries the same count as a HUE at constant brightness, cool where a point sees little and warm where it sees a lot. A point cloud renders at one uniform size — per-point `scale` is read for instance transforms, never for a bare cloud — so colour is the only per-point channel the picture has, and a ramp that darkens instead of shifting hue spends half of it on going invisible. `basics-neighborhood-count` is this graph with one radius for the whole cloud.",tags:[`basics`,`attributes`,`neighborhood`,`fields`,`measure`]},Xa={title:`turn a path's own points to follow it`,description:"A path built by `pointsToPath` carries no `tangent` — only a sampler writes one, for the points it created — so `orientAlongVector` has nothing to read. `writeTangents` supplies it, from the normalized central difference between each point's neighbours along the polyline, which stays smooth through corners and wraps on a closed path. Both nodes keep the points, their attributes and the topology exactly as they arrived, so the `width` column written before the path was built is still on the output after the rotation: that is the whole difference from `place/along-curve`, which resamples and hands back new points carrying none of it. Run the pair BEFORE any filter — every filter drops topology, and `writeTangents` would then find no paths.",tags:[`basics`,`path`,`rotation`,`attributes`]},Za={title:`turn each point to face a direction`,description:"`orientAlongVector` writes the standard `rot` quaternion so a chosen local axis points along `direction`, with `up` fixing the roll. `direction` is field-capable and resolved per point, so an expression is what makes each point face somewhere different: here `vec(P.x, 0, P.z)` points every point radially away from the origin. A zero-length direction leaves that point's rotation alone rather than inventing one.",tags:[`basics`,`transform`,`rotation`,`fields`]},Qa={title:`split one cloud into labelled groups`,description:"`partitionByAttribute` splits the input into one point cloud per distinct value of an i32, u32 or string attribute, so a single declared output holds several geometry items rather than one. Groups arrive in order of each value's first occurrence and each is tagged `<name>=<value>`, which is how a downstream node or a host routes them apart. The labels here come from a string `setAttribute` whose `value` acts as a per-point selector into `values`.",tags:[`basics`,`attributes`,`partition`,`routing`]},$a={title:`even out the spacing along a path`,description:"`pathResample` walks each polyline's own arc length and places new points at even steps along it, which is a different operation from thinning a cloud: `selfPrune` keeps a subset of the points it was handed, while this creates points that were never there. The ellipse shows why it is needed — `shape/ring` spaces its points evenly in ANGLE, and on anything that is not a circle that leaves them bunched at the two ends of the long axis. `count` mode places exactly that many samples on every path whatever its length, and on a closed one they divide it without duplicating the start, so every step here comes out equal; `spacing` mode steps a fixed number of world units instead, so a longer path simply gets more points. The output is still a path and a closed one comes back closed, but the points are new: they carry `tangent` and `curveU`. Nothing written on the input's POINTS is carried across — but every PRIMITIVE attribute is, onto both the new points and the resampled path, so a road resampled here stays a road that knows its own width.",tags:[`basics`,`path`,`resample`,`spacing`]},eo={title:`draw a curve as solid geometry`,description:"One oriented asset per segment, which is a different job from a skin. `sweepProfile` is what draws a curve as a continuous SURFACE; this node draws it as a run of discrete instanced assets, which is the only way to spell a chain of separate links, a row of sleepers or a string of beads. `pathSegments` emits ONE point per polyline segment: positioned at the segment's midpoint, `rot` turning the chosen local axis onto the segment, and `scale` carrying the segment's length on that axis with `radius` on the other two. Spawn a unit cylinder — radius 1, height 1 — on those points and each one lands exactly on its segment, so a whole tangle of cable costs a single draw call. The default axis is `+y` rather than orientAlongVector's `+z` because the assets this feeds are cylinders and capsules, which three.js builds along Y. `extend` adds to both ends, closing the wedge consecutive segments leave on the outside of a bend. The OUTPUT IS A PLAIN CLOUD: the points are midpoints, not the curve, so re-pathing them describes the midpoints.",tags:[`basics`,`curve`,`path`,`instancing`]},to={title:`cut one cloud into several separate paths`,description:"With `groupAttr` set, `pointsToPath` splits the cloud by a whole-number point attribute and emits one polyline per distinct id, in ascending id — four rows here become four independent paths over the same 40 points, not one path zig-zagging between them. The ids come from a `setAttribute` of type i32 reading world Z, which is what keeps the grouping a property of the geometry rather than a hardcoded list. Within a group the points are visited in input index order; `orderAttr` is the companion knob when that order is not the one the path should follow, and its ties always break to the lower index so the result never depends on the sort.",tags:[`basics`,`path`,`groups`,`attributes`]},no={title:`place points on a regular grid`,description:"The deterministic counterpart to scattering: `pointGrid` places countX * countY * countZ points stepped by `spacing` from `origin`, in X-fastest order. There is no randomness at all here — the same params always give the same positions, which makes a grid the right starting cloud when the variation should come from a later node rather than from the source.",tags:[`basics`,`grid`,`source`]},ro={title:`build a path from a point cloud`,description:"`pointsToPath` is the only way a saved graph can produce polyline geometry: it lays one `polyline` primitive over the points it was given, so the points and every attribute on them survive untouched and only topology is added. Visiting order is the input's point order unless `orderAttr` names a sort key. `closed` appends a trailing vertex referencing the first point — closure is structural, not a flag, so a closed path over 12 points has 13 vertices and there is no duplicated seam point to trip over. `shape/path-loop` is exactly this pair of nodes under one name.",tags:[`basics`,`path`,`topology`,`source`]},io={title:`reference a shipped primitive by name`,description:"Instead of embedding a copy of a subgraph, a node can name one from the shipped vocabulary: `ref: { name }` resolves against the registry, which is populated by importing `pcg-ts/primitives` (the `pcg` CLI does it for you). Prefer this over rebuilding the same four nodes by hand — the catalog in docs/primitives.md documents each primitive's real behaviour, including what varies per instance. A `ref` may also carry an optional `hash` to pin the exact content it was authored against; without one it always resolves to the library's current version.",tags:[`basics`,`primitives`,`vocabulary`,`ref`]},ao={title:`move an attribute between domains`,description:"Attributes live on domains — point, vertex, primitive, detail — and `promoteAttribute` walks the geometry's topology to move one between them. Here a per-point `height` becomes a per-triangle `height` by averaging the corners, which is what a shader or an exporter that colours faces rather than corners needs. Elements with no contributors keep the attribute default, and string attributes support only mode 'first'.",tags:[`basics`,`attributes`,`domains`,`promote`]},oo={title:`space props evenly along a curve`,description:"Two primitives cover the whole road-and-lamp-posts shape: `shape/path-meander` is a curve SOURCE — an open path that wanders off a straight line by noise and is re-evened by arc length, needing no cloud to start from — and `place/along-curve` resamples it and turns every new point to face the way the curve goes, so a `spacing` of 6 means a post every 6 world units however long the road turns out to be. The points `place/along-curve` emits are new ones carrying `P`, `tangent`, `curveU` and `rot`, plus every attribute the curve carried on its PRIMITIVES — a post inherits the road it stands on; when the curve's own POINTS matter instead, `write/orient-along-path` orients them in place. Note what varies: the meander carries its noise seed inside a field spec, so `variant` is its only re-roll.",tags:[`basics`,`primitives`,`path`,`placement`]},so={title:`aim things radially around a curve`,description:"`orientAlongVector` fixes the roll around a direction with an `up` hint, and a CONSTANT up cannot follow a curve that turns over: as the tangent passes through the up vector the roll flips a half turn, and everything placed along the curve snaps round with it. `place/radial-on-curve` solves that the only way it can be solved — with a per-point up carried ALONG the curve. `writeCurveFrame` seeds a normal perpendicular to the first tangent and transports it point to point by double reflection, the rotation that moves it least at each step, giving `curveNormal` and `curveBinormal` beside the tangent. The fan is then cos(a) * curveNormal + sin(a) * curveBinormal — the unit vector at angle `a` in the plane perpendicular to the tangent — fed back in as `up`, which is field-capable for exactly this. `spread` is how much of the turn the fan covers, measured from the normal: 0 lines everything up on one side, 1 is a complete fan. Compare `place/along-curve`, which aims along the tangent and gives every copy the same roll.",tags:[`basics`,`curve`,`path`,`instancing`,`orientation`]},co={title:`what a graph hands back that is not geometry`,description:"A cook returns items, and only some of them are shapes. This graph returns three kinds at once and the difference between them is the lesson.\n\n`attributeReduce` collapses a whole domain into ONE value on the DETAIL domain: the sum of the weights, their maximum, and a plain count of the points. Three reductions of one attribute need three distinct `outName`s — left empty the name is reused, which is what promoting would have produced and is fine for one. Mode 'count' reads no attribute at all, so `name` is left empty there rather than pointing at a column it will ignore. THE CONSTRAINT WORTH KNOWING: a point-domain field cannot read the detail domain. That is deliberate, not an oversight — a field resolves each element from that element alone, and a total is a property of every element at once, so a graph cannot feed its own totals back into its own points. Anything that needs to (calibrate a count against a budget, normalize by a maximum) runs as a loop in the HOST, between cooks, reading these reports and setting params for the next one.\n\n`removeAttribute` is the other half. `weight` here is scratch — it exists to be reduced — and every idiom that carries a value between nodes leaves its column on the output forever unless something takes it off. This is the only node that can, and the ORDER is the point: the reductions read the column, so they must run before the removal. It is `strict` by default, so a typo in the name is an error naming the columns that do exist rather than a silent no-op leaving exactly the debris it was meant to clear.\n\n`valueConstant` is the third kind: a plain number, riding back beside the geometry. Here it is the weight budget the graph was authored against, for a host to compare the reduced `weightSum` against — and as it stands this cook comes out OVER it, which is the interesting case: the comparison is a signal a host loop acts on by changing a param and cooking again, not an assertion the graph makes about itself — a number the graph declares rather than derives, which is why it is a constant and not another reduction. Worth knowing before reaching for it: EVERY input pin in this library is geometry-kind, so a value item has nowhere to go inside a graph. Its only destination is an output.",tags:[`basics`,`attributes`,`reduce`,`detail`,`values`]},lo={title:`make a saved noise re-roll with the graph seed`,description:'A serialized field expression bakes its numbers, so a noise that carries `opts.seed` as a literal leaves the graph\'s seed box moving every scatter and jitter while the shape stays exactly where it was. `opts.seed` is the way out, because besides an integer it takes one tagged form: `{ "from": "node", "variant": 5 }` derives this noise\'s seed as `hashCombine(the cooking node\'s own seed, variant)`, the node seed being `deriveNodeSeed(graph seed, node id)` — the same number `randomField` hashes. So the seed box now moves this surface and not merely the points on it. Every part of that shape is load-bearing. The whole derivation is u32 murmur with no float anywhere in it, which is why it is bit-exact on CPU and GPU rather than budgeted the way a noise interior is: a seed has no tolerance, since a one-ULP disagreement in one is not a rounding error in the output but `hashCombine` avalanching to an unrelated number and the two paths cooking different noises. That is also why the position is the one noise option that takes a spec and the seed admits no arbitrary expression — every field column is f32, so a seed read through one would arrive already rounded to 24 bits. `variant` stands where the old literal seed stood, and it picks WHICH draw off this node: two noises on one node with different variants are two independent fields, which is how a single node yields several. It is capped at 2^24, where an f32 stops holding every integer, because the GPU may read it back through a uniform slot — a variant is a slot number, not a seed. Adopting the form RE-ROLLS the noise: frequency, amplitude, position and normalization are untouched, but the field is a different draw from the same family, so it is an edit made once and deliberately rather than a new default. Change the seed and this surface becomes a different surface; write a literal back into `opts.seed` and it is deaf to the seed again.',tags:[`basics`,`fields`,`noise`,`determinism`]},uo={title:`measure distance since the last gate, and to the next one, around a closed lap`,description:"`pathScan` accumulates from a path's seam and never resets, which answers 'how much lies behind me' and nothing else. The questions a marker rule actually asks are 'how far since the last gate' and 'how far to the next one', and neither is a prefix sum: getting the first out of a scan means subtracting the scan value at the most recent gate behind you, and OBTAINING that value is a backward look-up along the path. A field cannot perform one — a field resolves each element from that element alone — and `pathScan` was the library's only order-aware node, so there was nothing to build the emulation out of. `pathRuns` is the missing primitive: a SEGMENTED scan, where the accumulator resets at points a boolean attribute flags.\n\nIt accumulates a VALUE rather than counting elements, which is the whole ergonomic difference. Scan a per-segment length and you get distance; scan a constant 1 and you get the number of points; scan a cost and you get cost. Here `seg` is the lap length over the sample count — `pathResample` in `count` mode spaces its samples evenly, so one number describes every segment — brought back from the primitive domain by `promoteAttribute`, the same way `basics-density-along-a-path` recovers its scan total.\n\nThe gates are picked with arithmetic on `index` — every sixtieth sample of the 240, offset by thirty — which is exact where a threshold on `curveU` would not be: `mod(index + 30, 60) < 0.5` selects four samples and cannot select a fifth by rounding, where `fract(curveU * 4)` near zero can read 0.9999 instead and drop a gate. The offset is the point of the exercise. Without it the first gate would land on sample zero, which is the seam, and the graph would demonstrate nothing: gates at the seam make wrapping a no-op. `index` names a SLOT rather than an element and anything that filters or reorders upstream renumbers it, which is safe here because it is read immediately after the resample that creates the samples.\n\nWHAT THE CLOSED LAP IS DOING HERE, because it is the case the primitive exists for. Sample zero sits thirty samples PAST the last gate, on the far side of the start/finish line from it. With `wrap` on, the walk starts at the first flagged point rather than at vertex zero, so that run stays ONE run and sample zero reads the distance back to the gate behind it — about a quarter of the way into its run rather than at its start. Turn `wrap` off and the seam cuts the run in two: sample zero reads zero, and the thirty samples after it read a distance measured from the SEAM rather than from their gate, wrong by however far back the gate is. Nothing about the column shows it — the values are all still positive and still increasing, and the ramp simply restarts at a place no gate stands. A real circuit always has a corner that straddles the line.\n\nBoth directions are cooked because they are different questions rather than one question reversed. `since` reads backward-looking (what is behind me since the last gate) and `ahead` reads forward-looking (what is in front of me up to the next one); recovering either from the other needs each run's total, which no point holds. The colour ramps from `since` over a quarter-lap, so each run climbs from blue at its gate to red just before the next, and the ramp is continuous across the seam — that continuity is the whole picture. The second output is the four gate points themselves, filtered on the same flag, so the ramp can be read against where it is supposed to reset.",tags:[`basics`,`path`,`runs`,`segmented-scan`,`closed`]},fo={title:`scatter points in a box`,description:"The smallest complete graph: one source node fills an axis-aligned box with a fixed count of points. Nothing is connected and nothing is filtered, so the output count is exactly `count`. Every point already carries the standard attributes (P, rot, scale, density, boundsMin, boundsMax, color, seed) whether the graph writes them or not, which is why later examples can filter on `density` without creating it first.",tags:[`basics`,`scatter`,`source`]},po={title:`scatter points anchored to the world, not to the box`,description:"The same shape of graph as 'scatter points in a box', with the one difference that makes a region streamable: `pointScatterInWorld` computes each point from its own lattice cell and index, so the box only says which points to RETURN. Widen it, move it, or ask for it in four pieces and every point that was already there stays exactly where it was, with the same per-point seed — `pointScatterInBounds` derives positions FROM the bounds and moves all 500 of them when the box moves an inch. Population is `density * area`: at 0.05 points per square unit over an 80x80 window that is 320 points, predictable without cooking, with `cellSize` deciding only how evenly they clump. The clip is half-open, so abutting windows tile the world with no gap and no duplicate — which is why a cell can derive its own halo by simply asking for a wider box.",tags:[`basics`,`scatter`,`source`,`streaming`]},mo={title:`a signed distance field, and which side of it`,description:"A SIGNED DISTANCE FIELD, which is what `distance` and `sign` are for together. `distance(P, centre) - 12` is negative inside a circle of radius 12, zero on it and positive outside — one number that carries both HOW FAR and WHICH SIDE, and splitting those two questions apart is the whole idiom. `sign` answers the second, `abs` the first, and the height here multiplies them: `sign(sd) * 5 * exp(-0.30 * |sd|)` raises a rim outside the circle and sinks a trench inside it, both decaying away from the boundary. The result is a crater, and the ring where it crosses zero is the shape the field was defined by. `sign` IS EXACT ON BOTH PATHS, and that is a design decision rather than a measurement that happened to come out clean: it is defined as `(x > 0) - (x < 0)`, a pair of comparisons with no interior to round, so the device and the CPU cannot disagree. Two answers depart from a host language on purpose — a NaN gets 0 rather than NaN, and a negative zero gets +0 — because a rule both paths execute exactly beats a rule one of them approximates. Neither input occurs in this cook, which is the ordinary case: the departures matter where a field feeds `sign` something degenerate, not on a clean grid. It is what `normalize` already does to a scalar; it exists to buy the NAME, on the precedent `step` set, since nobody reaches for a vector normalizer to ask which side of a line they are on. The exact-zero case is REACHABLE but unreached here, and the arithmetic says why: a grid point lands on the circle only where its integer offsets satisfy j^2 + k^2 = 711.11, which none do. The nearest sits 0.0075 off it, so the deepest trench and the highest rim come out at 4.989 rather than the nominal 5 — which is the cooked bounds, and a neat demonstration that a sampled field only ever shows you the samples. `distance` IS EXACTLY `length(sub(a, b))`, pinned by a test rather than merely intended — the difference is rounded to f32 before it is squared, because that is what `sub` stores and what the device subtracts, so the fused spelling cannot drift from the composed one. It is the fn `basics-field-shaping` open-codes seven times over, and its measured GPU budget is 1 ULP against `length`'s 4, because that family compounds two fns in one measurement where this is a single square root over a subtraction. THE COLOUR IS THE SIGN, not the height, which is why the two regions read as different materials rather than as one surface with a fold in it: a ramp with stops at -1, 0 and +1 can take only three values, because `sign` only ever produces three. In this cook it takes TWO — no point lands on the circle, so the middle stop is never selected, and it sits there for the boundary case rather than for anything visible.",tags:[`basics`,`fields`,`distance-falloff`,`composition`]},ho={title:`spawn a different asset per point`,description:"A string `setAttribute` with a non-empty `values` list turns its field-capable `value` into a per-point selector — floor, then clamp into range, NaN picks 0 — so weighting by repetition works: 'pine' twice in four entries is half the points. Pointing `spawnInstances`' `assetAttr` at that attribute splits the output into one batch per asset id, in first-occurrence order, with no per-point branching anywhere in the graph.",tags:[`basics`,`spawn`,`instancing`,`strings`]},go={title:`turn points into instance batches`,description:"`spawnInstances` is a terminal: it converts a point cloud into render-agnostic instance batches, one 4x4 world matrix per point composed as T(P) * R(rot) * S(scale) from the standard attributes. Points group into one batch per asset id. The node has two output pins — `instances` for the batches and `points`, which passes the input through unchanged for chaining or debug rendering — and this graph declares only the first.",tags:[`basics`,`spawn`,`instancing`]},_o={title:`wrap a graph as one node with its own knobs`,description:"A `subgraph` node carries an inner graph plus the pins and params it exposes, so a reusable piece becomes a single node with a deliberately small interface. Declarations live in the payload and VALUES live in the node's own `params`, exactly as a standard node keeps its schema in the registry and its value on the node. A declaration may not carry `type`, `enum` or `acceptsField` — those are re-derived from the targets' registered schemas, so a payload cannot claim a capability the inner params do not have.",tags:[`basics`,`subgraph`,`composition`,`params`]},vo={title:`scatter points over a mesh surface`,description:"`surfaceSample` picks each candidate's triangle with probability proportional to its area and then a uniform position inside it, so coverage is even in world units rather than per triangle. Output points carry P, the flat per-triangle `normal`, density 1 and a hashed per-point seed. `densityField` is field-capable and applied after placement, so the count is at most `count` and exactly `count` while density stays 1.",tags:[`basics`,`mesh`,`sampler`,`placement`]},yo={title:`put a real surface on a curve`,description:"A curve becomes a skin. `sweepProfile` places a cross-section on EVERY POINT of a polyline and stitches consecutive placements into three-vertex 'poly' triangles — the same topology `meshPrimitive` emits, so the result is not a second class of mesh: `surfaceSample`, `promoteAttribute` and the 'uv' and 'raycast' transfer mappings all see it. THE PATH IS NOT RESAMPLED. One ring per input point, exactly where the point is, which is why `radius` here can be a field: it resolves AT the ring rather than being averaged across a segment's two endpoints the way `pathSegments` must, so this taper from 0.9 to 0.15 along `curveU` is exact. For a finer surface, run `pathResample` first — that is the knob, not a subdivision param. Rings meet through a mitered joint, so the section keeps its radius round a bend instead of pinching by the cosine of the half-angle. The node writes `normal` itself rather than leaving `computeVertexNormals` to smooth across the uv seam and the caps, and writes `uv` with `u` as normalized arc length — the same measure `curveU` carries, so a texture lines up with anything else measured along this curve.",tags:[`basics`,`curve`,`surface`,`mesh`,`fields`]},bo={title:`tile a field across the origin`,description:"TILING IS `fract` AND `mod`, and this graph is built so the choice inside them is visible rather than asserted. Every bump is the same expression of a TILE-LOCAL coordinate — `fract(x / 8)` and `fract(z / 8)`, each in [0, 1] — closed at the top, because `fract(-1e-8)` rounds up to exactly 1 in f32 — and the checker under them is the TILE INDEX, `mod(floor(x / 8) + floor(z / 8), 2)`. The field is unbounded in principle and periodic in fact, which is the whole trick: nothing here stores a tile, and a point 4000 units away costs exactly what a point at the origin costs. The size is one inline `param`, and counting where it lands is instructive: FOUR logical uses — two `fract` divisions and two `floor` divisions — but TEN occurrences in the file, because `bump` reads its argument twice and the colour ramp repeats the whole tile index once per channel. One knob moves all ten coherently; spelling them as ten literals means ten edits to retile, and nine chances to leave one behind. That gap between four ideas and ten occurrences is the A3 entry in PLAN.md seen from another angle — nothing yet lets an expression bind a name to a subexpression it uses more than once, so the inline `param` is standing in for the `let` the grammar does not have. LOOK AT THE ORIGIN. Both fns in this library are FLOORED — `fract` is non-negative for every finite input and `mod`'s sign follows the DIVISOR — so `fract(-0.125)` is 0.875 and `mod(-1, 2)` is 1. The tiling therefore crosses x = 0 and z = 0 with no seam at all, and that is the entire reason the choice was made that way. A truncated remainder — JS `%`, and WGSL's `%` on floats — answers -0.125 and -1 instead, which mirrors every tile in the negative quadrants: the bumps invert into pits and the checker takes a third value the ramp was never given a stop for. It is a defect that cannot be seen in a demo built around the origin's positive corner, and it appears the moment a world grows in the other direction, which is precisely what an unbounded generator does. THE `floor` PAIR IS NOT THE SAME OPERATION as the `fract` pair, though they read alike: `fract` gives the position WITHIN a tile and `floor` gives WHICH tile, and together they are the standard decomposition of a coordinate. `fract(t)` is exactly `mod(t, 1)` — the library pins that equivalence in a test rather than just claiming it — so the two lines here are one idea used twice, once for a continuous value and once for an integer bin. Both are BIT-EXACT on the GPU, which is not a given for `mod`: it is four operations, and the CPU rounds each to f32 individually so it runs the device's expansion step for step rather than accumulating in f64 and rounding once. The bump is `t * (1 - t)` on each axis, a parabola that is zero at both tile edges and so leaves no ridge where tiles meet — a falloff that did not vanish at the boundary would show the grid as a lattice of cracks.",tags:[`basics`,`fields`,`grid`,`composition`]},xo={title:`read a value off a surface below each point`,description:"`transferAttribute` copies an attribute from its `source` geometry onto the main input's points. Mapping 'raycast' casts a ray from each point along `direction` and interpolates the value at the nearest forward hit, which is how a scattered cloud reads the terrain under it. A point whose ray hits nothing keeps the value it already had — never an invented one — and `missCountAttr` records how many missed as a detail attribute so a graph can assert on it.",tags:[`basics`,`transfer`,`mesh`,`raycast`]},So={title:`move, turn and size a whole cloud`,description:"`transformPoints` applies P' = R * (scale * P) + translate about the world origin, with `rotateEuler` in degrees extrinsic XYZ. It composes with the per-point transform attributes rather than replacing them: `rot` becomes R * rot and `scale` multiplies componentwise, so transforming a cloud that already carries orientations keeps them. All three params are field-capable, which is how one node can taper or twist a cloud instead of moving it rigidly.",tags:[`basics`,`transform`]},Co={title:`two things called bounds, and they are not the same thing`,description:"The word does double duty in this library and confusing the two costs an afternoon, so here they are in one graph.\n\n`filterPrimitivesByBounds` is a WORLD box that SELECTS. It keeps or drops whole primitives by testing their vertices against [boundsMin, boundsMax], and it is one of only two filters here that PRESERVE TOPOLOGY — the survivors keep their vertices, their vertex and primitive attributes, and the points they share, so a network filtered this way is still a network rather than a cloud that used to be one. Three params decide what 'in the box' means and they are read together. `vertex: \"first\"` tests exactly ONE vertex per primitive, which is what makes it an OWNERSHIP rule: every primitive has exactly one first vertex, so abutting boxes claim it exactly once between them and a partitioned cook can tile the world with no edge counted twice. `boundary: \"halfOpen\"` is the other half of that — min inclusive, max exclusive — so an edge lying exactly on a shared face belongs to one box, not both. A consequence worth seeing before it surprises you: only the FIRST vertex is tested, so an edge that starts inside and ends well outside survives WHOLE, and the filtered network overhangs its own box — about forty percent of the points left here sit outside [-12, 12]. That is the ownership rule working, not leaking: the box owns edges, not space. And `unreferencedPoints: \"drop\"`, used here, discards the points no surviving edge touches; the default 'keep' leaves the point domain completely untouched instead, same points in the same order, which is what anything holding a point index needs.\n\n`setBounds` is a per-point LOCAL extent that DESCRIBES. It writes `boundsMin` and `boundsMax` on every point as that point's own axis-aligned size, in world units, and nothing filters on it — spawners and downstream nodes read it to know how much room the thing at that point takes up. Constants here for legibility, but the param is field-capable and that is where it earns its place: an extent derived from the point's own `scale`, or chosen by a species attribute, gives every instance the box it actually occupies instead of one box for the whole cloud. Note also what it does NOT check — min against max is not validated, the two corners are written independently, and a point whose corners cross is a point with an inside-out box.\n\nSo: one bounds is a question asked of the world, the other is an answer a point carries about itself. They share a name and a shape and nothing else.",tags:[`basics`,`bounds`,`filter`,`topology`]},wo={title:`fill a box with points, then carve it`,description:"`volumeSample` is the library's only source that fills a volume with a REGULAR GRID — `pointScatterInBounds` fills a box too and `pointGrid` reaches into y, but neither puts one point in each cell of a requested size: each axis of the box is divided into max(1, floor(extent / cellSize)) whole cells and a point is placed at each cell centre, then jittered inside its own cell. `cellSize` is a REQUEST, not the cell you get — 28 units of extent at 1.4 gives exactly 20 cells because it divides evenly, while an extent that does not divide gives a LARGER cell (extent 20 at cellSize 12 is one 20-wide cell), and every axis has at least one cell however small the box. Bounds come from a connected geometry's P extents when one is wired and from `boundsMin`/`boundsMax` when none is, as here. JITTER IS A FIELD, and it is evaluated on the UN-JITTERED centres: 0 below y = 1, rising to 1 by y = 11, so one node emits an exact lattice at the floor and a fully random cloud at the ceiling with the dissolve in between. That gradient is the whole reason the param is field-capable — the alternative is two `volumeSample` nodes and a merge, which cannot interpolate between them. THE CARVE is `filterByDensity` in 'threshold' mode, which `basics-filter-by-density` describes but does not show, and its `threshold` is a field too: it rises from 0.32 at the floor to 0.86 at the ceiling, so the solid erodes upward and what survives tapers instead of ending in a flat lid. TWO THINGS THIS GRAPH LEARNED THE HARD WAY. A `normalized` fBm does NOT actually span [0, 1] — normalization maps its NOMINAL range, and the three octaves of summed gradient noise here only reach about the middle two fifths of it in practice — measured on this cook, 0.325 to 0.740. A threshold swept across the full [0, 1] against a raw normalized fBm therefore keeps everything at one end and nothing at the other, and the fix is the `clamp(remap(noise, 0.35, 0.68, 0, 1), 0, 1)` here, which stretches the range the noise really occupies onto the range the threshold really uses. And Perlin noise is identically ZERO on integer lattice coordinates, so a lattice of cell centres sampled at an integer frequency gives a dead attribute and a filter that keeps everything or nothing; `frequency: 0.055` keeps the samples off the lattice, `cellSize: 1` at `frequency: 1` would not. WHY THIS ONE SPAWNS. A dense 3D point cloud is unreadable as points — unlit dots on a regular lattice read as moiré rays rather than as a shape, and the structure this graph is about disappears into them. Instances are shaded geometry, so the ordered floor reads as packing and the dissolved ceiling as debris, and `scale` — ignored for a bare cloud — becomes the channel carrying `density` per box.",tags:[`basics`,`source`,`sampler`,`fields`,`filter`,`density`,`instancing`]},To={title:`plant a hillside, thinned by slope and treeline`,description:"The forest recipe as one serialized graph. A displaced plane is the terrain; `surfaceSample` scatters candidates over it carrying the flat per-triangle `normal`; two attributes derived from that geometry — `height` from the point's own Y, `slope` as `1 - normal.y` — become the two `filterByAttribute` gates that decide where a tree is allowed. Scale is stamped BEFORE the filters, since it depends on nothing they decide. The last attribute is a string: `species` selects into `values` per point, mixing roughly three pines to one bush, and the spawner splits by it.",tags:[`examples`,`terrain`,`placement`,`filter`,`spawner`]},Eo={title:`a fusable chain, on the CPU or the device`,description:`Five count-preserving nodes in a strict line, every field param authored as a serialized spec rather than composed in code, so the chain is device-eligible. Switch the cook path in the toolbar: the two device paths agree bit for bit, so the hash holds across them while the time drops as the tail fuses into one resident run. The CPU hash differs, and that is not a defect: GPU floats are not byte-identical to CPU floats. Raise the point count to see why the device path exists.`,tags:[`gpu`,`fields`,`attributes`,`performance`]},Do={title:`headless scatter`,description:`Scatter points across a 60x60 patch, write a height attribute from fbm perlin noise, keep the points above the midline, and jitter what survives. Cooks in plain Node with no renderer and no page — the graph is data, and the CLI is its feedback loop.`,tags:[`headless`,`scatter`,`fields`,`cli`]},Oo=JSON.parse('{"title":"a suspended rig, built from curves","description":"A box truss follows a spline pushed around by two noises: four chords with zigzag bracing and square frames every few bays. Components are scattered over it in noise clusters and aimed radially, chains hang it from the ceiling, cables wrap along it, and two more kinds hang off it — a fringe gathered into bundles, and swags strung between anchors. The cables are a `forEach`: one body cooked once per cable, each seeded on its own carrier, where this used to need one hand-built branch per cable and so could not be a saved graph at all. The wander is a plain `transformPoints`: the three numbers shaping it — how far it drifts up, how far sideways, and how fast — are `param` spec nodes carrying their own values inside its `translate` expression, and the editor reads each as a knob. It used to be a one-node subgraph, because a param could only be DECLARED on a wrapper, and the wrapper existed for nothing else. `wanderScale` is named twice in that one expression and is still one knob writing both — the case that made a wrapper look unavoidable. Everything that was drawn as a tube is a real surface now: `sweepProfile` skins the chords, the braces, the frames, the cables, the fringe and the swags, every one of which used to end at `pathSegments` with a unit cylinder landing on each segment — half the drawn triangles, because rings are shared between segments and no interior caps grow, and nine `extend` settings gone with them, because a continuous skin leaves no wedge at a bend to fill. The chains do NOT sweep, and that is the line between the two nodes: `pathSegments` still has a job of its own, one oriented asset per segment, and a chain of separate links is exactly that job — what it lost is the borrowed one, faking a tube. Four chords reach ONE sweep rather than four, because a sweep reads a geometry and a geometry holds as many polylines as you like: each strut arrives from `pathResample` already a polyline, `transformPoints` moves it without touching that topology, and `mergePrimitives` unions the four KEEPING it, so the sweep gets four paths in one geometry and the chord radius stays a single knob rather than one knob mirrored into four. Seven values this graph repeats are declared once at the top, under `params`. Six are numbers read by name from the expressions that need them: `trussHalfWidth` was TWENTY literals in four different float spellings of 0.425 — the chords at ± it, the braces and the component mounts at it × √2, eighteen of them here and two inside the cable body, which is why `pcg validate --params` counts nineteen readings on this graph rather than twenty: the eighteen plus the one wrapper slot that carries the value in — and `cableRadius` was three nodes that only the panel\'s `also` knew were one gauge of rope. A node-scoped param cannot say either of those, because the thing being said is that several nodes share one value. Four more say it about smaller things. `braceRadius` gangs the diagonal braces to the station frames, and it retired the last `also` row in the whole corpus: no fact about this graph\'s structure lives in a presentation file any more. `stretchMin` and `stretchMax` are the two ends of ONE draw that the component sizes write once per axis — `lerp(0.55, 1.6, randomField(\\"stretch\\"))`, four times over, for the rod, the bar and the panel\'s two faces — where how far a component may stretch is a single decision and eight literals were spelling it. `bundles` is the fringe\'s 7, read twice inside one expression: `floor(u × 7)` bins each strand and the `/ 7` puts the bin back in [0, 1), so the two have to agree, and until the number had a name nothing said so. The seventh travels the other way. ELEVEN WRAPS ARE ONE CALL NOW. Every place this graph wrapped a value into a range spelled it `x - N * floor(x / N)`, four nodes deep, eleven times over; they are `mod(x, N)` since the grammar grew one. The graph is 133 lines shorter and cooks the SAME BYTES — checked, not assumed, because `mod` rounds each of its four operations to f32 exactly where the four separate nodes did, and a corpus-wide digest of every cooked column moved on none of the 63 graphs. Those eleven sites were also the demand signal: `PLAN-rig-gaps-3.md` recorded them and filed them under ergonomics, while the fields plan was measuring demand for `mod` by grepping for a different idiom and finding none. `tubeSides` is an `i32`, and no expression can ever carry one — a field resolves per element and only f32, vec3 and vec4 read one — so it declares `targets` and is WRITTEN into the `sides` slot of all six skins — five `sweepProfile` nodes and, through its wrapper, the sixth inside the cable body — rather than substituted into a field, which is how one name reaches the half of the format that counts, enums, booleans and attribute names live in. It is one decision because it is a BUDGET and not a dimension: cost is linear in it, six skins pay it at once, and nothing about a 0.03-radius brace wants a different roundness from the 0.055-radius chord standing next to it. The gauge params say the opposite about the very same nodes — `cableRadius` and `braceRadius` gang radii precisely because a radius legitimately differs from member to member — which is why `sides` is the only one of the six non-field literals every sweep repeats that earns a name. The other five stay written out, and that is a measurement rather than an oversight: `profile` means \\"a rope is round\\" at three sites and \\"the stock is round tube\\" at the other three, `caps` turns on whether a tube\'s ends are visible at all (the station frames are closed rings and have no ends, the braces bury theirs inside the chords), `frame` is invisible on a circular section — as are the field-capable `up` and `roll` written out beside it — and `joint` with its `miterLimit` is one pair rather than two decisions, neither of which moves: `miter` is simply the right answer at the two places this rig actually bends and indistinguishable from `perpendicular` everywhere else, and the limit is never reached, because the sharpest bend anywhere a sweep sees is the braces\' zigzag at 100°, a stretch of 1.56 against a limit of 4, with the frame ring\'s square corner next at 1.41 and every resampled curve under 1.02. A shared name asserts that several slots must move together, and asserting that falsely is worse than the repetition. The sixth reading is inside the cable `forEach` body and gets there through a `sides` param on the wrapper, sitting next to `halfWidth` and working by the opposite mechanism: `halfWidth` declares NO targets and is read by the body\'s own expression, `sides` names one and is written into it. A body is bound by its wrapper either way and by nothing else, so ganging five of six would have left the cables at 8 while everything else moved. The three `writeCurveFrame` nodes repeat their three attribute names and KEEP them, now that a `string` param could reach them: `curveNormal` is named fourteen times here and only three of those are the writes — the other eleven are `attribute(\\"curveNormal\\")` inside expressions, where no param can follow — and `sweepProfile`\'s own `curveFrame` mode reads that attribute by that name in the library itself. The name is a shared vocabulary rather than this graph\'s to rename, so a knob over the three writers would only break the eleven readers. It used to tag every strut with a `strutId`, merge the POINTS, and rebuild the same four paths with `pointsToPath` — ten nodes spent throwing topology away and putting it back, because the topology-preserving union did not exist yet when this graph was written. The frames still regroup, and that contrast is the useful one: their rings connect the four chords ACROSS each station, topology that never existed anywhere upstream, so `pointsToPath` over `stationId` BUILDS something rather than restoring it — and the filter feeding it drops three points in four, which no union could have preserved. The chains and the fringe do not regroup at all any more, and that is the other half of the contrast: each strand is made a path BEFORE it is copied, and `copyToPoints` carries it across with `topology: \\"keep\\"` — the source\'s one polyline re-emitted per anchor, shifted onto that anchor\'s block of points. What that retires is not a node but a round trip. The copy no longer has to label its output with `targetIndexAttr` so that a rebuild can group on the label, and the fringe\'s swept surface stops carrying a dead `anchorId` on all 17,100 of its points; the path is built once over the strand itself — 35 points for a chain, 17 for a fringe strand — instead of over the 245 and the 1700 the copies make of them. The label was itself the second version of this problem: before `targetIndexAttr` the id was recovered arithmetically — `floor(index / 35)` for the chains and `floor(index / 17)` for the fringe — where the 35 and the 17 were the strand\'s point count written out a second time, in another node, with nothing holding the two together, so editing the strand welded every chain into one path and said nothing. A strand that is already a path cannot fall out of step with itself, which is the version that has no number in it at all. The swags are gated BEFORE the sweep now, which is where a gate has to sit once the thing downstream of it is a surface: `connectPoints` writes `edgeLength` on the primitive domain and the pick lands there too, so the gating is TWO `filterPrimitivesByAttribute` nodes cutting 456 chords to 360 and then to 62 while they are still polylines — the first drops every chord shorter than 4 units, the second keeps the roughly one in six the pick chose — and gating the segment cloud afterwards, which is what this graph used to do, meant building 7.35 times the geometry that survives. The components are proportioned by KIND rather than by one draw wearing four hats: one `byAttribute` reads the string `part` and hands back that kind\'s whole vec3, so a rod lengthens along the radius it points down, a bar along the chord it lies on, a panel widens on both of its faces while staying slab-thin, and a clamp is a squat collar rather than a cube. It was three nested `lerp`s over three `attributeIs` calls, written out once per AXIS — and `clamp` was in none of them, so it fell through all three to the uniform base scale and stayed there, because a fall-through nobody writes is a fall-through nobody can find. Its `default` is the same sentence made explicit: any part kind this expression does not name keeps the base scale, unstretched, and now says so. The kinds themselves are a WEIGHTING rather than a table with rows repeated to spell one: `values: [\\"rod\\", \\"bar\\", \\"panel\\", \\"clamp\\"]` with `weights: [4, 2, 1, 2]` and a `select` fraction, where nine rows used to carry the mix and a `mul(randomField, 9)` restated the table\'s own length beside them — append a fifth kind to that and the selector never reaches it, silently. The two spellings are byte-identical, because a whole-count weighting IS the repeated table. Three more numbers this graph used to restate are now read from the node that already knew them. `partScatter` jitters by half a STEP of the sampling above it: `partDense` publishes its step on the primitive domain, one `promoteAttribute` carries it onto the points before the cluster cut drops the topology, and the panel\'s scatter knob is that multiple rather than a distance — where the frozen 0.0189 it replaces was half a step of a 900-sample resample of a NOMINALLY 34-unit spine whose true arc length is 34.213, and so 0.05 of a step at the density knob\'s bottom and 1.10 of one at its top, invisible at one end and crossing its neighbours at the other. The carrier line states its SPACING instead of its far end, which is what stops the wraps knob from redrawing the cables it is not adding: the `15` in `end` was `count - 1`, and a `forEach` item key is content-derived, so 16 → 17 re-spaced the line and re-seeded every body — 1 of the 16 cables came back unchanged, against 16 of 16 now. `chainAlternate` reads the per-path `linkIndex` that `pathSegments` writes, not the global point index it used to: the two agree only while every chain has an even number of segments, and at `chainStrand.count` 36 the chains disagreed with each other — two distinct first-link orientations with nothing to report it, one at both 35 and 36 now. Eight declared outputs, one per part, plus the bare spine, so a viewer can tell them apart. The seed box re-rolls what is keyed on a node seed — where the components land, which chords get hung, how far each cable drops, and the four scalars that make each wrap its own — and the noises with it: all eight fbm fields take their seed from the node, `{ \\"from\\": \\"node\\", \\"variant\\": … }` rather than a literal, so the spine takes a different wander and the clusters a different shape instead of the same frozen field being walked over by points that moved. The six outside the cable body each carry their `variant` as an inline `param` of their own, so ONE noise can be re-rolled while the rest hold still — a node has a single seed, and the variant is what picks which draw off it. That is also what keeps the pairs apart: the spine\'s two wanders sit on one node and the fringe\'s two curls on another, so within each pair variant 0 and variant 1 are what make them independent draws, where a literal seed used to do it. The last two are the cable wobble, inside the `forEach` body, and they were held back as the deliberate exception — the body\'s seed varies per item, so its wobble was said to re-roll already. That was true of the sample WINDOW and false of the FIELD. Freeze the four per-carrier picks and cook: on the old literal seed the sixteen cables come back as ONE geometry, on the node seed as sixteen. A body node\'s seed is hashed with the item\'s own key, so `{ \\"from\\": \\"node\\" }` there means per-cable, and what it replaced was a fourth pick — `wofs`, transferred onto the wrap and multiplied by 1000 — whose whole job was to walk one frozen field far enough sideways that no two cables sampled the same place. That pick and its transfer are gone with it: the body is eight nodes where it was ten. Variants 0 and 1 keep the wobble\'s two components apart, the one riding the curve normal and the one riding the binormal, which a single literal seed had collapsed into the same number twice.","tags":["examples","curves","foreach","surface","instancing","rig"]}'),ko={title:`Riverbank`,description:"Distance to a FEATURE as the thing that shapes everything else, which several graphs use in passing and none is named for. A straight line of points is pushed sideways by a noise and pathed into a river, `splineSample` walks it at even spacing, and `sampleNearestPoint` writes each ground point's distance to the nearest of those samples into `riverDist` — one attribute that then drives three separate decisions: `filterByExpression` thins the trees near the water, their `scale` rises with it, and the driftwood is placed on the river's own samples rather than on the ground at all, pushed to the bank along the curve normal and turned to the `tangent` that `splineSample` already wrote. Measured across three equal-area distance bands, tree counts run 163, 256, 289 outward and mean scale 0.53, 0.81, 1.03, 1.22 — the falloff is in the numbers, not only in the picture. IT WAS AUTHORED BY AN AGENT THAT COULD NOT READ THIS REPOSITORY, and that is why it is here. Given only `pcg nodes`, `pcg fields`, `pcg validate` and `pcg inspect` — no source, no docs, no other graph, not even for the file format — it reverse-engineered the format in eight `validate` probes and reached a clean cook on its second write. What it could NOT learn from the catalog is what got fixed because of it: the field catalog published type signatures with no semantics, the noise output ranges the library already knew were never printed, and nothing warned that gradient noise is exactly zero on the integer lattice. The hand-rolled perpendicular it wrote in `driftToBank` — nine nested objects deep, because the grammar had no `cross` — is what put `cross` in the grammar, and `driftToBank` now reads as the single call it always meant. Byte-identical output, checked against a control that moves when the operands are swapped: the graph is unchanged and only its spelling of the idea got shorter. THE FOUR ASSET IDS IT INVENTED were the other thing it could not discover: `tree_pine`, `tree_birch`, `tree_willow` and `driftwood_log` cooked, validated and reported their instance counts while rendering as anonymous stand-ins, because an asset id is a free string and nothing checked it. They are the shared vocabulary's `pine`, `birch`, `willow` and `log` now, and `pcg assets <graph.json>` is what makes that checkable rather than a thing you notice in a picture.",tags:[`river`,`scatter`,`distance-falloff`,`path`,`spawn`]},Ao={title:`one cell of a streamed world, halo and all`,description:"The corpus graph a `World` can BIND, not just cook: every per-cell quantity is an ordinary top-level node param, so `bindPatches` reaches it as plain JSON and the level can cook on a worker. Its defaults are not a picture, they are the RECTANGLE OF ONE CELL: cell [0, 0] of a 64-unit level, which is [0, 64) on both axes, queried with the 4-unit halo it needs (the scatter window runs -4 to 68) and clipped back to what it owns. Note where that box is NOT — a cell is always [c*size, (c+1)*size), so no cell is ever centred on the origin, and a graph whose default box straddles it is quietly claiming to be a window rather than a cell. What a bind still supplies is the SEEDS: standalone they are the literals below, while a World writes ctx.worldSeed and salts of it, so the standalone cook shows this cell's geometry and mechanism rather than any particular world's bytes. Four seam hazards are wired on purpose. (1) The source is `pointScatterInWorld` — `basics-scatter-in-world` teaches it alone — whose lattice is a function of its own `seed` and never of the graph seed, so bind hands it a cell-INVARIANT `ctx.worldSeed` and varies only `boundsMin`/`boundsMax`. (2) The density noise carries a LITERAL `seed` inside its spec instead of `{ \"from\": \"node\", \"variant\": 0 }`, because a nodeSeed-folded noise 'samples a different region in every cell, so it must not feed anything that has to agree across a seam' (src/nodes/attributes.ts) — which is exactly what `basics-reseed-a-noise` wires up, and exactly what a level graph must not. (3) `pointNeighborhood` is the ONE-HOP rung: exact at a halo of `radius` and at no smaller width, so bind widens the scatter window by exactly 4 units and by nothing more. (4) `filterByBounds` at its default half-open boundary is the OWNERSHIP rule, bound from the UNWIDENED cell, because 'the exactness comes from the two cells sharing an endpoint value' — 'compare against the box, not against a recomputed index', since `floor(67.8 / 0.1)` is 677 while `678 * 0.1` is exactly 67.8 (docs/authoring.md). Its Y bounds are a finite +/-1e6 rather than infinities, which do not survive JSON and cannot be patched, and which an 'xz' World column does not bound anyway. `filterByDensity` and the `randomField` inside `size` both draw on their node seed, which the GRAPH seed does reach, so a per-cell `graph.setSeed` (or a `bindPatches` `seed`) would move them and 'the halo and the neighbour disagree, deterministically and silently' (src/runtime/types.ts); bind seeds them cell-invariantly and never reseeds the graph. The second output, `populationRank`, is the counter-example kept deliberately: a `fraction` field measures the population present in THIS cook, which under a World means the population HERE, so it is the unbounded rung no halo width can repair and the one thing in this graph a partitioned cook is not allowed to agree about.",tags:[`examples`,`world`,`streaming`,`halo`,`determinism`]},jo={title:`staged pipeline 1/5 — the ground and the wall`,description:"First step of a settlement-scale pipeline whose four stages are four files, each the previous file plus new nodes, connections and outputs — nothing removed, no param retuned, one shared seed. This stage ADDS the two things every later stage stands on: a subdivided plane pushed into rolling terrain by a noise field (`terrain`), and a 64-point ring displaced along its own radius and closed into a path with tangents written on it (`boundary`). Staging works because a node's seed is hashCombine(graphSeed, hashString(nodeId)) — node-local, independent of where the node sits in the DAG — so every earlier stage reproduces bit-identically inside every later one.",tags:[`pipeline`,`staged`,`terrain`,`path`]},Mo={title:`staged pipeline 2/5 — district centres and the field they claim`,description:"Stage 1 verbatim, plus a district layer. ADDS `districts`: a 34x34 grid masked to a disc and dropped onto the terrain, with every surviving cell told which district owns it. The centres come from a separate scatter thinned by `selfPrune`, numbered with an i32 `district` and given a string `districtKind`; `sampleNearestPoint` then writes the owning index, the distance to it and the kind onto each cell. `terrain` and `boundary` cook bit-identically to stage 1 — nothing upstream was touched. Staging works because a node's seed is hashCombine(graphSeed, hashString(nodeId)) — node-local, independent of where the node sits in the DAG — so every earlier stage reproduces bit-identically inside every later one.",tags:[`pipeline`,`staged`,`sampling`,`attributes`]},No={title:`staged pipeline 3/5, edited — hand-placed plots that win on priority`,description:'`pipeline-3-lots.json` verbatim, plus an authored edit layer: a `pointLine` terrace and a `pointGrid` block dropped onto the same terrain, stamped `locked = 1`, and wired into the edit slot the base reserved — ONE connection is the whole edit. It lands twice: as the `features` of a `filter/by-distance-to` that keeps procedural lots 3 units clear of it, and on pin `b` of `compose/merge-tagged`, which appends the authored points AFTER the procedural ones at the HIGHEST indices — the worst place an index-greedy prune could put them. They take every contested spot anyway, because `selfPrune` ranks by `priority: attribute("locked")`: higher wins, ties fall to the lower index, so the 12 authored points are visited first and the procedural ones prune against them. Survival is a value the points carry, not a position in a merge — drop the `priority` param and 8 of the 12 lose their spots to procedural neighbours; move the edit back to pin `a` and the same 12 survive. The point of the variant: `terrain`, `boundary` and `districts` cook bit-identically to the base while `lots` and `footprints` change — the edit is provably local.',tags:[`pipeline`,`staged`,`edits`,`authoring`]},Po={title:`staged pipeline 3/5 — a street, its frontage band, and lot footprints`,description:"Stages 1-2 verbatim, plus building plots. ADDS `lots` and `footprints`: the district centres are sorted by their bearing, atan2(z, x), and closed into one ring called `spine`, the district field is cut to a frontage band (within 11 of the street, at least 4 off it), each survivor is turned to face the nearest street sample and given a random size, and `selfPrune` spaces them 7 apart. A 4-corner ring copied onto every lot and grouped by `lotId` becomes one closed quad per plot. Note the reserved EDIT SLOT: `edits` is a `mergePoints` with nothing connected, so it cooks to an empty cloud that clears nothing and merges nothing — see the `-edits` variant, which is this file plus authored geometry and one connection. The slot comes with a RANK as well as a wire: procedural lots are stamped `locked = 0` on their way into the merge and the prune reads `priority: attribute(\"locked\")`, so a point arriving through the slot at 1 outranks every procedural neighbour it contests. Here nothing arrives, every point ties at 0, and the prune is exactly the index-greedy one it has always been — which is what `priority`'s default is for. WHAT `spine` IS, PLAINLY: an angular TOUR of the centres in bearing order, and NOT a road network. It cannot branch and it cannot fork — `pointsToPath` puts every point in exactly one group, so every centre gets exactly two neighbours and the result is always a single closed loop. What it is good for is what it is used for here: one continuous curve to measure frontage against, which `nearSpine`, `frontage` and `street` all read. The actual network is stage 5 (`pipeline-5-roads.json`), where `connectPoints` joins these same centres into 2-vertex polylines that SHARE their endpoints, so a junction can carry three roads or more. This ring stays where it is because the stages are supersets: stages 3 and 4 were measured against it, and retuning it would move them both. Staging works because a node's seed is hashCombine(graphSeed, hashString(nodeId)) — node-local, independent of where the node sits in the DAG — so every earlier stage reproduces bit-identically inside every later one.",tags:[`pipeline`,`staged`,`path`,`placement`]},Fo={title:`staged pipeline 4/5, edited — the full settlement with authored plots`,description:"`pipeline-4-detail.json` verbatim plus the same authored edit layer `pipeline-3-lots-edits.json` adds, so it is a superset of BOTH. It is the whole point of the arrangement: `terrain`, `boundary` and `districts` stay bit-identical to the unedited stage 4, while `lots`, `footprints`, `buildings` and everything downstream of them respond to the hand-placed geometry. An edit reaches exactly as far as the dependency graph says it does, and no further.",tags:[`pipeline`,`staged`,`edits`,`spawn`]},Io={title:`staged pipeline 4/5 — buildings, wall posts and forest`,description:"Stages 1-3 verbatim, plus everything that gets drawn. ADDS `buildings` (one asset per lot, chosen per point and spawned), `props` (posts spaced every 6 units along the boundary wall and turned to follow it) and `vegetation` (plantable scatter on the terrain, cut to the ground outside the wall, yawed at random and split into species batches). Every earlier output — `terrain`, `boundary`, `districts`, `lots`, `footprints` — is bit-identical to the stage that introduced it. Staging works because a node's seed is hashCombine(graphSeed, hashString(nodeId)) — node-local, independent of where the node sits in the DAG — so every earlier stage reproduces bit-identically inside every later one.",tags:[`pipeline`,`staged`,`spawn`,`instancing`]},Lo={title:`staged pipeline 5/5, edited — the settlement, its roads and authored plots`,description:"`pipeline-5-roads.json` verbatim plus the same authored edit layer `pipeline-3-lots-edits.json` adds, so it is a superset of BOTH. The road net is built from the district centres, upstream of the `edits` slot, so `roads` and `lamps` join `terrain`, `boundary` and `districts` on the list of outputs an edit provably cannot reach — while `lots`, `footprints`, `buildings` and everything downstream of them respond to the hand-placed geometry. An edit reaches exactly as far as the dependency graph says it does, and no further.",tags:[`pipeline`,`staged`,`edits`,`network`]},Ro={title:`staged pipeline 5/5 — a road network between the district centres`,description:"Stages 1-4 verbatim, plus the roads. ADDS `roads`, a genuine NETWORK rather than a tour: `connectPoints` in `relativeNeighborhood` mode joins the district centres into one 2-vertex `polyline` primitive per edge over the SAME points, so a centre that three roads meet at is one point of degree 3 — which `pointsToPath` cannot express, since it gives every point exactly one group (that is what the stage-3 `spine` is, and why it is a ring and not a net). The lune test is LOCAL — a pair is joined unless some third point is closer to both ends — so unlike a minimum spanning tree it survives partitioning, and it still CONTAINS one, so the net stays connected while keeping the cycles a road layout wants. Per-edge values need no edge domain: `roadWeight` is a centrality written on the centres (1 at the middle, 0 at the 63-unit rim), `roadClass` promotes it point→primitive with `min` so a road is only as wide as its weaker end, `roadKind` carries the district's name across with `first`, `roadWidth` is a field on the PRIMITIVE domain reading the promoted value, and `roadJunction` makes the return trip primitive→point with `max` so every centre learns the width of the widest road that reaches it. `place/along-curve` then walks the whole net at once — each edge is measured on its own length — and spawns `lamps`. Every earlier output — `terrain`, `boundary`, `districts`, `lots`, `footprints`, `buildings`, `props`, `vegetation` — is bit-identical to the stage that introduced it. Staging works because a node's seed is hashCombine(graphSeed, hashString(nodeId)) — node-local, independent of where the node sits in the DAG — so every earlier stage reproduces bit-identically inside every later one.",tags:[`pipeline`,`staged`,`network`,`topology`]},zo=[`basics-`,`examples-`,`pipeline-`],Bo=Object.assign({"../graphs/basics-attribute-from-noise.json":wa,"../graphs/basics-attribute-remap.json":Ta,"../graphs/basics-compose-primitives.json":Ea,"../graphs/basics-connect-by-reach.json":Da,"../graphs/basics-copy-to-points.json":Oa,"../graphs/basics-curve-shaping.json":ka,"../graphs/basics-density-along-a-path.json":Aa,"../graphs/basics-even-spacing.json":ja,"../graphs/basics-extrude-polygon.json":Ma,"../graphs/basics-field-params.json":Na,"../graphs/basics-field-shaping.json":Pa,"../graphs/basics-filter-by-attribute.json":Fa,"../graphs/basics-filter-by-density.json":Ia,"../graphs/basics-filter-by-expression.json":La,"../graphs/basics-filter-primitives-by-attribute.json":Ra,"../graphs/basics-flatten-and-remember.json":za,"../graphs/basics-foreach-per-group.json":Ba,"../graphs/basics-gather-on-path.json":Va,"../graphs/basics-inline-field-params.json":Ha,"../graphs/basics-jitter-points.json":Ua,"../graphs/basics-mask-by-species.json":Wa,"../graphs/basics-merge-points.json":Ga,"../graphs/basics-merge-primitives.json":Ka,"../graphs/basics-mesh-primitive.json":qa,"../graphs/basics-neighborhood-count.json":Ja,"../graphs/basics-neighborhood-varying-radius.json":Ya,"../graphs/basics-orient-along-path.json":Xa,"../graphs/basics-orient-along-vector.json":Za,"../graphs/basics-partition-by-attribute.json":Qa,"../graphs/basics-path-resample.json":$a,"../graphs/basics-path-segments.json":eo,"../graphs/basics-paths-by-group.json":to,"../graphs/basics-point-grid.json":no,"../graphs/basics-points-to-path.json":ro,"../graphs/basics-primitive-ref.json":io,"../graphs/basics-promote-attribute.json":ao,"../graphs/basics-props-along-a-path.json":oo,"../graphs/basics-radial-on-curve.json":so,"../graphs/basics-report-to-the-host.json":co,"../graphs/basics-reseed-a-noise.json":lo,"../graphs/basics-runs-along-a-path.json":uo,"../graphs/basics-scatter-in-bounds.json":fo,"../graphs/basics-scatter-in-world.json":po,"../graphs/basics-signed-distance.json":mo,"../graphs/basics-spawn-by-species.json":ho,"../graphs/basics-spawn-instances.json":go,"../graphs/basics-subgraph-exposed-params.json":_o,"../graphs/basics-surface-sample.json":vo,"../graphs/basics-sweep-profile.json":yo,"../graphs/basics-tiling-a-field.json":bo,"../graphs/basics-transfer-attribute.json":xo,"../graphs/basics-transform-points.json":So,"../graphs/basics-two-kinds-of-bounds.json":Co,"../graphs/basics-volume-scatter.json":wo,"../graphs/examples-forest.json":To,"../graphs/examples-gpu-fields.json":Eo,"../graphs/examples-headless-scatter.json":Do,"../graphs/examples-rig.json":Oo,"../graphs/examples-riverbank.json":ko,"../graphs/examples-streamed-terrain.json":Ao,"../graphs/pipeline-1-boundary.json":jo,"../graphs/pipeline-2-districts.json":Mo,"../graphs/pipeline-3-lots-edits.json":No,"../graphs/pipeline-3-lots.json":Po,"../graphs/pipeline-4-detail-edits.json":Fo,"../graphs/pipeline-4-detail.json":Io,"../graphs/pipeline-5-roads-edits.json":Lo,"../graphs/pipeline-5-roads.json":Ro}),Vo=Object.assign({"../graphs/basics-attribute-from-noise.json":()=>X(()=>import(`./basics-attribute-from-noise-Bh0XJGTO.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-attribute-remap.json":()=>X(()=>import(`./basics-attribute-remap-DGiahiRj.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-compose-primitives.json":()=>X(()=>import(`./basics-compose-primitives-Cp50AqDH.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-connect-by-reach.json":()=>X(()=>import(`./basics-connect-by-reach-DFsTUTdg.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-copy-to-points.json":()=>X(()=>import(`./basics-copy-to-points-C2tE3VlI.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-curve-shaping.json":()=>X(()=>import(`./basics-curve-shaping-C6Z4YzPU.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-density-along-a-path.json":()=>X(()=>import(`./basics-density-along-a-path-yBM7WSxT.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-even-spacing.json":()=>X(()=>import(`./basics-even-spacing-DgRK43R6.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-extrude-polygon.json":()=>X(()=>import(`./basics-extrude-polygon-Dzf9Oui8.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-field-params.json":()=>X(()=>import(`./basics-field-params-DELIN9_q.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-field-shaping.json":()=>X(()=>import(`./basics-field-shaping-EteMruo3.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-filter-by-attribute.json":()=>X(()=>import(`./basics-filter-by-attribute-BJ9y4CyU.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-filter-by-density.json":()=>X(()=>import(`./basics-filter-by-density-XLia_b7l.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-filter-by-expression.json":()=>X(()=>import(`./basics-filter-by-expression-N16UfhvL.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-filter-primitives-by-attribute.json":()=>X(()=>import(`./basics-filter-primitives-by-attribute-DpBVhXgB.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-flatten-and-remember.json":()=>X(()=>import(`./basics-flatten-and-remember-BBmdWLWT.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-foreach-per-group.json":()=>X(()=>import(`./basics-foreach-per-group-D1XhrZTn.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-gather-on-path.json":()=>X(()=>import(`./basics-gather-on-path-CHRSm7dj.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-inline-field-params.json":()=>X(()=>import(`./basics-inline-field-params-BO1hGRnV.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-jitter-points.json":()=>X(()=>import(`./basics-jitter-points-CKtixWHH.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-mask-by-species.json":()=>X(()=>import(`./basics-mask-by-species-l4yOJtVS.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-merge-points.json":()=>X(()=>import(`./basics-merge-points-Cwj1wryx.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-merge-primitives.json":()=>X(()=>import(`./basics-merge-primitives-CD-2oecb.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-mesh-primitive.json":()=>X(()=>import(`./basics-mesh-primitive-Cq-k6rZ9.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-neighborhood-count.json":()=>X(()=>import(`./basics-neighborhood-count-BkdhJ9MW.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-neighborhood-varying-radius.json":()=>X(()=>import(`./basics-neighborhood-varying-radius-DHkLl3r3.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-orient-along-path.json":()=>X(()=>import(`./basics-orient-along-path-BEDwI4Kq.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-orient-along-vector.json":()=>X(()=>import(`./basics-orient-along-vector-Dp1CD2wH.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-partition-by-attribute.json":()=>X(()=>import(`./basics-partition-by-attribute-CjTLCLVl.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-path-resample.json":()=>X(()=>import(`./basics-path-resample-C83J8FkR.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-path-segments.json":()=>X(()=>import(`./basics-path-segments-Q7DnLI4R.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-paths-by-group.json":()=>X(()=>import(`./basics-paths-by-group-HZqWrgu4.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-point-grid.json":()=>X(()=>import(`./basics-point-grid-BeaqvsHg.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-points-to-path.json":()=>X(()=>import(`./basics-points-to-path-Cy-un81t.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-primitive-ref.json":()=>X(()=>import(`./basics-primitive-ref-Ck1lA9Gh.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-promote-attribute.json":()=>X(()=>import(`./basics-promote-attribute-BRDalZxO.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-props-along-a-path.json":()=>X(()=>import(`./basics-props-along-a-path-DF9k7Sek.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-radial-on-curve.json":()=>X(()=>import(`./basics-radial-on-curve-DZ1pVHO6.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-report-to-the-host.json":()=>X(()=>import(`./basics-report-to-the-host-DLwh6_3R.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-reseed-a-noise.json":()=>X(()=>import(`./basics-reseed-a-noise-BBhhpoNl.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-runs-along-a-path.json":()=>X(()=>import(`./basics-runs-along-a-path-DoAYWrMq.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-scatter-in-bounds.json":()=>X(()=>import(`./basics-scatter-in-bounds-12m33xU3.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-scatter-in-world.json":()=>X(()=>import(`./basics-scatter-in-world-CQt03_Tc.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-signed-distance.json":()=>X(()=>import(`./basics-signed-distance-K6NlpZbZ.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-spawn-by-species.json":()=>X(()=>import(`./basics-spawn-by-species-Bpk53OKX.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-spawn-instances.json":()=>X(()=>import(`./basics-spawn-instances-B2FmTgOj.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-subgraph-exposed-params.json":()=>X(()=>import(`./basics-subgraph-exposed-params-enCUId_O.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-surface-sample.json":()=>X(()=>import(`./basics-surface-sample-Bmi2sYQ9.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-sweep-profile.json":()=>X(()=>import(`./basics-sweep-profile-Dd__Xn_u.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-tiling-a-field.json":()=>X(()=>import(`./basics-tiling-a-field-Czeq8IYS.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-transfer-attribute.json":()=>X(()=>import(`./basics-transfer-attribute-LLFqeMS-.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-transform-points.json":()=>X(()=>import(`./basics-transform-points-Car40RHc.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-two-kinds-of-bounds.json":()=>X(()=>import(`./basics-two-kinds-of-bounds-D32vou_t.js`).then(e=>e.default),[],import.meta.url),"../graphs/basics-volume-scatter.json":()=>X(()=>import(`./basics-volume-scatter-DaPV0Vs7.js`).then(e=>e.default),[],import.meta.url),"../graphs/examples-forest.json":()=>X(()=>import(`./examples-forest-BeHEgA8-.js`).then(e=>e.default),[],import.meta.url),"../graphs/examples-gpu-fields.json":()=>X(()=>import(`./examples-gpu-fields-BOW_WtYS.js`).then(e=>e.default),[],import.meta.url),"../graphs/examples-headless-scatter.json":()=>X(()=>import(`./examples-headless-scatter-7n4CP4AS.js`).then(e=>e.default),[],import.meta.url),"../graphs/examples-rig.json":()=>X(()=>import(`./examples-rig-Rvp2uGlD.js`).then(e=>e.default),[],import.meta.url),"../graphs/examples-riverbank.json":()=>X(()=>import(`./examples-riverbank-C_DbQVEq.js`).then(e=>e.default),[],import.meta.url),"../graphs/examples-streamed-terrain.json":()=>X(()=>import(`./examples-streamed-terrain-DEXm763A.js`).then(e=>e.default),[],import.meta.url),"../graphs/pipeline-1-boundary.json":()=>X(()=>import(`./pipeline-1-boundary-uarZRTnP.js`).then(e=>e.default),[],import.meta.url),"../graphs/pipeline-2-districts.json":()=>X(()=>import(`./pipeline-2-districts-Zes--LAm.js`).then(e=>e.default),[],import.meta.url),"../graphs/pipeline-3-lots-edits.json":()=>X(()=>import(`./pipeline-3-lots-edits-CXIjtum-.js`).then(e=>e.default),[],import.meta.url),"../graphs/pipeline-3-lots.json":()=>X(()=>import(`./pipeline-3-lots-CFkeBrc4.js`).then(e=>e.default),[],import.meta.url),"../graphs/pipeline-4-detail-edits.json":()=>X(()=>import(`./pipeline-4-detail-edits-DLPH-55M.js`).then(e=>e.default),[],import.meta.url),"../graphs/pipeline-4-detail.json":()=>X(()=>import(`./pipeline-4-detail-CMv6nWi7.js`).then(e=>e.default),[],import.meta.url),"../graphs/pipeline-5-roads-edits.json":()=>X(()=>import(`./pipeline-5-roads-edits-DVCmkcmC.js`).then(e=>e.default),[],import.meta.url),"../graphs/pipeline-5-roads.json":()=>X(()=>import(`./pipeline-5-roads-BMCaSYbH.js`).then(e=>e.default),[],import.meta.url)}),Ho=`../graphs/`,Uo=`.json`,Wo=e=>e.slice(10,-5),Go=Object.keys(Bo).filter(e=>zo.some(t=>Wo(e).startsWith(t))).sort().map(e=>{let t=Wo(e),n=Bo[e],r=t.indexOf(`-`);return{name:t,title:n?.title??t,description:n?.description??``,group:r<0?t:t.slice(0,r)}}),Ko=[...new Set(Go.map(e=>e.group))];function qo(e){return Go.find(t=>t.name===e)}async function Jo(e){let t=Vo[`${Ho}${e}${Uo}`];if(t===void 0)throw Error(`no graph named "${e}" in graphs/. Known: ${Go.map(e=>e.name).join(`, `)}`);return await t()}var Yo=(e,t=je)=>{var n=Xo(),r=V(n);q(n),O(()=>{J(n,`viewBox`,Et),J(r,`d`,t())}),_(e,n)},Xo=I(`<svg class="ic svelte-z96dgh" aria-hidden="true"><path></path></svg>`),Zo=k(`<option> </option>`),Qo=k(`<optgroup></optgroup>`),$o=k(`<i class="svelte-z96dgh"> </i> <b class="svelte-z96dgh"> </b>`,1),es=k(` <i class="svelte-z96dgh"> </i>`,1),ts=k(`<b class="svelte-z96dgh"> </b><!>`,1),ns=k(`<span><!></span> `,1),rs=k('<div><span class="title svelte-z96dgh" role="button" tabindex="0"><svg class="mark svelte-z96dgh" aria-hidden="true" focusable="false"></svg>Editor<span>▾</span></span> <div class="grp on-bar svelte-z96dgh"><label class="graph on-bar svelte-z96dgh">graph <select class="svelte-z96dgh"><option>starter graph</option><!></select></label> <label class="svelte-z96dgh">seed <input type="number" step="1" min="0" class="svelte-z96dgh"/></label> <button aria-label="export" title="export — serializeGraph → JSON" class="svelte-z96dgh"><!></button> <button aria-label="import" title="import — paste JSON → deserializeGraph" class="svelte-z96dgh"><!></button></div> <div class="grp on-bar svelte-z96dgh" title="the render"><button aria-label="scene" title="scene — the render. Turning it off leaves the graph alone on screen, never nothing. Hold shift over `scene + graph` to fly the scene through the graph."><!></button> <button aria-label="frame"><!></button> <label><select aria-label="shade" class="svelte-z96dgh"><option>lit</option><option>normals</option></select></label> <label><input type="range" min="0" step="0.05" aria-label="how far the scene is pushed back so the graph can be read over it" class="svelte-z96dgh"/></label></div> <div class="grp on-bar svelte-z96dgh" title="the node canvas"><button aria-label="graph" title="graph — the node canvas. Turning it off leaves the render alone on screen, never nothing. Space cycles the three views, shift-space goes back."><!></button>  <span class="pill svelte-z96dgh"><button aria-label="fit"><!></button> <button aria-label="100%, actual size"><!></button></span> <button aria-label="layout"><!></button></div> <label>cook <select class="svelte-z96dgh"></select></label> <span></span></div>');function is(e,t){we(t,!0);let n=K(()=>Ko.map(e=>({group:e,items:Go.filter(t=>t.group===e)}))),r=K(()=>t.legibilityApplies?`graph legibility — how far the scene is pushed back so the graph can be read over it. At 0 the render is untouched; by the top of the range a cable is traceable from its output pin to its input across a dense one. The node boxes were never the problem; the wires are. An overlay redraw: it changes nothing about the render, and nothing about the cook.`:"graph legibility — only in `scene + graph`. The scene view has no graph to read, and the graph view has no render left to push back.");function i(e){return e.toLocaleString(`en-US`).replaceAll(`,`,` `)}let a=K(()=>{let e=t.status?.gpu;if(!e)return[];let{real:n,partial:r}=Mn(e.fallbacks),a=Nn(n);return[{value:i(e.dispatches),label:`disp`},{value:`${e.residentRuns} / ${e.fusedNodes}`,label:`run / fused`},{value:i(e.readbacksSaved),label:`readbacks saved`},...r>0?[{value:`${r}×`,label:`suffix-fused`}]:[],...a===`none`?[]:[{value:a,label:`fell back`,tone:`warn`}]]}),o=K(()=>t.status===null?[{value:`cooking…`,label:``}]:[{value:t.host.fps,label:`fps`},{value:`${t.status.elapsedMs.toFixed(1)} ms`,label:`cook`,labelFirst:!0},{value:`${i(t.status.cooked)} / ${i(t.status.cached)}`,label:`cooked / cached`},{value:i(t.status.outputs),label:`out`},{value:i(t.status.points),label:`pts`},{value:i(t.status.instances),label:`inst`},{value:t.host.drew,label:`drew`,labelFirst:!0},...L(a),...t.status.errors.length>0?[{value:String(t.status.errors.length),label:`error(s)`,tone:`err`}]:[],{value:t.status.hash,label:`hash`,labelFirst:!0}]),s=K(()=>t.host.gpu.error===null?t.host.gpu.ready?`${t.host.gpu.label} — the two device paths agree bit for bit, so the hash holds across them. The CPU hash differs: GPU floats are not byte-identical to CPU floats.`:`probing for a WebGPU adapter…`:`no device — ${t.host.gpu.error}`);function c(e){let n=Math.floor(e.currentTarget.valueAsNumber);Number.isFinite(n)&&t.onSeed(n>>>0)}function l(e){(e.key===`Enter`||e.key===` `)&&(e.preventDefault(),t.onToggle())}var u=rs();let d;var p=V(u),m=V(p);f(m,()=>pt,!0),q(m);var h=H(m,2);let g;q(p);var v=H(p,2),y=V(v),b=H(V(y)),x=V(b);x.value=x.__value=``;var S=H(x);F(S,17,()=>L(n),({group:e,items:t})=>e,(e,t,n,r)=>{let i=()=>L(t).group,a=()=>L(t).items;var o=Qo();F(o,21,a,e=>e.name,(e,t)=>{var n=Zo(),r=V(n,!0);q(n);var i={};O(()=>{J(n,`title`,L(t).description),P(r,L(t).title),i!==(i=L(t).name)&&(n.value=(n.__value=L(t).name)??``)}),_(e,n)}),q(o),O(()=>J(o,`label`,i())),_(e,o)}),q(b);var C;Tt(b),q(y);var w=H(y,2),T=H(V(w));U(T),q(w);var E=H(w,2);Yo(V(E),()=>Ee.export),q(E);var D=H(E,2);Yo(V(D),()=>Ee.import),q(D),q(v);var k=H(v,2),A=V(k);let j;Yo(V(A),()=>Ee.scene),q(A);var M=H(A,2);let N;Yo(V(M),()=>Ee.frame),q(M);var ee=H(M,2);let te;var ne=V(ee),re=V(ne);re.value=re.__value=`lit`;var ie=H(re);ie.value=ie.__value=`normals`,q(ne);var ae;Tt(ne),q(ee);var oe=H(ee,2);let se;var I=V(oe);U(I),J(I,`max`,.9),q(oe),q(k);var ce=H(k,2),le=V(ce);let de;Yo(V(le),()=>Ee.graph),q(le);var fe=H(le,2),pe=V(fe);let me;Yo(V(pe),()=>Ee.fit),q(pe);var he=H(pe,2);let ge;Yo(V(he),()=>Ee.actual),q(he),q(fe);var z=H(fe,2);let _e;Yo(V(z),()=>Ee.layout),q(z),q(ce);var ve=H(ce,2);let ye;var be=H(V(ve));F(be,20,()=>kn,e=>e,(e,n)=>{var r=Zo(),i=V(r,!0);q(r);var a={};O(e=>{r.disabled=n!==`cpu`&&!t.host.gpu.ready,P(i,e),a!==(a=n)&&(r.value=(r.__value=n)??``)},[()=>An(n)]),_(e,r)}),q(be);var xe;Tt(be),q(ve);var Se=H(ve,2);let Ce;F(Se,21,()=>L(o),e=>e.label+e.value,(e,t)=>{var n=ns(),r=B(n),i=V(r),a=e=>{var n=$o(),r=B(n),i=V(r,!0);q(r);var a=H(r,1,!0);a.nodeValue=` `;var o=H(a),s=V(o,!0);q(o),O(()=>{P(i,L(t).label),P(s,L(t).value)}),_(e,n)},o=e=>{var n=ts(),r=B(n),i=V(r,!0);q(r);var a=H(r),o=e=>{var n=es(),r=B(n,!0);r.nodeValue=` `;var i=H(r),a=V(i,!0);q(i),O(()=>P(a,L(t).label)),_(e,n)};G(a,e=>{L(t).label!==``&&e(o)}),O(()=>P(i,L(t).value)),_(e,n)};G(i,e=>{L(t).labelFirst?e(a):e(o,-1)}),q(r);var s=H(r,1,!0);s.nodeValue=` `,O(()=>Y(r,1,`stat ${L(t).tone??``??``}`,`svelte-z96dgh`)),_(e,n)}),q(Se),q(u),O(()=>{d=Y(u,1,`toolbar svelte-z96dgh`,null,d,{collapsed:t.collapsed}),J(p,`aria-expanded`,!t.collapsed),J(m,`viewBox`,tt),g=Y(h,1,`chevron svelte-z96dgh`,null,g,{flip:t.collapsed}),C!==(C=t.preset)&&(b.value=(b.__value=t.preset)??``,ue(b,t.preset)),Oe(T,t.seed),j=Y(A,1,`view scene on-bar svelte-z96dgh`,null,j,{on:t.sceneOn}),J(A,`aria-pressed`,t.sceneOn),M.disabled=!t.sceneOn,J(M,`title`,t.sceneOn?`frame — point the camera at what the graph made (F) — done automatically whenever a graph loads`:`frame — only while the scene is on. There is no camera to point at anything you can see.`),N=Y(M,1,`svelte-z96dgh`,null,N,{off:!t.sceneOn}),te=Y(ee,1,`path shade svelte-z96dgh`,null,te,{off:!t.sceneOn}),J(ee,`title`,t.sceneOn?"how the geometry is shaded — normals read volume and overlap where a single key light flattens them into one silhouette. A redraw, not a recook; vertex colours only show under `lit`.":`shade — only while the scene is on. Nothing is being drawn for it to decide the look of.`),ne.disabled=!t.sceneOn,ae!==(ae=t.host.shading)&&(ne.value=(ne.__value=t.host.shading)??``,ue(ne,t.host.shading)),se=Y(oe,1,`legibility svelte-z96dgh`,null,se,{off:!t.legibilityApplies}),J(oe,`title`,L(r)),Oe(I,t.legibility),I.disabled=!t.legibilityApplies,de=Y(le,1,`view graph on-bar svelte-z96dgh`,null,de,{on:t.graphOn}),J(le,`aria-pressed`,t.graphOn),pe.disabled=!t.graphOn,J(pe,`title`,t.graphOn?`fit — zoom out until every node is on screen, however small. The canvas pans with the right button and zooms on the wheel`:`fit — only while the graph is on. There is no canvas on screen to zoom.`),me=Y(pe,1,`svelte-z96dgh`,null,me,{off:!t.graphOn}),he.disabled=!t.graphOn,J(he,`title`,t.graphOn?`100% — actual size: back to 1:1, centred on the graph (ctrl+0) — the zoom a graph opens at whenever it fits there`:`100% — only while the graph is on. There is no canvas on screen to zoom.`),ge=Y(he,1,`svelte-z96dgh`,null,ge,{off:!t.graphOn}),z.disabled=!t.graphOn,J(z,`title`,t.graphOn?`layout — re-run the deterministic topological layout`:`layout — only while the graph is on. The nodes would be rearranged out of sight.`),_e=Y(z,1,`svelte-z96dgh`,null,_e,{off:!t.graphOn}),ye=Y(ve,1,`path cook grp svelte-z96dgh`,null,ye,{off:!t.host.gpu.ready}),J(ve,`title`,L(s)),xe!==(xe=t.host.gpu.path)&&(be.value=(be.__value=t.host.gpu.path)??``,ue(be,t.host.gpu.path)),Ce=Y(Se,1,`status svelte-z96dgh`,null,Ce,{err:t.status!==null&&t.status.errors.length>0})}),R(`click`,p,function(...e){t.onToggle?.apply(this,e)}),R(`keydown`,p,l),R(`change`,b,e=>t.onPreset(e.currentTarget.value)),R(`change`,T,c),R(`click`,E,function(...e){t.onExport?.apply(this,e)}),R(`click`,D,function(...e){t.onImport?.apply(this,e)}),R(`click`,A,()=>t.onToggleLayer(`scene`)),R(`click`,M,function(...e){t.onFrame?.apply(this,e)}),R(`change`,ne,e=>t.onShading(e.currentTarget.value)),R(`input`,I,e=>t.onLegibility(e.currentTarget.valueAsNumber)),R(`click`,le,()=>t.onToggleLayer(`graph`)),R(`click`,pe,function(...e){t.onFit?.apply(this,e)}),R(`click`,he,function(...e){t.onActual?.apply(this,e)}),R(`click`,z,function(...e){t.onLayout?.apply(this,e)}),R(`change`,be,e=>t.onCookPath(e.currentTarget.value)),_(e,u),We()}o([`click`,`keydown`,`change`,`input`]);var as=k(`<div> </div>`),os=k(`<div class="cook-errors svelte-ijxgj5"></div>`),ss=k(`<div><!></div>`),cs=k(`<div class="scrim svelte-ijxgj5" aria-hidden="true"></div> <div><!> <!> <div class="body svelte-ijxgj5"><div><!></div> <div><!></div> <!> <button class="drawer-tab right svelte-ijxgj5" aria-label="toggle the param drawer">params</button></div> <!> <!></div> <!>`,1);function ls(e,n){we(n,!0);let r=et(n,`bridge`,7),i=W(oe({fps:`–`,drew:`–`,gpu:{path:`cpu`,ready:!1,label:``,error:null},shading:`lit`}));b(()=>{r().publish=e=>t(i,e,!0)});let a=[{id:`scene`,scene:!0,graph:!1},{id:`both`,scene:!0,graph:!0},{id:`graph`,scene:!1,graph:!0}],o=W(!1),s=W(1),c=K(()=>a[L(s)]),u=K(()=>L(o)&&L(c).id===`both`),d=W(0),f=K(()=>L(c).id===`both`),p=K(()=>L(f)?L(d):+!L(c).scene);function m(e=1){t(s,(L(s)+e+a.length)%a.length)}function h(e){let n=e===`scene`?`graph`:`scene`;t(s,a.findIndex(t=>t.id===(L(c)[e]?n:`both`)),!0)}let g=`basics-compose-primitives`,v=W(oe({seed:1,nodes:[],edges:[]})),y=W(null),x=W(null),C=W(0),w=W(null),E=W(null),D=W(``),k=W(null),A=W(!1),j=W(`imported`),M=W(null),N=W(``),ee=W(void 0),te=W(oe({})),ne=W(0),re=null,ie=K(()=>L(N)===``?`starter graph`:qo(L(N))?.title??L(N)),ae=Kn(),se=K(()=>L(v).nodes.find(e=>e.id===L(y))??null),I=K(()=>(L(C),new Map(L(v).nodes.map(e=>[e.id,Gn(n.controller.paramViews(e.id,e.type))])))),le=W(void 0),ue=W(null),fe={x:0,y:0};lt(()=>{L(c).graph?ye():t(ue,null)});let pe=W(oe(Ce().matches)),me=W(!1);function he(e){t(y,e,!0)}lt(()=>{let e=Ce(),n=e=>{t(pe,e.matches,!0),e.matches||t(me,!1)};return e.addEventListener(`change`,n),()=>e.removeEventListener(`change`,n)});let ge;function z(e,n=`info`){t(w,{text:e,kind:n},!0),ge!==void 0&&clearTimeout(ge),ge=setTimeout(()=>t(w,null),n===`error`?7e3:3500)}ot(()=>{n.controller.setStatusListener(e=>{if(t(x,e,!0),L(A)){if(t(A,!1),L(M)!==null){z(L(M),`error`),t(M,null);return}let n=L(k);z(n===null?`${L(j)}: output hash ${e.hash}`:e.hash===n?`${L(j)}: output hash ${e.hash} — identical to the pre-import cook`:`${L(j)}: output hash ${e.hash} (was ${n})`)}});let e=new URLSearchParams(window.location.search),r=e.get(`p`);if(r!==null&&r!==``)try{let e=JSON.parse(r);if(typeof e!=`object`||!e||Array.isArray(e))throw Error(`expected a JSON object of knob values`);re=e}catch(e){t(M,`settings in the link could not be read (${e instanceof Error?e.message:String(e)})`)}let i=e.get(`graph`);if(i!==null&&i!==``)if(qo(i)===void 0)t(M,`no graph named "${i}" in the corpus — opened the default instead`);else{xe(i,{updateUrl:!1});return}if(qo(g)!==void 0){xe(g,{updateUrl:!1});return}ve()});function ve(){let e=n.controller.importText(Jn);if(`error`in e){z(e.error,`error`);return}t(v,e.structure,!0),he(null),mt(C),be(),ye()}function ye(){_e().then(()=>L(le)?.resetView({preferActual:!0}))}function be(){t(te,aa(n.controller.knobs()),!0),t(ne,L(v).seed,!0)}async function xe(e,n){if(e===``){t(N,``),t(ee,void 0),ve(),n.updateUrl&&Ee(``);return}let r,i;try{r=await Jo(e),i=await Zi(e)}catch(e){z(e instanceof Error?e.message:String(e),`error`);return}let a=Fe(r,qo(e)?.title??e);if(a!==null){z(a,`error`);return}if(t(N,e,!0),t(ee,i,!0),be(),re!==null){let e=re;re=null,U(e,`link`)}n.updateUrl&&Ee(e)}function U(e,r){let{applied:i,problems:a}=n.controller.applyKnobPatch(e);if(e.seed!==void 0&&typeof e.seed==`number`&&(L(v).seed=e.seed>>>0),mt(C),a.length===0)return;let o=`${r===`link`?`shared settings`:`reset`}: applied ${i}, could not apply ${a.length} — ${a.join(`; `)}`;L(A)?t(M,o):z(o,`error`)}function Se(){U({...L(te),seed:L(ne)},`reset`)}function Te(e){let t=new URL(window.location.href);return t.searchParams.delete(`graph`),t.searchParams.delete(`p`),L(N)!==``&&t.searchParams.set(`graph`,L(N)),Object.keys(e).length>0&&t.searchParams.set(`p`,JSON.stringify(e)),t.toString()}function Ee(e){let t=new URL(window.location.href);e===``?t.searchParams.delete(`graph`):t.searchParams.set(`graph`,e),window.history.replaceState(null,``,t)}function De(e){let t=qn(e,new Set(L(v).nodes.map(e=>e.id))),r=n.controller.addNode(t,e);if(`error`in r){z(r.error,`error`);return}let i=L(v).nodes.length,a=L(ue)===null?void 0:L(le)?.graphPointAt(L(ue).x,L(ue).y),o=ce(e);L(v).nodes.push({id:t,type:e,...o===void 0?{}:{category:o},x:a?Math.round(a.x):48+i%4*36,y:a?Math.round(a.y):40+i%6*32,inputs:r.inputs,outputs:r.outputs}),he(t),mt(C)}function Oe(e,t,n){let r=L(v).nodes.find(t=>t.id===e);r&&(r.x=t,r.y=n)}function ke(e){let t=n.controller.connectEdge(e);if(t){z(t,`error`);return}L(v).edges.push(e)}function Ae(e){let t=L(v).edges[e];if(!t)return;let r=n.controller.disconnectEdge({from:t.from,fromPin:t.fromPin,to:t.to,toPin:t.toPin});if(r){z(r,`error`);return}L(v).edges.splice(e,1)}function je(e){let t=n.controller.deleteNode(e);if(t){z(t,`error`);return}L(v).nodes=L(v).nodes.filter(t=>t.id!==e),L(v).edges=L(v).edges.filter(t=>t.from!==e&&t.to!==e),L(y)===e&&he(null),mt(C)}function Me(e){L(v).seed=e>>>0,n.controller.setSeed(L(v).seed)}function Ne(){de(L(v).nodes,L(v).edges,new Map([...L(I)].map(([e,t])=>[e,t.length]))),ye()}function Pe(){t(D,n.controller.exportText(),!0),t(E,`export`)}function Fe(e,r=`imported`){t(k,L(x)?.hash??null,!0);let i=n.controller.importText(e);return`error`in i?i.error:(t(v,i.structure,!0),he(null),t(j,r,!0),t(A,!0),mt(C),ye(),null)}function Ie(e){let n=Fe(e);return n===null?(t(N,``),t(ee,void 0),be(),Ee(``),null):n}function Le(e,t,r){let i=n.controller.setPlainParam(e,t,r);return mt(C),i}function Re(e,t,r){let i=n.controller.applyFieldParam(e,t,r);return i===null&&mt(C),i}function ze(e){return e!==null&&(e.tagName===`INPUT`||e.tagName===`TEXTAREA`||e.tagName===`SELECT`||e.isContentEditable)}function Be(e){let n=e.target;if(e.key===` `&&!e.ctrlKey&&!e.altKey&&!e.metaKey){if(L(E)!==null||ze(n)||n?.tagName===`BUTTON`)return;e.preventDefault(),m(e.shiftKey?-1:1);return}if(e.key===`Tab`&&!e.ctrlKey&&!e.altKey&&!e.metaKey){if(L(E)!==null||ze(n)||!L(c).graph)return;e.preventDefault(),t(ue,L(ue)===null?{...fe}:null,!0);return}if((e.key===`f`||e.key===`F`)&&!e.ctrlKey&&!e.altKey&&!e.metaKey){if(L(E)!==null||ze(n))return;e.preventDefault(),r().frame?.();return}if(e.key===`0`&&(e.ctrlKey||e.metaKey)&&!e.altKey){if(L(E)!==null||!L(c).graph)return;e.preventDefault(),L(le)?.actualSize();return}(e.key===`Delete`||e.key===`Backspace`)&&L(E)===null&&(ze(n)||L(y)!==null&&(e.preventDefault(),je(L(y))))}var Ve=cs();S(`keydown`,T,e=>{t(o,e.shiftKey,!0),Be(e)}),S(`keyup`,T,e=>t(o,e.shiftKey,!0)),S(`blur`,T,()=>t(o,!1)),S(`pointermove`,T,e=>{t(o,e.shiftKey,!0),fe.x=e.clientX,fe.y=e.clientY});var Ue=B(Ve),Ge=H(Ue,2);let Ke;var qe=V(Ge);is(qe,{get seed(){return L(v).seed},get status(){return L(x)},get collapsed(){return L(pe)},get preset(){return L(N)},onPreset:e=>void xe(e,{updateUrl:!0}),onSeed:Me,onExport:Pe,onImport:()=>t(E,`import`),onLayout:Ne,onFit:()=>L(le)?.resetView(),onActual:()=>L(le)?.actualSize(),onFrame:()=>r().frame?.(),onCookPath:e=>r().setCookPath?.(e),onShading:e=>r().setShading?.(e),get legibility(){return L(d)},get legibilityApplies(){return L(f)},onLegibility:e=>t(d,e,!0),get sceneOn(){return L(c).scene},get graphOn(){return L(c).graph},onToggleLayer:h,get host(){return L(i)},onToggle:()=>{t(pe,!L(pe)),L(c).graph&&Ce().matches&&ye()}});var Je=H(qe,2),Ye=e=>{var t=os();F(t,21,()=>L(x).errors,Xe,(e,t)=>{var n=as(),r=V(n,!0);q(n),O(()=>P(r,L(t))),_(e,n)}),q(t),_(e,t)};G(Je,e=>{L(x)&&L(x).errors.length>0&&e(Ye)});var Ze=H(Je,2),Qe=V(Ze);let $e;var tt=V(Qe);He(lr(tt,{get model(){return L(v)},get selectedId(){return L(y)},get previews(){return L(I)},onSelect:he,onMove:Oe,onConnect:ke,onDeleteEdge:Ae}),e=>t(le,e,!0),()=>L(le)),q(Qe);var nt=H(Qe,2);let rt;_a(V(nt),{get controller(){return n.controller},get rev(){return L(C)},get spec(){return L(ee)},get title(){return L(ie)},get baseline(){return L(te)},get seed(){return L(v).seed},get loadedSeed(){return L(ne)},onEdit:()=>mt(C),onReset:Se,shareUrl:Te}),q(nt);var it=H(nt,2),at=e=>{var t=ss();let r;mi(V(t),{get controller(){return n.controller},get node(){return L(se)},get paramsRev(){return L(C)},onPlain:Le,onFieldApply:Re,onDelete:je}),q(t),O(()=>r=Y(t,1,`panel node svelte-ijxgj5`,null,r,{open:L(me)})),_(e,t)};G(it,e=>{L(se)&&e(at)});var st=H(it,2);q(Ze);var ct=H(Ze,2);Ca(ct,{get groups(){return ae},get at(){return L(ue)},onAdd:De,onDismiss:()=>t(ue,null)});var ut=H(ct,2),dt=e=>{var t=as(),n=V(t,!0);q(t),O(()=>{Y(t,1,`toast ${L(w).kind??``}`,`svelte-ijxgj5`),P(n,L(w).text)}),_(e,t)};G(ut,e=>{L(w)&&e(dt)}),q(Ge);var ft=H(Ge,2),pt=e=>{yi(e,{title:`export — serializeGraph JSON`,get initial(){return L(D)},mode:`export`,onClose:()=>t(E,null)})},ht=e=>{yi(e,{title:`import — paste serialized graph JSON`,initial:``,mode:`import`,onApply:Ie,onClose:()=>t(E,null)})};G(ft,e=>{L(E)===`export`?e(pt):L(E)===`import`&&e(ht,1)}),O(()=>{l(Ue,`opacity: ${L(p)??``}`),Ke=Y(Ge,1,`editor svelte-ijxgj5`,null,Ke,{collapsed:L(pe),bare:!L(c).graph}),J(Ze,`hidden`,!L(c).graph),$e=Y(Qe,1,`canvas-wrap svelte-ijxgj5`,null,$e,{through:L(u)}),rt=Y(nt,1,`panel graph svelte-ijxgj5`,null,rt,{open:L(me)}),J(st,`aria-expanded`,L(me))}),R(`click`,st,()=>t(me,!L(me))),_(e,Ve),We()}o([`click`]);var{scene:us,camera:ds,controls:fs,start:ps}=jt({cameraPosition:[20,15,20],target:[0,2,0],background:0,lights:{sky:16777215,ground:2763306,sun:16777215}}),ms=new Be(30,30,3815994,2368548);us.add(ms);var hs=``;function gs(e){let t=`${e.size}/${e.divisions}`;t!==hs&&(hs=t,us.remove(ms),ms.dispose(),ms=new Be(e.size,e.divisions,3815994,2368548),us.add(ms)),ms.position.set(e.x,e.y+e.lift,e.z)}var _s=Cn(),vs=new Se;us.add(vs);var ys=[],bs=[9676740,14853191,7258784,13601732,13791327,9418826,5879503,10720984],xs=16757844,Ss=e=>{if(e%bs.length===0)return xs;let t=bs[e%bs.length],n=e=>Math.round((t>>e&255)+(255-(t>>e&255))*.45);return n(16)<<16|n(8)<<8|n(0)},Cs=e=>({mesh:t=>new Ge({color:bs[e%bs.length],roughness:.85,metalness:0,vertexColors:t}),line:t=>new ie({color:Ss(e),vertexColors:t})}),ws={mesh:()=>new ht,line:e=>new ie({color:13620964,vertexColors:e})},Ts=new ht,Es=`lit`,Ds=(e,t)=>e===`normals`?ws:Cs(t),Os=`–`,ks=!0,As=null;function js(e){let t=e===void 0?In(vs):e;t!==null&&(As=t,Bn(t,ds,fs),Ms=ds.position.distanceTo(fs?.target??t.center),ks=!1)}var Ms=0;function Ns(){if(As===null||fs===void 0)return;let e=ds.position.distanceTo(fs.target);if(e>Ms*.8&&e<Ms*1.25)return;Ms=e;let{near:t,far:n}=zn(As,e);ds.near=t,ds.far=n,ds.updateProjectionMatrix()}var Ps=null;function Fs(e){e!==Es&&(Es=e,Ps!==null&&Is(Ps.items,{...Ps.info,fresh:!1}))}function Is(e,t){Ps={items:e,info:t};for(let e of ys)vs.remove(e);Fn(ys),ys=[];let n=new Map;for(let e of t.outputs)n.has(e)||n.set(e,n.size);let r=new Map;for(let[i,a]of e.entries()){let{objects:e,report:o}=Pn(a,{assets:_s,materials:Ds(Es,n.get(t.outputs[i]??``)??0),pointSize:.16,...Es===`normals`?{instanceMaterial:Ts}:{}});for(let t of e)vs.add(t);ys.push(...e);for(let e of o.drew)r.set(e,(r.get(e)??0)+1);o.skipped!==void 0&&r.set(`skipped: ${o.skipped}`,1)}Os=r.size===0?`nothing`:[...r].map(([e,t])=>t>1?`${t}× ${e}`:e).join(` · `),t.fresh&&(ks=!0);let i=In(vs);i!==null&&(As=i,gs(Hn(i)),ks&&js(i))}var Ls={};Ls.setShading=e=>{Fs(e),Bs()},Ls.frame=js;var Rs=`–`,zs={path:`cpu`,ready:!1,label:``,error:null},Bs=()=>Ls.publish?.({fps:Rs,drew:Os,gpu:zs,shading:Es});function Vs(e){Bs()}var Hs=new ir({render:Is,status:Vs}),Us=document.getElementById(`editor`);if(!Us)throw Error(`missing #editor element`);N(ls,{target:Us,props:{controller:Hs,bridge:Ls}}),Bs();var Ws;function Gs(){if(Ws===void 0||zs.path===`cpu`){Hs.setGpuResolver(void 0);return}Hs.setGpuResolver(zs.path===`gpu-fused`?Ws.fused:Ws.perNode)}Ls.setCookPath=e=>{zs={...zs,path:e},Gs(),Bs()};var Ks=!1;(async()=>{let e=await Dn(e=>{Ks=!0,Ws=void 0,zs={path:`cpu`,ready:!1,label:``,error:`device lost — ${e}`},Gs(),Hs.abandonCook(`device lost — ${e}`),Bs()});if(`error`in e){zs={path:`cpu`,ready:!1,label:``,error:e.error},Gs(),Bs();return}Ks||(Ws=jn(e),zs={...zs,ready:!0,label:e.label,error:null},Gs(),Bs())})();var qs=u(e=>{Rs=e,Bs()});ps(()=>{Ns(),qs()});