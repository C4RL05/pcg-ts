import{$ as e,Z as t}from"./mobile-DCLgnEZN.js";function n(n,r){let i=[];for(let a of n){let n=r[a.assetId];if(!n){let e=Object.keys(r).sort().join(`, `);throw Error(`toInstancedMeshes: unknown assetId "${a.assetId}"; known assets: `+(e===``?`(none)`:e))}if(a.transforms.length!==a.count*16)throw Error(`toInstancedMeshes: batch "${a.assetId}" has ${a.transforms.length} transform floats, expected count * 16 = ${a.count*16}`);if(a.colors&&a.colors.length!==a.count*3)throw Error(`toInstancedMeshes: batch "${a.assetId}" has ${a.colors.length} colour floats, expected count * 3 = ${a.count*3} (rgb per instance; alpha is dropped at the spawner)`);let o=new e(n.geometry,n.material,a.count);o.instanceMatrix.array.set(a.transforms),o.instanceMatrix.needsUpdate=!0,a.colors&&(o.instanceColor=new t(a.colors.slice(),3),o.instanceColor.needsUpdate=!0),o.name=a.assetId,o.computeBoundingSphere(),i.push(o)}return i}export{n as t};